

# 内参感知基础先验光束法平差

## 联合针孔标定、多视图几何与偏差校正的学习先验

---

### 摘要

前向推理型三维基础模型可直接从图像中预测相机位姿、深度图、点图、跟踪点、法向量，有时也包括内参。这些预测功能强大，但在几何层面十分脆弱：许多此类模型假设针孔相机的主点位于图像中心，而真实图像可能经过非对称裁剪、视频稳像或其他处理，从而对应一个主点非居中的针孔相机。我们提出**内参感知基础先验光束法平差**，把它表述为一个联合优化目标，但当前工作的重点不是立刻实现完整 joint solver，而是先判断哪些先验项有资格进入该目标。

核心难点在于：当真实主点偏离中心时，基于居中主点模型所产生的学习输出并非无偏的含噪测量，而是对错误内参假设的有偏响应。为此，我们推导了校正、过滤或延迟这些先验所需的几何理论。焦距误差表现为图像平面方向上的各向异性拉伸，而主点误差则表现为相机坐标系中的秩一剪切：

$$H - I = s e_3^T, \qquad s = (\beta_x, \beta_y, 0)^T$$

我们推导了该剪切如何被刚性位姿、Schur 补光束法平差、逐点深度以及曼哈顿法向正则化所吸收。在消除点之后，主点漂移通常被吸收为偏航和俯仰旋转而非横向平移，而恒定深度平面则暴露出平移-旋转的不可辨识性。我们进一步证明：不兼容的逐视图剪切请求意味着残差，但不自动意味着曲率；只有通过特定的非仿射响应算子（如空间变化的重投影权重、可见性、置信度、遮挡或曼哈顿法向耦合），弯曲才会出现。当前阶段的核心实验不是比较哪条 pipeline 暂时最好，而是用 `raw/direct`、`corrected`、`rerun` 三类典型路径去测量 `corrected-rerun gap`：这个距离决定了几何校正先验是否有资格进入未来的联合优化。

---

## 1. 引言

### 1.1 结合学习先验的联合几何优化

经典多视图几何能够从对应点中估计相机内参和位姿。当存在足够多的静态、分布均匀的匹配点以及非退化的相机运动时，该方法十分可靠；但在纹理稀疏、基线狭窄、动态物体、低视差或主点与焦距约束不足的情况下，问题将变得病态。

近年来的三维基础模型提供了一种互补策略：在仍强制执行多视图几何的同时，将前向推理预测作为弱先验使用。我们提出如下联合目标函数：

$$\min_{K,\{T_t\},\{X_j\},\{Z_t\}} E_{\mathrm{corr}} + \lambda_{\mathrm{BA}} E_{\mathrm{reproj}} + \lambda_{\mathrm{FM}} E_{\mathrm{FM}} + \lambda_K E_K.$$

其中 $E_{\mathrm{corr}}$ 和 $E_{\mathrm{reproj}}$ 约束多视图几何，$E_K$ 对共享针孔内参进行正则化，而 $E_{\mathrm{FM}}$ 将解拉向基础模型的预测：

$$E_{\mathrm{FM}} = E_{\mathrm{pose}}(T_t, \widehat{T}_t) + E_{\mathrm{depth}}(Z_t, \widehat{Z}_t) + E_{\mathrm{normal}}(N_t, \widehat{N}_t) + E_{\mathrm{point}}(X_j, \widehat{X}_j).$$

该流水线的吸引力在于：学习预测可对病态标定与重建进行正则化，尤其是在对应几何噪声大、稀疏或局部退化的情况下。但学习输出只有在作为有效弱先验时才有用。若基础模型在错误的相机假设下运行，则它们未必有效。

这里有一个研究设计上的约束：当前阶段**故意没有直接实现完整联合优化器**。原因不是理论目标不需要它，而是如果在不知道每个先验项是否有效、是否稳定、是否可校正之前就把所有项耦合进一个 joint objective，那么即使系统指标变化，我们也无法判断到底是哪一项真正起作用、哪一项在作恶。当前工作的任务是先做**约束资格审查**，再决定哪些先验有资格被纳入最终联合优化。

### 1.2 先验有效性问题

许多前向推理型三维系统假设居中针孔相机。在这类系统中，内参矩阵实际上被限定为

$$K_0 = \begin{bmatrix} f_x & 0 & W/2 \\ 0 & f_y & H/2 \\ 0 & 0 & 1 \end{bmatrix}.$$

而真实针孔相机的内参可能是

$$K^* = \begin{bmatrix} f_x^* & 0 & c_x^* \\ 0 & f_y^* & c_y^* \\ 0 & 0 & 1 \end{bmatrix},$$

其中

$$(c_x^*, c_y^*) \neq (W/2, H/2).$$

若 $K^*$ 已知，自然的工程解决方案十分简单：将输入图像重映射到居中规范相机 $K_\star$，再运行居中相机模型。对于无畸变针孔相机，这是一个确定性图像变形：

$$I_\star(q_\star) = I(K^* K_\star^{-1} q_\star).$$

这不需要场景深度，因为它只改变了在同一光学中心下的相机射线参数化。但在实际联合系统中，往往在正确的 $K^*$ 未知时就已对原始图像进行了学习预测。这些预测是以模型的居中相机惯例为条件生成的。因此，

$$(\widehat{T}_t, \widehat{Z}_t, \widehat{N}_t, \widehat{X}_j)$$

不应被解释为真实几何的无偏含噪测量，而应视为错误内参下的有偏响应。

本文解决的正是使学习先验联合标定问题具有良好定性所必需的几何问题：

> 若将一幅主点非居中的针孔图像在居中主点相机假设下解释，我们应在旋转、平移、焦距、深度、平面法向量以及多视图曲面几何中预期怎样的系统性偏差？

这一问题对于学习先验优化而言是特有的：纯对应点优化器无需知道基础模型如何失效即可估计几何，而学习先验优化器在信任先验之前必须理解偏差结构。更具体地说，我们最终要解决的是一个**优化约束问题**，不是单纯的求解问题：求解器再强，也不能稳定地从错误约束中恢复正确解。

### 主要贡献

1. 提出**内参感知基础先验光束法平差**，一种用于共享针孔标定、相机位姿、三维结构以及学习位姿/深度/法向/点先验的联合优化框架。
2. 形式化**先验有效性问题**：基础模型输出在居中主点假设下生成、却应用于主点非居中图像时，会产生偏差。
3. 将内参失配表示为相机坐标仿射映射 $H = \widehat{K}^{-1} K^*$，将焦距拉伸与主点剪切分离。
4. 证明当深度可自由重分配时，单视图重投影存在无穷多个精确的错误内参解，因而单视图残差无法诊断偏差。
5. 推导内参剪切在 $SE(3)$ 上的最小二乘刚性投影，给出主点漂移的偏航/俯仰激励的闭合解。
6. 利用流形上 Jacobian 和 Schur 补点消元推导一阶光束法平差响应，给出内参误差到位姿和结构偏差的显式局部映射。
7. 证明在消除点之后且深度非退化变化的条件下，主点误差是一阶旋转性的而非平移性的；平移在恒定深度场景中以软不可辨识性的形式出现。
8. 推导错误内参对曼哈顿平面法向量的影响，并区分重投影补偿度量与曼哈顿框架度量。
9. 证明不兼容的逐视图仿射剪切本身并不意味着曲率；常系数融合给出仿射平均。并推导出一个局部约化二次模型，其中曲率通过空间变化的重投影、可见性或置信度权重而出现。
10. 将上述推导转化为实用的先验策略：学习先验应在对预期错误内参响应建模之后，再进行校正、去偏差、过滤或延迟使用。
11. 明确当前阶段的核心实验判据是 `corrected-rerun gap`：若几何校正后的先验足够接近正确内参下的 rerun 输出，则这些先验有资格进入未来联合优化；若距离仍大，则说明约束建模尚未闭合。

---

## 2. 问题设置

设 $K^*$ 为真实针孔内参，$\widehat{K}$ 为重建系统所假设的内参。定义

$$H = \widehat{K}^{-1} K^* = \begin{bmatrix} \alpha_x & 0 & \beta_x \\ 0 & \alpha_y & \beta_y \\ 0 & 0 & 1 \end{bmatrix},$$

其中

$$\alpha_x = \frac{f_x^*}{\widehat{f}_x}, \qquad \beta_x = \frac{c_x^* - \widehat{c}_x}{\widehat{f}_x},$$

$y$ 方向类似。在相机坐标系中，

$$H \begin{bmatrix} X \\ Y \\ Z \end{bmatrix} = \begin{bmatrix} \alpha_x X + \beta_x Z \\ \alpha_y Y + \beta_y Z \\ Z \end{bmatrix}.$$

因此，焦距误差是图像平面方向上的各向异性拉伸，而主点误差是一个秩一剪切：

$$H - I = s e_3^T, \qquad s = (\beta_x, \beta_y, 0)^T$$

（纯主点漂移情形）。在归一化图像坐标中，

$$\pi\!\left(H[X, Y, Z]^T\right) = \begin{bmatrix} \alpha_x X/Z + \beta_x \\ \alpha_y Y/Z + \beta_y \end{bmatrix}.$$

主点漂移因此表现为归一化图像平面上的常数偏移，而焦距漂移表现为缩放。这一简单观察是全部困难的根源。

---

## 3. 可辨识性全貌

决定性的事实是：未标定图像对应点仅能确定重建到三维射影变换等价类为止。从射影类过渡到度量或欧氏重建，需要标定信息，经典方法通过无穷远平面和绝对二次曲线的像来实现。

在光束法平差语言中，这是一个**规范（gauge）问题**：

- 欧氏重建具有 7 自由度的相似变换规范。
- 射影重建具有 15 自由度的射影变换规范。

曼哈顿世界假设对主方向或平面法向量施加正交约束，从而提供恰好能够打破部分射影不可辨识性的额外几何信息。

### 3.1 单视图不唯一性

"错误主点意味着正残差"这一论断对于单视图而言是假的，前提是深度可以自由重分配。

固定真实相机坐标系中的点

$$p_i^* = T^* X_i^*.$$

定义错误内参射线

$$\tilde{r}_i := \frac{H p_i^*}{e_3^T H p_i^*} \in \mathbb{R}^3.$$

对任意选取的 $\widehat{T} \in SE(3)$ 以及任意正标量 $\rho_i$，定义

$$\widehat{X}_i := \widehat{T}^{-1}(\rho_i \tilde{r}_i).$$

则

$$\pi(\widehat{T} \widehat{X}_i) = \pi(\rho_i \tilde{r}_i) = \pi(H p_i^*).$$

重投影误差精确为零。不仅存在不唯一性，还存在无穷多个精确的错误内参单视图解。这与未标定重建所蕴含的射影不唯一性完全相同。

### 3.2 多视图度量相容性

有意义的陈述是多视图的：

> 若单一公共三维结构以及所有相机均被约束使用同一固定的错误内参 $\widehat{K}$，则精确零残差意味着同一射影重建允许两种欧氏升级，一种对应 $K^*$，另一种对应 $\widehat{K}$。

对于一般非平面场景和一般非临界运动，自标定在相似变换意义下局部唯一。因此，第二种升级不可能存在，除非 $\widehat{K}$ 与 $K^*$ 属于同一度量升级类，而一般情况下这强迫

$$\widehat{K} = K^*.$$

错误内参只有在退化情形（如平面场景、纯旋转或其他临界运动序列）下才能保持精确相容。

这里必须区分两类常被混同的问题。第一类是“同一位置、同一张照片、同一光心”下把非中心主点图像改写成中心主点图像；在恒定深度平面上，它确实等价于横向平移。第二类是本文关心的问题：同一个非中心主点相机在运动中拍下一段视频，而所有帧通过共享三维结构耦合。此时每一帧都在各自相机坐标系中施加同一个剪切，但这些逐视图剪切一般不能被一个共享世界平移同时解释，因此问题的主导吸收模式转为多视图 BA 中的旋转响应。

---

## 4. 内参剪切的刚性投影

我们首先考察仿射映射 $H$ 如何在单个相机坐标系中被一个刚性运动近似。

设 $p_i^* = T^* X_i^*$ 为相机坐标系中的点。定义

$$\bar{p} = \frac{1}{N} \sum_i p_i^*, \qquad q_i = p_i^* - \bar{p}.$$

令

$$H = I + E, \qquad R = I + [\delta\omega]_\times + O(\|E\|^2), \qquad t = \delta t + O(\|E\|^2).$$

对

$$\sum_i \|H p_i^* - (R p_i^* + t)\|^2$$

求最小化，一阶近似给出

$$\delta t = E\bar{p} - \delta\omega \times \bar{p},$$

以及

$$(\operatorname{tr}\Sigma\, I - \Sigma)\,\delta\omega = \frac{1}{N} \sum_i q_i \times E q_i,$$

其中

$$\Sigma := \frac{1}{N} \sum_i q_i q_i^T.$$

等价地，若 $C = H\Sigma$，则精确最小二乘旋转为极分解因子

$$\widehat{R} = \operatorname{polar}(C).$$

线性化反对称矩阵 $\Omega = [\delta\omega]_\times$ 满足

$$\Omega\Sigma + \Sigma\Omega = E\Sigma - \Sigma E^T.$$

这正是刚性投影的精确代数结构：保留仿射扰动的刚性部分，丢弃对称的非刚性部分。

### 4.1 纯主点剪切

对于纯主点剪切，

$$E = \beta_x e_1 e_3^T + \beta_y e_2 e_3^T.$$

则

$$\frac{1}{N} \sum_i q_i \times E q_i = \begin{bmatrix} -\beta_y \Sigma_{zz} \\ \beta_x \Sigma_{zz} \\ \beta_y \Sigma_{xz} - \beta_x \Sigma_{yz} \end{bmatrix}.$$

因此，主点误差主要激励俯仰和偏航，只有在点云存在 $xz$ 或 $yz$ 协方差时才激励滚转。

在常见的对称情形

$$\Sigma = \operatorname{diag}(\sigma_x^2, \sigma_y^2, \sigma_z^2)$$

下，我们得到

$$\delta\omega_x = -\beta_y \frac{\sigma_z^2}{\sigma_y^2 + \sigma_z^2},$$

$$\delta\omega_y = \beta_x \frac{\sigma_z^2}{\sigma_x^2 + \sigma_z^2},$$

以及

$$\delta\omega_z = 0.$$

平移量为

$$\delta t = E\bar{p} - \delta\omega \times \bar{p}.$$

在极限情形 $\sigma_z^2 \gg \sigma_x^2, \sigma_y^2$ 下，刚性吸收量趋近于

$$\delta\omega \approx (-\beta_y, \beta_x, 0),$$

这恰好是在光轴附近模拟主点偏移的无穷小相机旋转。

---

## 5. 光束法平差响应

设

$$p_{ti} = R_t^* X_i^* + t_t^* = (X_{ti}, Y_{ti}, Z_{ti})^T,$$

并定义

$$x_{ti} = \frac{X_{ti}}{Z_{ti}}, \qquad y_{ti} = \frac{Y_{ti}}{Z_{ti}}.$$

使用左乘位姿扰动

$$\widehat{T}_t = \exp((\Delta\xi_t)^\wedge) T_t^*, \qquad \Delta\xi_t = \begin{bmatrix} \Delta\rho_t \\ \Delta\phi_t \end{bmatrix} \in \mathbb{R}^6,$$

并令 $\widehat{X}_i = X_i^* + \Delta X_i$。

预测像素为

$$z_{ti}(\theta_K, \xi_t, X_i) = \begin{bmatrix} f_x X_{ti}/Z_{ti} + c_x \\ f_y Y_{ti}/Z_{ti} + c_y \end{bmatrix}.$$

在真实状态处的测量 Jacobian 为

$$G_{p,ti} := \frac{\partial z_{ti}}{\partial p_{ti}} = \begin{bmatrix} f_x/Z_{ti} & 0 & -f_x X_{ti}/Z_{ti}^2 \\ 0 & f_y/Z_{ti} & -f_y Y_{ti}/Z_{ti}^2 \end{bmatrix},$$

$$G_{\xi,ti} := \frac{\partial z_{ti}}{\partial \Delta\xi_t} = G_{p,ti} \begin{bmatrix} I & -[p_{ti}]_\times \end{bmatrix},$$

$$G_{X,ti} := \frac{\partial z_{ti}}{\partial \Delta X_i} = G_{p,ti} R_t^*.$$

内参 Jacobian 为

$$G_{K,ti} := \frac{\partial z_{ti}}{\partial (f_x, f_y, c_x, c_y)} = \begin{bmatrix} x_{ti} & 0 & 1 & 0 \\ 0 & y_{ti} & 0 & 1 \end{bmatrix}.$$

展开 $G_{\xi,ti}$，记 $X, Y, Z = (X_{ti}, Y_{ti}, Z_{ti})$：

$$G_{\xi,ti} = \begin{bmatrix} \dfrac{f_x}{Z} & 0 & -\dfrac{f_x X}{Z^2} & -\dfrac{f_x XY}{Z^2} & f_x\!\left(1+\dfrac{X^2}{Z^2}\right) & -\dfrac{f_x Y}{Z} \\[6pt] 0 & \dfrac{f_y}{Z} & -\dfrac{f_y Y}{Z^2} & -f_y\!\left(1+\dfrac{Y^2}{Z^2}\right) & \dfrac{f_y XY}{Z^2} & \dfrac{f_y X}{Z} \end{bmatrix}.$$

这些是流形上光束法平差所使用的解析 Jacobian。

### 5.1 一阶驻点映射

设 $r$ 为堆叠重投影残差向量。一阶近似下，

$$r \approx -G_\Xi \Delta\Xi - G_X \Delta X - G_K \Delta\theta_K.$$

在状态空间的规范固定切片上，驻点映射的隐函数定理导数为

$$\frac{\partial(\Delta\Xi, \Delta X)}{\partial \theta_K} = -\mathcal{H}^{-1} \mathcal{B}_K,$$

其中

$$\mathcal{H} = \begin{bmatrix} U & W \\ W^T & V \end{bmatrix} = \begin{bmatrix} G_\Xi^T G_\Xi & G_\Xi^T G_X \\ G_X^T G_\Xi & G_X^T G_X \end{bmatrix} + \lambda \nabla^2 \mathcal{E}_{\text{prior}},$$

以及

$$\mathcal{B}_K = \begin{bmatrix} G_\Xi^T G_K \\ G_X^T G_K \end{bmatrix}.$$

若不固定规范，$\mathcal{H}^{-1}$ 应替换为 Moore-Penrose 伪逆，因为相似变换规范存在零空间。

通过 Schur 补消去点，得到

$$\Delta\Xi = -\underbrace{(U - WV^{-1}W^T)^{-1}}_{S^{-1}} \underbrace{(G_\Xi^T G_K - WV^{-1} G_X^T G_K)}_{\Gamma_K} \Delta\theta_K.$$

因此

$$\Delta\xi_t = \mathcal{F}_t(T_t^*, \mathcal{X}^*, \Delta K) = -[e_t^T S^{-1} \Gamma_K] \Delta\theta_K,$$

其中 $e_t$ 选取第 $t$ 个 6 维位姿块。这正是稀疏光束法平差中熟知的约化相机系统：逐点深度的自由度被压缩进 Schur 项 $WV^{-1}W^T$，只有在消点后仍存在的内参强迫项才会驱动位姿偏差。

### 5.2 光轴附近的主点漂移

设

$$\beta_x = \frac{c_x^* - \widehat{c}_x}{\widehat{f}_x} \approx -\frac{\Delta c_x}{f_x^*}, \qquad \beta_y = \frac{c_y^* - \widehat{c}_y}{\widehat{f}_y} \approx -\frac{\Delta c_y}{f_y^*}.$$

在光轴附近，仅保留主导位姿分量，归一化图像扰动模型为

$$\delta x_i \approx -\frac{\Delta t_x}{Z_i} + \omega_y,$$

$$\delta y_i \approx -\frac{\Delta t_y}{Z_i} - \omega_x.$$

主点子问题解耦为：

$$\min_{\Delta t_x, \omega_y} \sum_i \left(\beta_x + \frac{\Delta t_x}{Z_i} - \omega_y\right)^2,$$

以及

$$\min_{\Delta t_y, \omega_x} \sum_i \left(\beta_y + \frac{\Delta t_y}{Z_i} + \omega_x\right)^2.$$

$x$ 方向的 Hessian 矩阵为

$$H_x = \begin{bmatrix} \sum_i Z_i^{-2} & -\sum_i Z_i^{-1} \\ -\sum_i Z_i^{-1} & N \end{bmatrix}.$$

当 $\operatorname{Var}(1/Z_i) > 0$ 时，法方程的精确解为

$$\omega_y = \beta_x, \qquad \Delta t_x = 0,$$

以及

$$\omega_x = -\beta_y, \qquad \Delta t_y = 0.$$

因此，在一般非退化深度状态下，

$$\frac{\partial \omega_y}{\partial \Delta c_x} \approx -\frac{1}{f_x^*}, \qquad \frac{\partial \omega_x}{\partial \Delta c_y} \approx \frac{1}{f_y^*},$$

以及

$$\frac{\partial \Delta t_x}{\partial \Delta c_x} \approx \frac{\partial \Delta t_y}{\partial \Delta c_y} \approx 0.$$

因此，在消点后的此近轴约化模型下，主点误差是一阶旋转性的，而非平移性的。

### 5.3 恒定深度退化

若 $Z_i \equiv Z_0$，则

$$\det H_x = N \sum_i Z_i^{-2} - \left(\sum_i Z_i^{-1}\right)^2 = 0.$$

精确一阶补偿族变为

$$\omega_y - \frac{\Delta t_x}{Z_0} = \beta_x, \qquad -\omega_x - \frac{\Delta t_y}{Z_0} = \beta_y.$$

正面朝向的恒定深度平面无法区分主点偏移与耦合的偏航/平移或俯仰/平移模式。

对于 $Z \sim \operatorname{Uniform}[Z_{\min}, Z_{\max}]$，有

$$\mathbb{E}[Z^{-1}] = \frac{\ln(Z_{\max}/Z_{\min})}{Z_{\max} - Z_{\min}}, \qquad \mathbb{E}[Z^{-2}] = \frac{1}{Z_{\min} Z_{\max}}.$$

因此

$$\frac{\det H_x}{N^2} = \frac{1}{Z_{\min} Z_{\max}} - \left(\frac{\ln(Z_{\max}/Z_{\min})}{Z_{\max} - Z_{\min}}\right)^2 > 0.$$

只要逆深度方差非零，退化即被打破。恒定深度极限中的软特征方向为

$$v_{\text{soft}}^{(x)} \propto (Z_0, 1), \qquad v_{\text{soft}}^{(y)} \propto (Z_0, -1),$$

分别对应 $(\Delta t_x, \omega_y)$ 和 $(\Delta t_y, \omega_x)$ 的耦合不可辨识性。

---

## 6. 曼哈顿约束下的平面法向量

对于相机坐标系中某个真实平面

$$\Pi_{\text{true}}: \quad n^{*T} X = d,$$

错误内参、无先验重建仍精确为平面。若 $X = Z K^{*-1} q$ 是射线 $q$ 上的真实点，则错误射线重建为

$$\widehat{X} = HX.$$

重建平面为

$$\widehat{\Pi}_0: \quad \tilde{n}^T \widehat{X} = d, \qquad \tilde{n} = H^{-T} n^*.$$

由于

$$H^{-T} = \begin{bmatrix} 1/\alpha_x & 0 & 0 \\ 0 & 1/\alpha_y & 0 \\ -\beta_x/\alpha_x & -\beta_y/\alpha_y & 1 \end{bmatrix},$$

精确的无约束法向量为

$$\tilde{n} = \begin{bmatrix} n_x^*/\alpha_x \\ n_y^*/\alpha_y \\ n_z^* - \beta_x n_x^*/\alpha_x - \beta_y n_y^*/\alpha_y \end{bmatrix},$$

归一化法向量为

$$\widehat{n}_0 = \frac{\tilde{n}}{\|\tilde{n}\|}.$$

因此，正面朝向平面（$n^* = e_3$）不受主点漂移影响，而侧墙或地板因其法向量有 $x$ 或 $y$ 分量而发生倾斜。

记

$$\widehat{f}_x = f_x^* + \Delta f_x, \qquad \widehat{c}_x = c_x^* + \Delta c_x,$$

则有

$$\alpha_x = 1 - \frac{\Delta f_x}{f_x^*} + O(\|\Delta K\|^2), \qquad \beta_x = -\frac{\Delta c_x}{f_x^*} + O(\|\Delta K\|^2),$$

$y$ 方向类似。因此

$$\tilde{n} = n^* + \begin{bmatrix} (\Delta f_x / f_x^*) n_x^* \\ (\Delta f_y / f_y^*) n_y^* \\ (\Delta c_x / f_x^*) n_x^* + (\Delta c_y / f_y^*) n_y^* \end{bmatrix} + O(\|\Delta K\|^2).$$

归一化后，

$$\widehat{n}_0 = n^* + P_{n^*}^\perp \begin{bmatrix} (\Delta f_x/f_x^*) n_x^* \\ (\Delta f_y/f_y^*) n_y^* \\ (\Delta c_x/f_x^*) n_x^* + (\Delta c_y/f_y^*) n_y^* \end{bmatrix} + O(\|\Delta K\|^2),$$

其中

$$P_{n^*}^\perp = I - n^* n^{*T}.$$

主点分量通过扰动法向量的 $z$ 分量引入倾斜，而焦距误差则对平面内分量进行重新缩放。

### 6.1 强曼哈顿先验

对于孤立平面，没有正交惩罚可施加。若局部曼哈顿三元组为

$$N^* = [n_1^*, n_2^*, n_3^*] \in SO(3),$$

则无约束变换三元组为

$$\tilde{N} = H^{-T} N^*.$$

正交正则化问题（带单位范数约束）的驻点方程为

$$\widehat{n}_k - \tilde{n}_k + \lambda \sum_{\ell \perp k} (\widehat{n}_k^T \widehat{n}_\ell) \widehat{n}_\ell + \mu_k \widehat{n}_k = 0, \qquad \|\widehat{n}_k\| = 1.$$

这是精确的隐式方程组。在强先验状态下，显式渐近解是最近正交标架：

$$\widehat{N} = \operatorname{polar}(\tilde{N}) = \operatorname{polar}(H^{-T} N^*) + O(\|\Delta K\|^2, \lambda^{-1}).$$

因此，曼哈顿先验只保留法向量畸变的正交部分，而排除非正交的剪切部分。

对于纯主点剪切，$\alpha_x = \alpha_y = 1$，故

$$H^{-T} = I - F^T, \qquad F = \begin{bmatrix} 0 & 0 & \beta_x \\ 0 & 0 & \beta_y \\ 0 & 0 & 0 \end{bmatrix}.$$

极分解因子的无穷小旋转为

$$\omega_{\mathrm{MW}} = \left(-\frac{\beta_y}{2}, \frac{\beta_x}{2}, 0\right) = \left(\frac{\Delta c_y}{2 f_y^*}, -\frac{\Delta c_x}{2 f_x^*}, 0\right) + O(\|\Delta K\|^2).$$

在强曼哈顿极限下，优化器将法向量框架的剪切转化为三元组的整体俯仰-偏航旋转。出现 $1/2$ 因子并非矛盾于重投影补偿

$$(\omega_x, \omega_y) = (-\beta_y, \beta_x).$$

前者是剪切后法向量矩阵的最近正交框架；后者是消点后的最近重投影补偿量。它们是在两种不同度量下的投影。

---

## 7. 残差分布：剪切、平均与曲率

主点误差本身是一种类剪切仿射畸变。即便是不兼容的逐视图仿射畸变也不自动意味着曲率，它们意味着残差。曲率只有在特定融合能量以非仿射方式对该残差进行空间分布后才会出现。

对于视图 $t$，错误内参矩阵所引发的一阶世界坐标位移为

$$u_t(X) = R_t^{*T}(H - I)(R_t^* X + t_t^*) = A_t X + b_t.$$

对于纯主点漂移，

$$H - I = s e_3^T, \qquad s = (\beta_x, \beta_y, 0)^T,$$

因此

$$A_t = R_t^{*T} s (e_3^T R_t^*) = a_t r_{3,t}^{*T},$$

其中 $a_t := R_t^{*T} s$。这是世界坐标中的一个秩一剪切。若所有视图与某个公共仿射场相容，则重建流形在全局上被剪切：平面仍为平面，零高斯曲率保持为零，误差不是弯曲。

一般多视图运动会破坏相容性，因为 $A_t = R_t^{*T}(H - I)R_t^*$ 取决于相机朝向。不同视图请求不同的剪切方向和深度相关偏移，没有任何一个全局仿射变形能同时满足所有请求。然而，不相容性本身尚不构成曲率定理。

### 7.1 无弯曲类

考虑常权重仿射融合能量

$$E_{\mathrm{aff}}[u] = \int \sum_t w_t \|u(X) - u_t(X)\|^2\, d\mu(X),$$

其中 $w_t > 0$ 为常数。逐点最小化器为

$$u^\star(X) = \bar{A} X + \bar{b},$$

其中

$$\bar{A} = \frac{\sum_t w_t A_t}{\sum_t w_t}, \qquad \bar{b} = \frac{\sum_t w_t b_t}{\sum_t w_t}.$$

因此，常权重最小二乘融合仿射请求的结果仍为仿射。存在残差

$$u^\star(X) - u_t(X) = (\bar{A} - A_t) X + (\bar{b} - b_t),$$

但不会弯曲平面。

这并非病理性反例，而是整个常系数二次融合类的普遍规律。更一般地，若

$$E_{\mathrm{const}}[u] = \int \sum_t (u(X) - u_t(X))^T W_t (u(X) - u_t(X))\, d\mu(X),$$

其中 $W_t \succeq 0$ 与 $X$ 无关，则在 $\sum_t W_t$ 非奇异时，

$$u^\star(X) = \left(\sum_t W_t\right)^{-1} \sum_t W_t (A_t X + b_t),$$

结果仍为仿射。因此，在任何常权重一阶位移融合模型中，残差不相容不足以产生曲率。

### 7.2 约化重投影模型

实际基于重投影的融合通常不属于常系数类。线性化后，局部位移通道具有如下形式：

$$E_{\mathrm{red}}[u] = \int \sum_t (u(s) - u_t(s))^T W_t(s) (u(s) - u_t(s))\, ds,$$

其中

$$W_t(s) = J_t(s)^T J_t(s)$$

是该曲面坐标处所见到的约化重投影、Schur 补、置信度和可见性权重。即便特征置信度为常数，$J_t(s)$ 也随深度、视角、投影尺度、遮挡和曲面参数化而变化。这正是无条件弯曲断言中所缺少的非仿射响应算子。

记

$$u_t(s) = b_t + a_t s + O(s^2),$$

$$W_t(s) = W_{t0} + W_{t1} s + \frac{1}{2} W_{t2} s^2 + O(s^3).$$

定义

$$M_k = \sum_t W_{tk}, \qquad q_0 = \sum_t W_{t0} b_t,$$

$$q_1 = \sum_t (W_{t0} a_t + W_{t1} b_t),$$

$$q_2 = \sum_t \left(W_{t1} a_t + \frac{1}{2} W_{t2} b_t\right).$$

逐点最小化器满足

$$u^\star(s) = M(s)^{-1} q(s) = c_0 + c_1 s + c_2 s^2 + O(s^3),$$

其中

$$c_0 = M_0^{-1} q_0,$$

$$c_1 = M_0^{-1}(q_1 - M_1 c_0),$$

$$c_2 = M_0^{-1}\!\left(q_2 - M_1 c_1 - \frac{1}{2} M_2 c_0\right).$$

弯曲通道为 $2c_2$。若对所有视图均有 $W_{t1} = W_{t2} = 0$，则 $c_2 = 0$，响应为仿射。若约化权重的空间变化与仿射请求不对齐，则 $c_2$ 一般非零。代数上，$c_2$ 是

$$\{W_{t0}, W_{t1}, W_{t2}, a_t, b_t\}$$

的解析函数，且不恒为零；除非存在额外对称性强迫，否则其零集为低维集合。

### 7.3 标量可见性与置信度模型

标量可见性/置信度模型是矩阵公式的可交换特例。设

$$f_t(s) = a_t s + b_t + O(s^2),$$

$$w_t(s) = w_{t0}\!\left(1 + \eta_t s + \frac{1}{2}\kappa_t s^2 + O(s^3)\right),$$

其中 $w_{t0} > 0$。令

$$\pi_t = \frac{w_{t0}}{\sum_j w_{j0}}, \qquad \langle g \rangle = \sum_t \pi_t g_t.$$

逐点最小化器为

$$\zeta^\star(s) = \frac{\sum_t w_t(s) f_t(s)}{\sum_t w_t(s)} = c_0 + c_1 s + c_2 s^2 + O(s^3),$$

其中

$$c_0 = \langle b \rangle,$$

$$c_1 = \langle a \rangle + \operatorname{Cov}(b, \eta),$$

$$c_2 = \operatorname{Cov}(a, \eta) + \frac{1}{2}\operatorname{Cov}(b, \kappa) - \langle\eta\rangle \operatorname{Cov}(b, \eta).$$

因此

$$\zeta^{\star\prime\prime}(0) = 2c_2.$$

弯曲仅在可见性或置信度的空间变化与不兼容的仿射斜率或偏移相关时出现。

#### 协方差解释

局部标量模型中的主导项为

$$2\operatorname{Cov}(a, \eta).$$

因此，在该约化模型的二阶近似下，弯曲不是由视图不相容性单独引起的，而是由以下两者的协方差引起的：

- 每个视图所请求的剪切斜率 $a_t$；
- 该视图融合权重、可见性或置信度的局部空间梯度 $\eta_t$。

等价地，当主导重建的视图随曲面块变化，且这些变化中的视图请求不同的错误内参剪切斜率时，曲率出现。若所有视图保持常数相对权重，不兼容的剪切请求平均为另一仿射场。若权重空间变化但所有视图请求相同斜率，结果也是仿射。只有当斜率不一致与空间主导权重变化耦合时，弯曲通道才打开。

完整的标量二阶响应为

$$\zeta^{\star\prime\prime}(0) = 2\operatorname{Cov}(a, \eta) + \operatorname{Cov}(b, \kappa) - 2\langle\eta\rangle \operatorname{Cov}(b, \eta).$$

因此，协方差项 $2\operatorname{Cov}(a, \eta)$ 是最简洁的一阶可见性梯度机制，而偏移变化、二阶权重变化、边界、鲁棒核和平滑算子则贡献额外的曲率通道。

### 7.4 平滑与 Euler-Lagrange 响应

若添加薄板或深度平滑项，

$$E_\lambda[\zeta] = \int \sum_t w_t(s)(\zeta(s) - f_t(s))^2\, ds + \lambda \int |\zeta''(s)|^2\, ds,$$

则 Euler-Lagrange 方程为

$$\lambda \zeta''''(s) + W(s)\zeta(s) = R(s),$$

其中

$$W(s) = \sum_t w_t(s), \qquad R(s) = \sum_t w_t(s) f_t(s).$$

对于常数 $W$ 和仿射 $R$，内部解为仿射，边界层效应除外。平滑本身不会从仿射请求中产生弯曲，它只是传播由非仿射强迫、空间变化权重、非线性投影算子、边界、遮挡或正则化约束所产生的弯曲。

### 7.5 曼哈顿法向量的残差分布

对于单个视图，局部仿射位移

$$X \mapsto X + u_t(X)$$

（梯度为 $A_t$）将法向量送至

$$N_t = (I - A_t^T) N^* + O(\|A_t\|^2).$$

在强曼哈顿先验下，逐点法向量框架估计满足

$$N^\star(s) = \operatorname{polar}(M(s)),$$

其中

$$M(s) = \frac{\sum_t w_t(s) N_t}{\sum_t w_t(s)}.$$

一阶近似下，

$$N^\star(s) = (I + [\omega_{\mathrm{MW}}(s)]_\times) N^*,$$

其中

$$[\omega_{\mathrm{MW}}(s)]_\times = \operatorname{skew}(-\bar{A}_w(s)^T), \qquad \bar{A}_w(s) = \frac{\sum_t w_t(s) A_t}{\sum_t w_t(s)}.$$

常数加权平均 $\bar{A}_w$ 只产生常数曼哈顿框架旋转。平面曲率需要空间变化的法向量：

$$\partial_s N^\star(0) = [\omega_{\mathrm{MW}}'(0)]_\times N^*.$$

若 $w_t(s) = w_{t0}(1 + \eta_t s + O(s^2))$，则

$$\omega_{\mathrm{MW}}'(0) = \sum_t \pi_t (\eta_t - \langle\eta\rangle) \omega_{\mathrm{MW},t}.$$

因此，在曼哈顿状态下，弯曲由空间权重梯度与逐视图剪切引发的曼哈顿旋转之间的协方差控制。若所有视图的相对权重为常数，法向量框架仅发生旋转，不产生平面曲率。

### 7.6 单视图深度传输

对于单视图，主点偏移 $\beta = (\beta_x, \beta_y)$ 下，错误射线深度场可在局部写作坐标传输：

$$Z_\beta(u, v) = Z^*(u - \beta_x, v - \beta_y).$$

因此

$$\delta Z_{\text{single}}(u, v) = -\beta \cdot \nabla Z^*(u, v) + O(\|\beta\|^2).$$

这是一阶传输项，而非内蕴曲率。深度误差沿梯度方向移动；它不会对曲面产生内蕴弯曲。

---

## 8. 内参感知基础先验光束法平差

几何原理现在可以总结为：

$$\text{错误内参} \Rightarrow \text{逐视图仿射剪切请求 } u_t(X) = A_t X + b_t.$$

无约束重建可将其保留为射影或仿射规范变化。具有固定欧氏相机的光束法平差将其一部分投影到 $\mathfrak{se}(3)$ 上（对于主点漂移主要是偏航和俯仰），将其余部分边缘化到结构中。但剩余残差的空间分布不由不相容性单独决定：常权重仿射融合给出仿射平均，无弯曲；弯曲需要非仿射响应算子——空间变化的可见性或置信度、非线性重投影权重、遮挡边界、具有非仿射强迫的平滑，或在块上变化的曼哈顿框架权重。

这促使我们为内参感知基础先验光束法平差制定几何感知的先验策略。以标定优先的规范化为最安全的特例，但更广泛的要求是：学习先验必须根据预期的错误内参响应进行校正、去偏差、过滤或延迟。

### 8.1 状态变量与输入

优化状态为

$$\Theta = \left(K,\, \{T_t\}_{t=1}^{M},\, \{X_j\}_{j=1}^{N},\, \{Z_t\},\, \{N_t\},\, \mathcal{S}\right),$$

其中 $K = (f_x, f_y, c_x, c_y)$ 被所有帧共享，$T_t \in SE(3)$ 为相机位姿，$X_j$ 为稀疏或半稠密三维点，$Z_t$ 为可选深度图，$N_t$ 为可选法向量图或曼哈顿框架，$\mathcal{S}$ 包含深度尺度-偏移、鲁棒开关、置信度标定或逐序列裁剪参数等干扰变量。畸变可在后续添加，但核心方法首先求解非居中针孔问题。

输入包括：

- 图像观测 $I_t$；
- 稀疏或稠密对应点 $x_{tj} \leftrightarrow x_{sj}$；
- 跟踪置信度与可见性掩码；
- 基础模型预测 $\widehat{T}_t, \widehat{Z}_t, \widehat{P}_t, \widehat{N}_t$；
- 模型置信度图 $\widehat{C}_t$（若有）；
- 关于焦距、视场角、裁剪或图像分辨率的可选弱元数据先验。

学习预测不直接插入，而是被视为在假设相机 $\widehat{K}$（通常为居中主点）下运行模型所产生的输出。当前标定残差为

$$\Delta K = K - \widehat{K},$$

或等价的归一化失配

$$H = \widehat{K}^{-1} K.$$

### 8.2 完整目标函数

提出的目标函数为

$$\min_{\Theta}\, E_{\mathrm{corr}} + \lambda_{\mathrm{repr}} E_{\mathrm{repr}} + \lambda_{\mathrm{FM}} E_{\mathrm{FM}}^{\mathrm{corr}} + \lambda_K E_K + \lambda_{\mathrm{reg}} E_{\mathrm{reg}}.$$

对应约束项可以射线形式写出：

$$E_{\mathrm{corr}} = \sum_{(s,t),i} \rho_{\mathrm{epi}}\!\left(r_s^i(K)^T [t_{st}]_\times R_{st}\, r_t^i(K)\right),$$

其中 $r_t^i(K) = K^{-1} \tilde{x}_t^i$。当三角化点可用时，重投影项为

$$E_{\mathrm{repr}} = \sum_{t,j} \rho_{\mathrm{repr}}\!\left(\pi_K(T_t X_j) - x_{tj}\right).$$

内参先验为弱显式约束：

$$E_K = \rho_f(f_x, f_y) + \rho_c(c_x, c_y) + \rho_{\mathrm{shared}}(K).$$

其中 $\rho_f$ 可编码方形像素或焦距范围先验，$\rho_c$ 防止主点在约束不足的序列中漂移。共享相机项将所有帧绑定到同一物理针孔相机，除非数据中明确包含逐帧数字稳像或裁剪信息。

正则化项 $E_{\mathrm{reg}}$ 可包含深度平滑、法向量一致性、时序深度一致性或曼哈顿正交性：

$$E_{\mathrm{reg}} = E_{\mathrm{smooth}} + E_{\mathrm{normal}} + E_{\mathrm{MW}} + E_{\mathrm{switch}}.$$

独特之处在于 $E_{\mathrm{FM}}^{\mathrm{corr}}$：学习先验仅在经过错误内参响应的校正、边缘化或过滤之后才进入。

### 8.3 先验状态

基础模型先验可处于以下三种状态之一。

**已校正先验。** 若 $K^*$ 已知或已估计，选取居中目标相机 $K_\star$，重映射

$$I_\star(q_\star) = I(K^* K_\star^{-1} q_\star).$$

然后在 $I_\star$ 上运行基础模型。所得学习预测是最安全的先验，因为输入满足模型的居中主点惯例。

**去偏差先验。** 若模型已在原始非居中图像上运行，其输出应被解释为错误内参响应。上述公式预测了主导偏差通道：偏航/俯仰位姿偏差、深度传输、平面法向量剪切，以及通过非仿射约化权重产生的可能曲率。这些预测可用于在先验进入 $E_{\mathrm{FM}}$ 之前减去或边缘化预期偏差。

**已过滤先验。** 若 $K^*$ 不确定性过大，学习输出应仅通过鲁棒的、低权重的或可切换的残差项进入。残差与预测错误内参响应通道对齐的先验应降权处理，而非视为独立证据。

### 8.4 在信任先验之前估计 $K^*$

若 $K^*$ 未知，则无法直接执行重映射，标定估计必须首先进行。

可能的求解器包括：

- 基于 $F$ 的自标定（从二维对应点出发，利用 $E(K) = K^T F K$ 和本质矩阵约束）；
- 已知二维-三维对应点时主点未知的 PnP；
- 最小绝对位姿求解器，如 P4.5Pfuv 和 P5Pfuva；
- 主点和径向畸变未知的非最小求解器，如 P7Pfruv；
- 共享 $K$ 的光束法平差（仅在重建充分约束后才精化主点）。

最安全的实用流程为：

1. 使用二维-二维或二维-三维几何估计初始 $K$；
2. 在位姿和结构稳定之前，固定或强正则化 $c_x, c_y$；
3. 在全局 BA 中精化共享 $c_x, c_y$；
4. 将所有帧规范化到居中针孔坐标系；
5. 运行前向推理型三维模型；
6. 仅在规范化之后，才以弱、鲁棒、已过滤先验的形式引入模型位姿/深度。

### 8.5 偏差感知学习先验

朴素学习先验项为

$$E_{\mathrm{FM}} = \sum_t \rho_T(T_t, \widehat{T}_t) + \rho_Z(Z_t, \widehat{Z}_t) + \rho_N(N_t, \widehat{N}_t).$$

偏差感知版本应改为使用

$$E_{\mathrm{FM}}^{\mathrm{corr}} = \sum_t w_t^{\mathrm{FM}}\, \rho_T(T_t, \mathcal{C}_T(\widehat{T}_t, \Delta K)) + w_t^Z\, \rho_Z(Z_t, \mathcal{C}_Z(\widehat{Z}_t, \Delta K)) + w_t^N\, \rho_N(N_t, \mathcal{C}_N(\widehat{N}_t, \Delta K)),$$

其中 $\mathcal{C}_T, \mathcal{C}_Z, \mathcal{C}_N$ 是从几何响应模型推导出的校正或边缘化映射。至少，先验权重 $w_t^{\mathrm{FM}}, w_t^Z, w_t^N$ 应在 $\Delta K$ 较大或预测残差与已知错误内参偏差通道对齐时减小。

位姿校正基于局部 BA 响应：

$$\delta\xi_t^K = -[e_t^T S^{-1} \Gamma_K] \Delta\theta_K.$$

若模型位姿在 $\widehat{K}$ 下产生，则一阶去偏差位姿先验可写作

$$\mathcal{C}_T(\widehat{T}_t, \Delta K) = \exp(-(\delta\xi_t^K)^\wedge)\, \widehat{T}_t.$$

符号约定取决于 $\widehat{T}_t$ 被解释为有偏输出还是参考状态；实现时应通过合成主点扫描验证符号。

对于深度，单视图传输近似给出

$$\mathcal{C}_Z(\widehat{Z}_t, \Delta K)(u, v) \approx \widehat{Z}_t(u, v) + \beta \cdot \nabla \widehat{Z}_t(u, v),$$

其中 $\widehat{Z}_t$ 为居中相机响应，目标为真实相机深度。若学习深度具有仿射不变性或仅为相对深度，此校正应与逐帧尺度和偏移结合：

$$Z_t(u) \approx a_t\, \mathcal{C}_Z(\widehat{Z}_t, \Delta K)(u) + b_t.$$

对于法向量，针孔剪切模型给出 $\tilde{n} = H^{-T} n$。因此，在错误内参模型下预测的法向量可通过逆局部变换移向真实内参框架，再归一化：

$$\mathcal{C}_N(\widehat{n}, \Delta K) = \frac{H^T \widehat{n}}{\|H^T \widehat{n}\|},$$

若使用强正交先验，则需额外进行曼哈顿框架投影。

### 8.6 鲁棒过滤与可切换先验

即使在一阶校正之后，学习先验也应保持较弱。我们使用可切换权重

$$\gamma_t^T, \gamma_t^Z, \gamma_t^N \in [0, 1]$$

并优化

$$E_{\mathrm{FM}}^{\mathrm{gated}} = \sum_t \gamma_t^T\, \rho_T(T_t, \mathcal{C}_T(\widehat{T}_t, \Delta K)) + \gamma_t^Z\, \rho_Z(Z_t, \mathcal{C}_Z(\widehat{Z}_t, \Delta K)) + \gamma_t^N\, \rho_N(N_t, \mathcal{C}_N(\widehat{N}_t, \Delta K)) + \lambda_\gamma E_\gamma.$$

门控先验 $E_\gamma$ 阻止简单地将每个学习残差关闭，同时允许在学习输出与已知错误内参偏差通道的一致性高于与多视图几何的一致性时进行拒绝。一个简单的确定性门控函数为

$$\gamma_t = \sigma\!\left(\tau - \eta_K \|\Delta K\| - \eta_r R_t^{\mathrm{geom}}\right),$$

其中 $R_t^{\mathrm{geom}}$ 衡量在应用预测校正之后，学习先验与对应几何之间的不一致程度。学习置信度图可调制 $\gamma_t$，但不应替代几何门控。

### 8.7 优化调度

完整方法采用分阶段调度：

1. **几何引导。** 从对应点估计初始共享 $K$、位姿和稀疏结构。在序列受到充分约束之前，固定或强正则化 $c_x, c_y$。
2. **主点释放。** 仅在位姿和结构稳定后精化共享 $c_x, c_y$。监测主点的约束条件或后验协方差。
3. **先验分类。** 根据当前 $K$ 的不确定性，将基础模型预测分类为已校正、去偏差或已过滤。
4. **偏差感知联合求解。** 联合优化对应约束、重投影、共享内参和已校正学习先验。
5. **条件允许时进行规范化重运行。** 若估计的 $K$ 变得可靠，将帧规范化到居中针孔坐标系并重新运行基础模型，以已校正输入先验替换去偏差的原始图像先验。
6. **最终鲁棒精化。** 以可切换先验和鲁棒损失对所有激活项重新优化，保持共享相机模型的显式性。

这里的一个研究设计细节很关键：虽然第 4 步写成“偏差感知联合求解”，但当前实际阶段并不急于把所有约束都丢进去求一个大系统最优。更严格的顺序是：先用消融实验确认每种先验路径是否有效，再决定它是否有资格进入第 4 步。也就是说，这套调度描述的是目标态，而当前实现更接近第 1-3 步加上对第 5 步的 oracle 式评估。

---

## 9. 实验方案

上述推导建议四个验证阶段。

### 9.1 合成纯几何实验

生成已知三维几何，扫描主点偏移 $\Delta c_x, \Delta c_y$，测量：

- 刚性 Procrustes 偏航/俯仰响应；
- 平移-旋转软模式；
- 焦距偏差；
- 平面法向量倾斜；
- 恒定深度退化。

### 9.2 求解器基准测试

比较以下方法：

- 居中主点 PnP；
- DLT（直接线性变换）；
- P4.5Pfuv；
- P5Pfuva；
- P7Pfruv；
- 基于 $F$ 的自标定；
- 共享 $K$ 的光束法平差。

评价指标：

$$\frac{|f - \widehat{f}|}{f}, \qquad \|c - \widehat{c}\|, \qquad \text{旋转误差}, \qquad \text{平移方向误差}, \qquad \text{重投影误差}.$$

### 9.3 残差分布测试

构造多视图仿射剪切请求 $A_t X + b_t$，比较：

- 常权重融合（结果应保持仿射）；
- 空间变化权重融合（曲率应与 $2c_2$ 一致）；
- 曼哈顿法向量融合（法向量变化应与 $\omega_{\mathrm{MW}}'(0)$ 一致）。

此测试防止问题静默退化为常权重仿射平均。

### 9.4 基础模型响应实验

最后，将合成偏心图像输入基础模型，比较其预测位姿、深度和点图与纯几何响应：

$$g_{\mathrm{model}} \quad \text{vs.} \quad g_{\mathrm{geometry}}.$$

在理解这一比较之前，模型输出不应被视为强先验。

### 9.5 学习先验消融实验

以三类典型方向建立资格审查三角形：

- `raw/direct`：错误内参条件下的原始基础模型输出直接作为先验，用来测量污染有多重；
- `corrected`：对 wrong-intrinsics 输出施加显式几何校正，用来测量当前偏差模型解释了多少；
- `rerun`：在正确或重心化后的相机条件下重新运行同一个基础模型，用来测量同模型族的可达上界。

关键观察对象不是单独哪条路径胜出，而是 `corrected-rerun gap`。若这个 gap 很小，则说明 wrong-intrinsics 输出中的主要偏差是几何可解释、可校正、可优化吸收的；若这个 gap 很大，则说明仍有大量模型响应偏差不能靠输出后几何校正闭合，未来联合优化即使实现，也不应盲目把该先验当作强约束加入。

---

## 10. 讨论

核心难点不在于一旦 $K$ 已知，对非居中主点进行变形是困难的。核心难点在于 $K$ 通常是未知的，错误的 $K$ 可以被位姿、焦距、结构和法向量框架正则化以不同方式吸收，取决于重建能量的具体形式。

同样需要避免把“单照片里的主点改写”和“运动视频里的多视图 BA 响应”混为一谈。前者是在同一光心下改写射线参数化，恒定深度平面上确实可由平移解释；后者则是同一个非中心主点相机在多视图共享结构约束下留下的逐视图局部剪切，这正是为什么本文中的主点误差在非退化 BA 里主要投影到偏航/俯仰，而不是横向平移。

这也解释了为什么当前研究顺序必须是“先资格审查，再联合优化”。若过早做 joint solve，错误约束可能被其他自由度掩盖，看起来“也能 work”；但这种 work 没有解释力，也没有可迁移性。只有当 `corrected` 与 `rerun` 的差距被压到足够小，我们才有理由相信该几何校正先验值得进入最终联合目标。

最重要的负面结果是：仿射不相容性本身不是曲率定理。它产生残差，但残差必须经由非仿射算子进行空间分布之后，弯曲才会出现。这对计算机图形流水线具有重要意义，因为许多看起来像弯曲墙壁或变形地板的重建伪影，并非仅由主点剪切引起，而是由该剪切与视角相关权重、可见性、遮挡、置信度、先验和曲面正则化的交互所产生的。

---

## 11. 结论

错误指定的针孔内参向重建问题注入仿射扰动 $H - I$。主点误差是一个秩一剪切，而非仅是图像平移。单视图重建可通过重分配深度将其精确吸收。一般多视图重建无法将所有逐视图剪切吸收为一个欧氏结构。光束法平差将剪切的一部分投影到 $SE(3)$ 上（主要是偏航和俯仰），剩余响应取决于约化融合算子。曼哈顿先验将剪切后的法向量框架投影回 $SO(3)$，在不同度量下产生旋转。曲率不是自动出现的，只有当残差不相容性被非仿射权重、可见性、置信度或正则化进行空间分布时才会出现。

这一分析支持几何感知的学习先验策略。最安全的实现是标定优先：估计真实针孔内参 $K$，将图像规范化到居中主点，然后运行居中相机的前向推理型三维模型。当无法做到这一点时，学习的位姿、深度、点图和法向量预测应仅通过本文推导的错误内参响应所提供的校正映射、鲁棒门控或低置信度先验进入联合光束法平差。

更重要的是，未来是否值得实现完整的内参感知联合优化，不取决于求解器是否足够复杂，而取决于这些先验约束是否先通过了资格审查。`corrected-rerun gap` 越小，说明当前几何偏差模型越接近 foundation model 在正确相机条件下的真实行为；gap 越大，则说明问题仍主要卡在约束建模而不是数值求解。当前工作的价值，正是在为未来的 joint BA 做这一层筛选。
