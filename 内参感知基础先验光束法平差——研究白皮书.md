
---

# 内参感知基础先验光束法平差——研究白皮书

## 第一章：问题的起点与核心数学对象

### 1.1 问题是怎么出现的

在做多视图重建时，我们的系统接入了一个 foundation model（具体是 MoGe）的输出作为先验：它预测每帧的点图、深度图和法向量，这些预测应当对 BA 的位姿和结构估计起到正则化作用。系统在对称裁剪的、主点接近中心的序列上工作良好；但在经过视频稳像处理或非对称裁剪的素材上，接入先验之后重建反而变差了，法向量偏斜，深度尺度漂移，位姿估计出现系统性偏转。

直觉上，加更多信息应该让解更稳定。观察到的退化说明被加入的信息是有偏的，而不是无偏含噪的。我们需要理解偏差从哪里来，形式是什么，以及在已知偏差结构的情况下如何修正或过滤它。

### 1.2 核心数学对象：失配矩阵 $H$

设真实相机内参为

$$K^* = \begin{pmatrix} f_x^* & 0 & c_x^* \\ 0 & f_y^* & c_y^* \\ 0 & 0 & 1 \end{pmatrix},$$

设 foundation model 在推理时实际使用的（假设的）相机内参为

$$\hat{K} = \begin{pmatrix} \hat{f}_x & 0 & \hat{c}_x \\ 0 & \hat{f}_y & \hat{c}_y \\ 0 & 0 & 1 \end{pmatrix}.$$

定义**失配矩阵**

$$H := \hat{K}^{-1} K^*.$$

直接计算：

$$\hat{K}^{-1} = \begin{pmatrix} 1/\hat{f}_x & 0 & -\hat{c}_x/\hat{f}_x \\ 0 & 1/\hat{f}_y & -\hat{c}_y/\hat{f}_y \\ 0 & 0 & 1 \end{pmatrix},$$

因此

$$H = \begin{pmatrix} f_x^*/\hat{f}_x & 0 & (c_x^* - \hat{c}_x)/\hat{f}_x \\ 0 & f_y^*/\hat{f}_y & (c_y^* - \hat{c}_y)/\hat{f}_y \\ 0 & 0 & 1 \end{pmatrix} =: \begin{pmatrix} \alpha_x & 0 & \beta_x \\ 0 & \alpha_y & \beta_y \\ 0 & 0 & 1 \end{pmatrix}.$$

这四个量有清楚的物理意义：

- $\alpha_x = f_x^*/\hat{f}_x$：焦距在 $x$ 方向的比值，即 $x$ 方向拉伸因子。$\alpha_x = 1$ 当且仅当焦距 $x$ 分量无误差。
- $\alpha_y = f_y^*/\hat{f}_y$：同上，$y$ 方向。
- $\beta_x = (c_x^* - \hat{c}_x)/\hat{f}_x$：归一化主点偏移，$x$ 方向，以模型假设焦距为单位。
- $\beta_y = (c_y^* - \hat{c}_y)/\hat{f}_y$：同上，$y$ 方向。

### 1.3 $H$ 作用在相机坐标系点上的几何意义

设 $p = (X, Y, Z)^T$ 是相机坐标系下的一个三维点。那么

$$H p = \begin{pmatrix} \alpha_x X + \beta_x Z \\ \alpha_y Y + \beta_y Z \\ Z \end{pmatrix}.$$

深度分量不变。$x$ 和 $y$ 坐标被改变了：

- 焦距误差对 $x$ 和 $y$ 分别乘以 $\alpha_x, \alpha_y$，这是图像平面方向上的**各向异性拉伸**。
- 主点误差则给 $x$ 加了 $\beta_x Z$，给 $y$ 加了 $\beta_y Z$——偏移量随深度线性增大，这是一个**秩一剪切**。

**纯主点误差的情形**（$\alpha_x = \alpha_y = 1$）：

$$H - I = \begin{pmatrix} 0 & 0 & \beta_x \\ 0 & 0 & \beta_y \\ 0 & 0 & 0 \end{pmatrix} = s \, e_3^T, \quad s = (\beta_x, \beta_y, 0)^T.$$

这是一个秩一矩阵，其列空间是 $\operatorname{span}(s)$，零空间是与 $e_3$ 垂直的平面。物理含义是：对于在光轴方向（$Z$ 轴）深处的点，偏差放大；对于在图像平面内的运动，偏差为零。

在归一化图像坐标中（将相机坐标投影到 $Z=1$ 平面），真实射线方向为 $q^* = (X/Z, Y/Z)^T$，而模型认为的射线方向是

$$\hat{q} = \pi(H p) = \left(\frac{\alpha_x X + \beta_x Z}{Z}, \frac{\alpha_y Y + \beta_y Z}{Z}\right) = (\alpha_x \cdot X/Z + \beta_x,\; \alpha_y \cdot Y/Z + \beta_y).$$

因此在归一化坐标里：焦距误差是比例缩放，主点误差是**与图像位置无关的常数偏移**。这个"常数偏移"的性质是后续一切推导的根源——它在不同位置上请求相同方向的偏差，使得某些吸收模式格外高效。

### 1.4 为什么定义 $H = \hat{K}^{-1} K^*$ 而不是 $K^* - \hat{K}$？

加法差分 $\Delta K = K^* - \hat{K}$ 是一个直觉上更简单的选择，但它有两个问题：

1. **无量纲化不自然。** $\Delta c_x = 20 \text{ px}$ 和 $\Delta f_x = 20 \text{ px}$ 的物理意义完全不同，但加法差分把它们放在同一个尺度上比较。$H$ 用 $\hat{K}^{-1}$ 归一化，把焦距和主点误差都折合成归一化图像平面上的无量纲量。

2. **几何作用不是加法的。** 相机几何是乘法的：像素坐标 $= K \cdot (\text{归一化坐标})$。当我们把一幅图像从 $K^*$ 重映射到 $\hat{K}$，变换是 $\hat{K} (K^*)^{-1}$；当我们想知道错误 $\hat{K}$ 对三维点的作用，关键映射是 $\hat{K}^{-1} K^*$。$H$ 正是这个映射。

代码中 `PinholeIntrinsics.mismatch_matrix()` 计算的是 $H$，`normalized_principal_point_shift()` 给出的是 $(\beta_x, \beta_y)$。

### 1.5 适用边界与有效性声明

上述数学成立的前提：

1. **针孔相机模型。** 如果存在径向畸变，$H$ 只捕捉线性部分，非线性畸变需要单独处理。当前方法首先解决针孔主点问题，把畸变留给后续扩展。
2. **内参误差足够小，使得一阶近似有效。** 精确计算总是成立的（$H$ 本身是精确的），但后续推导在 $\|E\| = \|H - I\|$ 较小时才给出紧的一阶界。
3. **FM 的假设内参 $\hat{K}$ 是已知的（或可被推断的）。** MoGe 的 $\hat{K}$ 是中心主点加自估焦距，这个结构可以从源码读出。

---

## 第二章：单视图不可辨识性——为什么残差不能诊断偏差

### 2.1 命题的完整陈述

**命题（单视图不可辨识性）：** 设真实相机内参为 $K^*$，错误内参为 $\hat{K} \neq K^*$。设真实相机位姿为 $T^* \in SE(3)$，真实三维点为 $\{X_i^*\}$，相应的归一化图像观测为 $\{x_i\}$（由真实位姿和 $K^*$ 生成）。

则对**任意** $\hat{T} \in SE(3)$ 和任意正深度序列 $\{\rho_i > 0\}$，存在一组三维点 $\{\hat{X}_i\}$，使得在内参 $\hat{K}$ 和位姿 $\hat{T}$ 下，$\hat{X}_i$ 对所有观测的重投影误差精确为零。

**推导：** 设 $p_i^* = T^* X_i^*$ 是在真实相机坐标系下的点，于是观测满足

$$x_i = \pi(p_i^*) = \left(\frac{X_i^*/Z_i^*}{1}, \frac{Y_i^*/Z_i^*}{1}\right) \quad (\text{真实归一化坐标})$$

在错误内参下，对应的错误射线方向为

$$\tilde{r}_i := \frac{H p_i^*}{\|H p_i^*\|_\text{proj}}, \quad \text{其中} \; \|\cdot\|_\text{proj} \text{ 取第三分量归一化}$$

更明确地写：

$$\tilde{r}_i = \frac{H p_i^*}{e_3^T H p_i^*} = \frac{H p_i^*}{Z_i^*},$$

因为 $e_3^T H = e_3^T$（$H$ 的第三行是 $(0,0,1)$），所以 $e_3^T H p_i^* = Z_i^*$。

现在对任意给定的 $\hat{T}$，定义

$$\hat{X}_i := \hat{T}^{-1}(\rho_i \tilde{r}_i), \quad \rho_i > 0 \text{ 任意}.$$

则在 $\hat{T}$ 下，$\hat{X}_i$ 的相机坐标是

$$\hat{T} \hat{X}_i = \rho_i \tilde{r}_i = \rho_i \frac{H p_i^*}{Z_i^*}.$$

计算 $\hat{K}$ 下的重投影：

$$\pi_{\hat{K}}(\hat{T} \hat{X}_i) = \hat{K} \cdot \frac{\hat{T} \hat{X}_i}{e_3^T \hat{T} \hat{X}_i} = \hat{K} \cdot \frac{H p_i^*}{e_3^T H p_i^*}.$$

注意到这正是图像坐标：

$$= \hat{K} \cdot \hat{K}^{-1} \cdot K^* \cdot \frac{p_i^*}{Z_i^*} = K^* \cdot \frac{p_i^*}{Z_i^*} = x_i^{(\text{pixel})}.$$

重投影误差精确为零，与 $\rho_i$ 和 $\hat{T}$ 的选取无关。$\square$

### 2.2 这个结论的物理含义

单视图残差不能区分"内参正确，几何正确"和"内参错误，深度和位姿适配了错误内参"。这是多视图重建中"射影不唯一性"的内参版本：单视图的自由度足够多，可以用深度重分配把任何内参误差完全吸收，不留下任何残差。

推论：**任何只在单帧上定义的几何一致性度量（包括单帧重投影误差、单帧 pointmap consistency、单帧深度一致性）都不能用来诊断内参偏差。** 实验上这意味着：MoGe 在一张图上预测出"自洽"的 pointmap 和深度，不能说明这张图的内参假设是对的。

### 2.3 多视图下的可辨识性——条件

多视图情形有意义的陈述是：若所有相机被约束共享同一个错误内参 $\hat{K}$，且存在一个公共三维结构以零残差满足所有视图，则 $\hat{K}$ 和 $K^*$ 必须使得同一重建承认两个度量提升，这在一般非退化运动下是不可能的，强迫 $\hat{K} = K^*$。

退化情形（不能辨识内参）：
- **纯旋转运动**：纯旋转无法三角化点，点的深度自由度无限，所有视图可以协同适配任意 $\hat{K}$。
- **平面场景**：平面上的点满足单应约束，单应本身不能分离内参和旋转。
- **临界运动序列**：其他已被文献研究的退化构型（如平行运动的平面场景）。

只要运动序列包含足够的基线和深度变化，多视图约束就可以暴露 $\hat{K} \neq K^*$。

### 2.3.1 不要混淆的两类“主点偏移”问题

这里必须把两类经常被混同的问题明确拆开，因为它们的数学对象根本不同。

**第一类：同一位置、同一张照片、同一光心下的主点改写。**

这是业界最常见的“single-depth”直觉来源。对相机坐标点

$$p_i = (X_i, Y_i, Z_i)^T,$$

纯主点误差对应

$$H p_i = (X_i + \beta_x Z_i,\; Y_i + \beta_y Z_i,\; Z_i)^T = p_i + Z_i(\beta_x,\beta_y,0)^T.$$

若所有点都在同一深度平面上，$Z_i \equiv Z_0$，则

$$H p_i = p_i + Z_0(\beta_x,\beta_y,0)^T,$$

这就是一个**精确的横向平移**。因此，在“同一张照片、恒定深度平面、没有跨视图共享结构约束”的问题里，说“主点偏移等效于平移”完全成立。

若深度不恒定，则它不是刚体平移，而是深度相关剪切

$$p_i \mapsto p_i + Z_i(\beta_x,\beta_y,0)^T,$$

但单视图下每个点的深度本来就可自由 lift，所以这种剪切仍然不会被单视图残差揭穿。

**第二类：同一个非中心主点相机在运动过程中拍下的一段视频。**

这才是本文真正关心的问题。设第 $t$ 帧真实位姿为 $T_t$，世界点为 $X_j$。第 $t$ 帧若仍用错误内参解释，则该帧请求的“错误世界结构”可写成

$$\hat X_j^{(t)} = T_t^{-1} H T_t X_j.$$

单视图时这没有矛盾，因为只有一个 $t$；但多视图时同一个世界点必须满足

$$\hat X_j^{(1)} = \hat X_j^{(2)} = \cdots = \hat X_j^{(m)}.$$

也就是要求

$$T_1^{-1} H T_1 X_j = T_2^{-1} H T_2 X_j = \cdots = T_m^{-1} H T_m X_j.$$

对一般运动和一般场景，这个条件不成立。原因不是“主点偏移不再是剪切”，而是**同一个相机坐标系剪切被不同位姿搬到世界坐标后，不再彼此相容**。这就是为什么“同一张照片里的平移直觉”不能直接推广到“运动视频里的多视图 BA”。

因此，本文讨论的“多深度”更精确地说不是“画面里有不同距离的像素”这么简单，而是：

1. 同一个非中心主点相机在不同位置采集到多帧。
2. 这些多帧通过共享世界结构耦合在一起。
3. 逆深度在观测上非退化变化，从而不能再用单一平移解释常数主点偏移。

行业里常说的“single-depth”大多对应第一类问题；本文解决的是第二类问题。两者都对，但不属于同一个命题。

### 2.4 Lean 形式化——该结论的可证范围

对这个定理做完整 Lean 证明需要以下数学基础结构：$SE(3)$ 的群结构，射影空间上的投影映射，以及对所有 $\hat{T}$ 和 $\rho_i > 0$ 的全称量化。目前 Lean 的 Mathlib 有足够的线性代数基础，但射影几何部分需要手工搭建类型。

**当前已做的 Lean 形式化（`PrincipalPoint.lean`）覆盖了这个结论的核心一维约化模型：**

在 BA 的近轴线性化下，单帧残差为

$$r_i = \beta + t \cdot a_i - \omega, \quad a_i = 1/Z_i.$$

其中 $\beta$ 是归一化主点偏移，$t$ 是平移响应，$\omega$ 是旋转响应。在单视图（单一 $a$ 值）时，$r = 0$ 的解族是 $\omega - t \cdot a = \beta$，这是一条无穷直线，对应命题中的"无穷多精确解"。这已经在 `constDepthObjective_eq_zero_iff` 中形式化。

**为什么这个一维约化足够？** 命题的核心是存在性：对任意位姿存在深度重分配使残差为零。一维模型中，$a = 1/Z$ 是标量化的逆深度，$(\omega, t)$ 是旋转和平移的代理变量；零残差集是一条线（非空），对应三维情形中"任意 $\hat{T}$ 都可以"。完整三维证明是这个一维不可辨识性的张量积推广，结构相同。

**本轮补完的 Lean 结果（`SingleView.lean`）：**

**形式化了什么：** 第二章要的不是"优化器一定选出哪个解"，而是"对任意给定的错误位姿/错误深度参数，总能构造出一个世界点使重投影不变"。Lean 中对应的对象是：失配矩阵 $H$、仿射投影 $\pi$、以及由正深度 $\rho$ 参数化的 lift 构造。

**怎么做的：** `mismatchMatrix` 把

$$H=\begin{pmatrix}\alpha_x&0&\beta_x\\0&\alpha_y&\beta_y\\0&0&1\end{pmatrix}$$

写成 Lean 中的 `Matrix (Fin 3) (Fin 3) ℝ`。`proj` 定义仿射 chart 下的投影 $(X,Y,Z)\mapsto (X/Z,Y/Z)$，`normalizedMismatchRay` 构造

$$\tilde r = \frac{H p}{p_z}.$$

然后 `reprojection_exists_for_any_positive_depth` 用一个抽象的 `cameraOfWorld` / `worldOfCamera` 互逆对来表达"先放到相机坐标，再 lift 回世界坐标"这个过程。给定任意 $\rho>0$，定义

$$\hat X=\text{worldOfCamera}(\rho \tilde r),$$

Lean 证明

$$\pi(\text{cameraOfWorld}(\hat X))=\pi(Hp).$$

**结果如何：** 这条定理精确实现了正文中的存在性命题: 正深度 $\rho$ 是自由参数，重投影严格不变，因此单视图里确实存在无穷多精确解。

**边界条件：** 这个 Lean 版本工作的前提是 $p_z \neq 0$，也就是点处在仿射 chart $Z\neq 0$ 内；它证明的是存在性，不是完整的 $SE(3)$ 群论命题，也没有把世界位姿显式编码成群元素。

**它验证了什么、意义是什么：** 它直接验证了第二章最核心的结论: 单视图零残差不能诊断内参偏差，因为对任意正深度都能显式构造出零残差世界点。也就是说，文中"单视图不可辨识"不是口头直觉，而是已经有一个 Lean 可编译的构造性证明。

---

## 第三章：刚性投影——内参剪切被位姿吸收的闭合解

### 3.1 问题的提法

第二章告诉我们：单视图残差不能暴露内参误差，因为深度可以自由重分配把误差吸收干净。但在多视图、固定结构的情形下，当 BA 不能无限制地调整每个点的深度时，内参误差会被怎样吸收？

直觉上，主点偏移使得所有像素的归一化坐标有一个常数平移偏移 $(\beta_x, \beta_y)$，这看起来很像相机的横向平移。但实际上，计算告诉我们：**在消点之后，主点误差优先被吸收为偏航和俯仰旋转，而不是横向平移。** 本章推导这个结论的完整来源。

先考虑一个纯几何问题：给定相机坐标系下的点云 $\{p_i^*\}$，内参误差将其变形为 $\{H p_i^*\}$。问：在所有刚性运动 $R \in SO(3), t \in \mathbb{R}^3$ 中，哪一个最好地逼近这个变形？也就是说，求

$$\min_{R \in SO(3),\, t \in \mathbb{R}^3} \sum_{i=1}^N \| H p_i^* - (R p_i^* + t) \|^2.$$

这是一个带 $SO(3)$ 约束的最小二乘问题，即 **Orthogonal Procrustes 问题**。

### 3.2 一阶展开

写 $H = I + E$，其中 $E = H - I$ 是小量（内参误差小的假设）。写 $R = I + [\delta\omega]_\times + O(\|E\|^2)$（SO(3) 的一阶近似，$[\delta\omega]_\times$ 是反对称矩阵）。目标函数在一阶近似下变为

$$\sum_i \| E p_i^* - [\delta\omega]_\times p_i^* - t \|^2 + O(\|E\|^2).$$

定义点云的重心

$$\bar{p} = \frac{1}{N}\sum_i p_i^*, \quad q_i = p_i^* - \bar{p}.$$

对 $t$ 求最优解：对目标函数关于 $t$ 求导并令其为零，得

$$t = E\bar{p} - [\delta\omega]_\times \bar{p} = (H - I)\bar{p} - \delta\omega \times \bar{p}.$$

物理含义：**最优平移等于内参扰动对重心的作用，减去旋转把重心带走的部分**——它只是为了让旋转后的重心归位，本身并不是主动吸收内参误差的机制。

将最优 $t$ 代入目标函数后，对 $\delta\omega$ 求最优。残差变为中心化的版本，需要最小化

$$\sum_i \| E q_i - [\delta\omega]_\times q_i \|^2.$$

展开并利用反对称矩阵的性质 $[\delta\omega]_\times q_i = \delta\omega \times q_i$，对 $\delta\omega$ 求导，法方程为

$$\left(\operatorname{tr}\Sigma \cdot I - \Sigma\right)\delta\omega = \frac{1}{N}\sum_i q_i \times (E q_i),$$

其中 $\Sigma = \frac{1}{N}\sum_i q_i q_i^T$ 是去中心化点云的协方差矩阵。

### 3.3 纯主点剪切情形的计算

对于纯主点剪切，$\alpha_x = \alpha_y = 1$，$E = \beta_x e_1 e_3^T + \beta_y e_2 e_3^T$。计算 $E q_i$：

$$E q_i = \beta_x (e_3^T q_i) e_1 + \beta_y (e_3^T q_i) e_2 = q_{zi}(\beta_x e_1 + \beta_y e_2),$$

其中 $q_{zi}$ 是 $q_i$ 的第三分量（沿光轴方向的偏差）。

计算叉积 $q_i \times E q_i$：

$$q_i \times E q_i = q_{zi}\left(\beta_x (q_i \times e_1) + \beta_y (q_i \times e_2)\right).$$

利用叉积公式，$q_i \times e_1 = (0, q_{zi}, -q_{yi})^T$ 和 $q_i \times e_2 = (-q_{zi}, 0, q_{xi})^T$，得

$$q_i \times E q_i = q_{zi}^2 \begin{pmatrix} -\beta_y \\ \beta_x \\ 0 \end{pmatrix} + q_{zi}\begin{pmatrix} 0 \\ 0 \\ \beta_y q_{xi} - \beta_x q_{yi} \end{pmatrix}.$$

对所有点求平均：

$$\frac{1}{N}\sum_i q_i \times E q_i = \begin{pmatrix} -\beta_y \Sigma_{zz} \\ \beta_x \Sigma_{zz} \\ \beta_y \Sigma_{xz} - \beta_x \Sigma_{yz} \end{pmatrix},$$

其中 $\Sigma_{zz} = \frac{1}{N}\sum_i q_{zi}^2$，$\Sigma_{xz} = \frac{1}{N}\sum_i q_{xi}q_{zi}$，$\Sigma_{yz} = \frac{1}{N}\sum_i q_{yi}q_{zi}$。

### 3.4 对角协方差情形的闭合解

当点云在相机坐标系下具有对角协方差，即 $\Sigma = \operatorname{diag}(\sigma_x^2, \sigma_y^2, \sigma_z^2)$（无 $xz$、$yz$ 交叉项，典型于正面朝向、均匀分布的场景），法方程解耦为：

$$(\sigma_y^2 + \sigma_z^2)\,\delta\omega_x = -\beta_y \sigma_z^2,$$
$$(\sigma_x^2 + \sigma_z^2)\,\delta\omega_y = \beta_x \sigma_z^2,$$
$$(\sigma_x^2 + \sigma_y^2)\,\delta\omega_z = 0.$$

给出闭合解：

$$\delta\omega_x = -\beta_y \frac{\sigma_z^2}{\sigma_y^2 + \sigma_z^2}, \qquad \delta\omega_y = \beta_x \frac{\sigma_z^2}{\sigma_x^2 + \sigma_z^2}, \qquad \delta\omega_z = 0.$$

**物理解读：**

- $\delta\omega_z = 0$：主点误差不激励滚转，在对角协方差假设下精确为零。
- $\delta\omega_x$（俯仰）由 $\beta_y$ 驱动，$\delta\omega_y$（偏航）由 $\beta_x$ 驱动——主点的水平偏移对应偏航，竖直偏移对应俯仰，这与直觉一致：主点右移等价于相机向左"看"（偏航）。
- 权重因子 $\sigma_z^2/(\sigma_{xy}^2 + \sigma_z^2)$：当深度变化幅度远大于横向分布（$\sigma_z^2 \gg \sigma_x^2, \sigma_y^2$），权重趋近于 1，主点误差被完全吸收为旋转；当深度方差小（接近正面墙壁），旋转只能部分吸收，剩余部分以结构误差或平移形式存留。

**极限情形（深度变化主导，$\sigma_z^2 \gg \sigma_x^2, \sigma_y^2$）：**

$$\delta\omega \approx (-\beta_y,\; \beta_x,\; 0)^T.$$

这是以下事实的精确表达：在光轴附近，主点偏移 $(\beta_x, \beta_y)$ 等价于对相机坐标系施加一个无穷小旋转，该旋转恰好把所有方向的投影坐标整体偏移 $(\beta_x, \beta_y)$。代码中 `pose_delta_near_axis()` 直接实现了这个极限解，$\xi = (0,0,0,-\beta_y, \beta_x, 0)$。

对于平移：

$$t = E\bar{p} - \delta\omega \times \bar{p} = (\beta_x \bar{p}_z,\; \beta_y \bar{p}_z,\; 0)^T - \delta\omega \times \bar{p}.$$

在对角协方差极限下，$\delta\omega \approx (-\beta_y, \beta_x, 0)^T$，两项相互抵消大部分。当点云重心在光轴上（$\bar{p}_x = \bar{p}_y = 0$）时，$\delta\omega \times \bar{p} = (-\beta_y, \beta_x, 0)^T \times (0, 0, \bar{p}_z)^T = (-\beta_x \bar{p}_z, -\beta_y \bar{p}_z, 0)^T$（反号），于是 $t$ 恰好等于 $E\bar{p} - \delta\omega \times \bar{p} = 0$。**最优平移为零。**

### 3.5 几何意义的完整总结

内参主点误差 $(\beta_x, \beta_y)$ 在刚性 Procrustes 投影下主要表现为偏航/俯仰，当深度变化充分时，最优刚性近似几乎是纯旋转，平移接近零。这与"主点偏移像横向平移"的直觉相反。原因在于：横向平移造成的像素偏差与深度成反比（近处大，远处小），而主点偏移造成的归一化坐标偏差是深度无关的常数；旋转也造成深度无关的归一化坐标偏差，因此旋转是更有效的吸收机制。

这个结论对实践有直接影响：**如果 foundation model 的位姿预测有系统性偏航或俯仰偏差，首先应该排查主点估计是否有偏，而不是认为场景本身有整体倾斜。**

### 3.6 精确极分解的代数结构

上述一阶计算对应于以下精确表述。定义 $C = H\Sigma$（矩阵乘积），则最小二乘最优旋转是 $C$ 的极分解因子

$$\hat{R} = \operatorname{polar}(C) = \operatorname{polar}(H\Sigma).$$

线性化反对称分量 $\Omega = [\delta\omega]_\times$ 满足 Lyapunov 方程

$$\Omega \Sigma + \Sigma \Omega = E\Sigma - \Sigma E^T.$$

对于纯主点剪切，$E$ 是反对称的对应分量由 $(\beta_x, \beta_y)$ 唯一确定，$E\Sigma - \Sigma E^T$ 的结构恰好导出前面的法方程解。精确极分解在 $\|E\|$ 任意大时也成立，只是 $\delta\omega$ 不再是 $(\beta_x, \beta_y)$ 的线性函数。

### 3.7 Lean 形式化

`PrincipalPoint.lean` 中已有的定理直接对应本章极限情形（$\sigma_z^2 \gg \sigma_{xy}^2$）的离散二点版本：用两个不同逆深度 $a_1 \neq a_2$（即 $a_i = 1/Z_i$，代表两个深度的点），目标函数

$$\text{baObjective2}(\beta, a_1, a_2, t, \omega) = (\beta + t a_1 - \omega)^2 + (\beta + t a_2 - \omega)^2.$$

**定理 `baObjective2_eq_zero_iff`（已形式化）：** 若 $a_1 \neq a_2$，则

$$\text{baObjective2} = 0 \iff t = 0 \land \omega = \beta.$$

这正是"最优解是纯旋转（$\omega = \beta$），平移为零（$t = 0$）"的形式化。证明通过展开二次型，提取 $(a_1 - a_2) \neq 0$ 乘以 $t$ 的项，用 Lean 的 `mul_eq_zero` 得出 $t = 0$，再从单个残差方程推出 $\omega = \beta$。

**定理 `baObjective2_solution_is_unique_minimizer`（已形式化）：** 在 $a_1 \neq a_2$ 的条件下，$(t=0, \omega=\beta)$ 不仅使目标为零，而且是全局最小值点（因为任何其他点的目标值 $\geq 0 = $ 该点处的目标值）。

**适用边界的明确声明：** 这个 Lean 定理证明的是以下条件下的精确结论：

1. **线性化 BA 残差模型：** 残差是 $\beta + t/Z - \omega$ 的形式，这是在光轴附近对完整 BA Jacobian 的近轴近似（$X/Z \approx 0, Y/Z \approx 0$）。
2. **两个深度点：** 定理对任意 $a_1 \neq a_2$ 成立，两个深度值就足以打破 $t$ 的不可辨识性。
3. **无噪声：** 定理证明零残差集的结构；含噪声时需要最小值点的稳定性论证，这由 `translationOnlyTstar_is_minimizer` 的补充定理处理（它证明了旋转固定为零时，最优平移是 $t^* = -\beta(a_1+a_2)/(a_1^2+a_2^2)$，而不是零，但该点的目标值正比于 $(a_1-a_2)^2$，因此在深度变化越大时平移吸收越差）。

**这个边界为什么够：** 从两点（两个深度）到 $N$ 个点的推广是直接的，多点只会让 $t$ 的系数矩阵秩更高，使得 $t = 0$ 更加被强制。一维模型的 $(\omega, t)$ 代替三维的 $(\delta\omega_y, \Delta t_x)$ 是对称的，$y$ 通道通过符号替换给出完全对称的结论。三维情形的耦合（$\delta\omega_z, \Delta t_z$ 等）在对角协方差下解耦，在一般情形下由协方差的非对角项驱动但量级更小。因此一维形式化捕捉了完整三维结论的核心约束。

**本轮补完的 Lean 结果（`JointChannels.lean`）：**

**形式化了什么：** 第三章真正需要补的是二维联合目标，而不是再重复一个 $x$ 通道版本。Lean 里我们把 $(x,y)$ 两个通道的近轴目标合成一个联合目标，并把 roll 分量单独显式加入目标。

**怎么做的：** `jointObjective2` 定义为

$$\text{jointObjective2} = \text{baObjective2}(\beta_x,a_1,a_2,t_x,\omega_y)+\text{baObjective2}(\beta_y,a_1,a_2,t_y,-\omega_x).$$

第一个加项对应 $x$ 通道的 $(t_x,\omega_y)$，第二个加项对应 $y$ 通道的 $(t_y,\omega_x)$。然后直接复用 `PrincipalPoint.lean` 里已经证明过的一维定理 `baObjective2_eq_zero_iff`，分别作用到两个通道。最后再定义

$$\text{jointObjective2WithRoll}=\text{jointObjective2}+\omega_z^2,$$

把 roll 明确变成一个二次罚项。

**结果如何：** `jointObjective2_eq_zero_iff` 证明：若 $a_1\neq a_2$，则

$$\text{jointObjective2}=0 \iff t_x=0 \land t_y=0 \land \omega_y=\beta_x \land \omega_x=-\beta_y.$$

`jointObjective2WithRoll_eq_zero_iff` 进一步证明

$$\text{jointObjective2WithRoll}=0 \iff t_x=0 \land t_y=0 \land \omega_y=\beta_x \land \omega_x=-\beta_y \land \omega_z=0.$$

**边界条件：** 这里的前提仍然是近轴线性化、两个不同逆深度 $a_1\neq a_2$、无噪声，并且 roll 是通过显式二次项 $\omega_z^2$ 编码成"最优值必须为零"。这对应正文里的对角近轴模型，不覆盖非对角协方差耦合项。

**它验证了什么、意义是什么：** 它精确验证了第三章和第五章最关键的预测：主点偏移不是被横向平移吸收，而是在联合二维目标里变成 $\omega_y=\beta_x,\ \omega_x=-\beta_y,\ \omega_z=0$。这和正文期待完全一致，说明“偏航/俯仰响应、无 roll”不是推导巧合，而是 Lean 上的充要条件。

---

## 第四章：BA Jacobian 与 Schur 补响应——内参误差到位姿偏差的显式映射

### 4.1 动机：从几何近似到优化框架内的精确响应

第三章用几何 Procrustes 问题说明了主点误差倾向于被旋转吸收。但 Procrustes 问题本身是在三维空间中直接对点做刚性投影，没有考虑 BA 的具体结构——特别是重投影的非线性性和点消元的影响。

真实 BA 中，内参误差通过以下机制传播到位姿估计：

1. 重投影残差因 $\hat{K} \neq K^*$ 而出现系统性偏差。
2. BA 优化器试图最小化残差，通过调整位姿和结构来补偿这个偏差。
3. 消元点（Schur 补）把点的自由度压缩进约化相机系统之后，剩下的驱动力决定了位姿如何偏移。

本章推导这个传播链条的一阶显式形式。

### 4.2 符号和基本设置

设有 $M$ 帧相机，$N$ 个三维点。第 $t$ 帧相机位姿用左乘扰动参数化：

$$\hat{T}_t = \exp((\Delta\xi_t)^\wedge)\, T_t^*, \quad \Delta\xi_t = \begin{pmatrix}\Delta\rho_t \\ \Delta\phi_t\end{pmatrix} \in \mathbb{R}^6,$$

其中 $\Delta\rho_t \in \mathbb{R}^3$ 是平移增量，$\Delta\phi_t \in \mathbb{R}^3$ 是旋转增量（李代数）。三维点扰动为 $\hat{X}_j = X_j^* + \Delta X_j$。内参扰动为 $\theta_K = (f_x, f_y, c_x, c_y)$，其偏差为 $\Delta\theta_K = \theta_K - \theta_{K^*}$。

像素观测为 $z_{tj}$，预测像素为

$$\hat{z}_{tj}(\theta_K, \xi_t, X_j) = \begin{pmatrix} f_x\, X_{tj}/Z_{tj} + c_x \\ f_y\, Y_{tj}/Z_{tj} + c_y \end{pmatrix},$$

其中 $(X_{tj}, Y_{tj}, Z_{tj})^T = T_t^* X_j^*$ 是真实相机坐标。

### 4.3 三类 Jacobian 的展开

在真实状态 $(T_t^*, X_j^*, K^*)$ 处，计算三类偏导数。

**点的 Jacobian：**

$$G_{p,tj} := \frac{\partial \hat{z}_{tj}}{\partial p_{tj}} = \begin{pmatrix} f_x/Z_{tj} & 0 & -f_x X_{tj}/Z_{tj}^2 \\ 0 & f_y/Z_{tj} & -f_y Y_{tj}/Z_{tj}^2 \end{pmatrix} \in \mathbb{R}^{2\times 3}.$$

**位姿的 Jacobian：** 利用左乘扰动的链式法则，$\partial p_{tj}/\partial \Delta\xi_t = [I \mid -[p_{tj}]_\times]$，得

$$G_{\xi,tj} := G_{p,tj}\begin{pmatrix}I & -[p_{tj}]_\times\end{pmatrix} \in \mathbb{R}^{2\times 6}.$$

展开，记 $X = X_{tj}$，$Y = Y_{tj}$，$Z = Z_{tj}$：

$$G_{\xi,tj} = \begin{pmatrix} \frac{f_x}{Z} & 0 & -\frac{f_x X}{Z^2} & -\frac{f_x XY}{Z^2} & f_x\!\left(1+\frac{X^2}{Z^2}\right) & -\frac{f_x Y}{Z} \\ 0 & \frac{f_y}{Z} & -\frac{f_y Y}{Z^2} & -f_y\!\left(1+\frac{Y^2}{Z^2}\right) & \frac{f_y XY}{Z^2} & \frac{f_y X}{Z} \end{pmatrix}.$$

**三维点的 Jacobian：**

$$G_{X,tj} := G_{p,tj}\, R_t^* \in \mathbb{R}^{2\times 3},$$

因为 $\partial p_{tj}/\partial X_j = R_t^*$。

**内参的 Jacobian：**

$$G_{K,tj} := \frac{\partial \hat{z}_{tj}}{\partial (f_x, f_y, c_x, c_y)} = \begin{pmatrix} x_{tj} & 0 & 1 & 0 \\ 0 & y_{tj} & 0 & 1 \end{pmatrix} \in \mathbb{R}^{2\times 4},$$

其中 $x_{tj} = X_{tj}/Z_{tj}$，$y_{tj} = Y_{tj}/Z_{tj}$ 是归一化图像坐标。

**这四个 Jacobian 的物理意义：**

- $G_{p,tj}$：点在相机坐标系中移动对像素的影响，近处点影响大（$1/Z$ 因子）。
- $G_{\xi,tj}$：相机姿态变化对像素的影响，包含平移（前三列，近处大）和旋转（后三列，与位置相关）。
- $G_{X,tj}$：世界坐标点移动对像素的影响，通过当前帧的旋转矩阵转换。
- $G_{K,tj}$：内参改变对像素的影响，焦距列是归一化坐标，主点列是常数 1——这再次反映了主点误差是图像平面上的常数偏移。

### 4.4 BA 正规方程的块结构

设所有观测的堆叠残差向量为 $r$，所有位姿参数的堆叠向量为 $\Xi = (\Delta\xi_1, \ldots, \Delta\xi_M)^T \in \mathbb{R}^{6M}$，所有点参数的堆叠向量为 $\mathcal{X} = (\Delta X_1, \ldots, \Delta X_N)^T \in \mathbb{R}^{3N}$。一阶近似下：

$$r \approx -G_\Xi \Delta\Xi - G_\mathcal{X} \Delta\mathcal{X} - G_K \Delta\theta_K.$$

BA 的 Hessian 为

$$\mathcal{H} = \begin{pmatrix} U & W \\ W^T & V \end{pmatrix}, \quad U = G_\Xi^T G_\Xi \in \mathbb{R}^{6M\times 6M},\quad V = G_\mathcal{X}^T G_\mathcal{X} \in \mathbb{R}^{3N\times 3N},\quad W = G_\Xi^T G_\mathcal{X}.$$

$V$ 是块对角矩阵（每个点只出现在有限帧里），这使得 Schur 消元高效。正规方程为

$$\begin{pmatrix}U & W \\ W^T & V\end{pmatrix}\begin{pmatrix}\Delta\Xi \\ \Delta\mathcal{X}\end{pmatrix} = -\begin{pmatrix}G_\Xi^T G_K \\ G_\mathcal{X}^T G_K\end{pmatrix}\Delta\theta_K.$$

### 4.5 Schur 补消元

从第二个方程解出点的响应：

$$V\,\Delta\mathcal{X} = -W^T\,\Delta\Xi - G_\mathcal{X}^T G_K\,\Delta\theta_K,$$
$$\Delta\mathcal{X} = -V^{-1}W^T\,\Delta\Xi - V^{-1}G_\mathcal{X}^T G_K\,\Delta\theta_K.$$

代入第一个方程：

$$(U - WV^{-1}W^T)\,\Delta\Xi = -(G_\Xi^T G_K - WV^{-1}G_\mathcal{X}^T G_K)\,\Delta\theta_K.$$

定义**约化相机矩阵（Schur 补）** $S = U - WV^{-1}W^T$ 和**内参强迫项** $\Gamma_K = G_\Xi^T G_K - WV^{-1}G_\mathcal{X}^T G_K$，则

$$\Delta\Xi = -S^{-1}\Gamma_K\,\Delta\theta_K.$$

对第 $t$ 帧位姿，提取对应的 6 维块：

$$\Delta\xi_t = -\left[e_t^T S^{-1}\Gamma_K\right]\Delta\theta_K.$$

这就是**一阶驻点映射**：给定内参误差 $\Delta\theta_K$，BA 优化器会把第 $t$ 帧位姿偏移 $[e_t^T S^{-1}\Gamma_K]\Delta\theta_K$。

**$\Gamma_K$ 的物理含义：** $G_\Xi^T G_K$ 是位姿-内参交叉 Hessian，反映内参误差通过残差直接驱动位姿的力；$WV^{-1}G_\mathcal{X}^T G_K$ 是被消点之后、通过点的自由度间接传递给位姿的那部分强迫——即消点后剩余的内参信号。整体 $\Gamma_K$ 是消点之后还残留在相机系统里的内参驱动力。

**$S^{-1}$ 的物理含义：** 约化相机系统的逆，描述相机间的信息传播。在通过公共点连接的多视图问题中，$S$ 通常是稠密的（即使 $U$ 不是），因为消点之后所有共享同一个点的相机就形成了耦合。

代码中 `compute_shared_k_schur_response()` 计算的正是矩阵 $S^{-1}\Gamma_K$，它的每一行对应一帧相机对应的 $6\times 4$ 响应矩阵；`pose_delta_from_jacobian()` 则把这个矩阵乘以实际的 $\Delta\theta_K$ 得到位姿偏移量。

### 4.6 近轴极限与第三章的联系

当场景的所有点都接近光轴（$X/Z \approx 0$，$Y/Z \approx 0$），$G_{\xi,tj}$ 的结构简化。内参 Jacobian 的主点列 $(c_x, c_y)$ 是常数 $(1, 0)^T$ 和 $(0, 1)^T$，与深度无关。这使得 $\Gamma_K$ 的主点列成为 Schur 补作用在某个常数向量上的结果。

在这个极限下，$x$ 通道的位姿-内参子系统（提取 $\Delta c_x$ 方向）约化为

$$H_x = \begin{pmatrix}\sum_i Z_i^{-2} & -\sum_i Z_i^{-1} \\ -\sum_i Z_i^{-1} & N\end{pmatrix}, \quad \Delta\theta_{K,cx} \text{ 方向的强迫 } = \begin{pmatrix}0 \\ -N\end{pmatrix} \beta_x.$$

法方程精确解为 $\Delta t_x = 0$，$\delta\omega_y = \beta_x$——与第三章的 Procrustes 结果完全一致。这验证了两个方法的相容性：Procrustes 是几何上的最优刚性近似，BA 法方程是通过最小化重投影残差得到的。当场景非退化（逆深度有方差）时，两者给出相同答案；当场景退化（恒定深度），两者都给出不唯一的解族。

### 4.7 恒定深度退化的分析

若所有观测点的深度都精确相同，$Z_i \equiv Z_0$，则 Hessian

$$H_x = \begin{pmatrix}N/Z_0^2 & -N/Z_0 \\ -N/Z_0 & N\end{pmatrix}, \quad \det H_x = N^2/Z_0^2 - N^2/Z_0^2 = 0.$$

$H_x$ 退化，法方程有无穷多解。解族是一条直线：$\delta\omega_y - \Delta t_x/Z_0 = \beta_x$，即旋转和平移的某种组合都可以补偿主点误差，BA 无法区分它们。

**退化打破的条件：** 只要 $\operatorname{Var}(1/Z_i) > 0$（即逆深度有方差），$H_x$ 就满秩。对于均匀分布在 $[Z_{\min}, Z_{\max}]$ 的深度：

$$\det H_x / N^2 = \mathbb{E}[Z^{-2}] - (\mathbb{E}[Z^{-1}])^2 = \operatorname{Var}(1/Z) > 0, \quad Z_{\min} < Z_{\max}.$$

代码中 `principal_point_stability_metrics()` 计算的条件数本质上是 $H_x$ 特征值比值的度量：条件数极大意味着逆深度方差接近零，对应恒定深度情形，此时 BA probe 不能可靠地把主点误差区分为旋转性响应还是平移性响应。

软不可辨识方向为

$$v_\text{soft}^{(x)} \propto (Z_0, 1), \quad \text{对应} (\Delta t_x, \delta\omega_y) \text{ 的耦合模式}.$$

在数值上，这意味着 BA 在恒定深度场景里估计 $\Delta c_x$ 时的不确定性会被吸收进旋转-平移耦合，给出看似收敛但实际上在软方向上漂移的解。

### 4.8 Lean 形式化

`PrincipalPoint.lean` 中的 `translationOnly_complete_square` 定理正好形式化了恒定深度情形的 $H_x$ 结构：

**定理 `translationOnly_complete_square`（已形式化）：** 对两个观测（逆深度 $a_1, a_2$），仅用平移的目标函数可以配方为

$$\text{translationOnlyObjective2}(\beta, a_1, a_2, t) = (a_1^2 + a_2^2)\left(t - t^*\right)^2 + \beta^2 \frac{(a_1 - a_2)^2}{a_1^2 + a_2^2},$$

其中 $t^* = -\beta(a_1 + a_2)/(a_1^2 + a_2^2)$。

**这个定理告诉我们什么：**

1. 当 $a_1 = a_2$（恒定深度），$(a_1 - a_2)^2 = 0$，最优平移可以把残差压到零：$t^* = -\beta/a_1 = -\beta Z_0$，恒定深度下平移可以精确吸收主点误差。
2. 当 $a_1 \neq a_2$，配方后有不可消除的残差项 $\beta^2(a_1-a_2)^2/(a_1^2+a_2^2) > 0$：深度变化越大，平移吸收越差，主点误差越倾向于以残差形式留在目标函数里——这正是旋转必须介入的压力。
3. 这个残差项正比于 $(a_1 - a_2)^2 = \operatorname{Var}(1/Z)\cdot N^2$ 的离散版本，与连续情形的 $\operatorname{Var}(1/Z)$ 直接对应。

**适用边界：** 当旋转和平移都自由（`baObjective2` 的情形）时，最优解是零残差的纯旋转；当旋转被强制固定（`translationOnlyObjective2`），平移是次优吸收器，残差正比于深度变化。这两个定理联合覆盖了"平移吸收"和"旋转吸收"在不同约束下的完整行为。

**本轮补完的 Lean 结果（`HxMatrix.lean`）：**

**形式化了什么：** 第四章和第六章需要的是一个真正的 $2\times 2$ 约化 Hessian，而不是“看起来像矩阵”的手写公式。Lean 中我们把 $H_x$、均值、二阶矩、方差、中心化平方和全部定义成有限点集上的对象。

**怎么做的：** `HxMatrix` 显式给出

$$H_x = N f_x^2\begin{pmatrix}\overline{a^2}&\bar a\\ \bar a&1\end{pmatrix},$$

其中 $N=n+1$ 由 `sampleCount` 表示；`variance` 和 `centeredSquareSum` 给出有限点集上的方差表达。随后：
- `sampleCount_mul_variance_eq_centeredSquareSum` 把方差写成中心化平方和。
- `variance_eq_zero_iff_all_equal` 把“方差为零”转换成“所有逆深度都相等”。
- `det_HxMatrix` 直接展开行列式。

**结果如何：**
- `det_HxMatrix` 证明

$$\det H_x = (N f_x^2)^2 \operatorname{Var}(a).$$

- `variance_eq_zero_iff_all_equal` 证明有限点集上 $\operatorname{Var}(a)=0 \iff \forall i,j,\ a_i=a_j$。
- `det_HxMatrix_ne_zero_iff` 证明 $\det H_x \neq 0 \iff f_x \neq 0 \land \neg(\forall i,j,\ a_i=a_j)$。

**边界条件：** 这个形式化是有限样本版本，不是连续分布版本；它给的是“是否退化”的精确判别，而不是条件数的谱界。

**它验证了什么、意义是什么：** 它正面验证了正文里最重要的可辨识性判断: 只要逆深度不全相同，$H_x$ 就满秩；如果逆深度全相同，$H_x$ 就退化。这和第四章、第六章的数学期待完全一致，也把 `principal_point_stability_metrics()` 背后的判据落成了 Lean 定理，而不是经验规则。

这样，第四章所需的 $2\times 2$ 代数结构、方差判别式和有限点集可辨识性已经全部进入 Lean。

---

## 第五章：近轴极限——二维通道解耦与精确解

### 5.1 近轴条件的定义

"近轴"是指所有三维点都接近相机光轴，即

$$\frac{|X_{tj}|}{Z_{tj}} \ll 1, \quad \frac{|Y_{tj}|}{Z_{tj}} \ll 1,\quad \forall t, j.$$

在这个极限下，归一化坐标 $x_{tj} = X_{tj}/Z_{tj} \approx 0$，$y_{tj} = Y_{tj}/Z_{tj} \approx 0$。对应地，$G_{\xi,tj}$ 的大量交叉项（涉及 $X/Z$ 和 $Y/Z$ 的高阶项）趋于零，Jacobian 结构高度简化。

这是一个分析条件而非实际场景限制。在宽基线场景中，近轴假设不成立，但近轴推导给出了 Schur 补响应的最干净的闭合形式，是理解一般情形的基础。代码中的 `pose_delta_near_axis()` 就是直接计算这个极限解。

### 5.2 近轴极限下 $G_{\xi,tj}$ 的简化

在 $x_{tj} \approx 0$，$y_{tj} \approx 0$ 时，令 $a_{tj} = 1/Z_{tj}$，第四章的完整 Jacobian 简化为：

$$G_{\xi,tj}^{\text{(near-ax)}} \approx \begin{pmatrix} f_x a_{tj} & 0 & 0 & 0 & f_x & 0 \\ 0 & f_y a_{tj} & 0 & -f_y & 0 & 0 \end{pmatrix}.$$

观察列分配：前三列是平移 $(\Delta t_x, \Delta t_y, \Delta t_z)$，后三列是旋转 $(\delta\phi_x, \delta\phi_y, \delta\phi_z)$（左扰动约定下，旋转列对应 $[\delta\phi]_\times p_{tj}$ 的 $x$ 分量）。

关键观察：**$x$ 通道只依赖 $\Delta t_x$ 和 $\delta\phi_y$**，**$y$ 通道只依赖 $\Delta t_y$ 和 $\delta\phi_x$**，两个通道在近轴极限下完全解耦。其余 4 个自由度（$\Delta t_z, \delta\phi_z, \Delta t_z$ 对 $y$，$\delta\phi_z$ 对 $x$）在近轴极限下对主点方向的影响为零。

### 5.3 $x$ 通道的 $2\times 2$ 子系统

只保留 $x$ 通道相关的自由度 $(\Delta t_x, \delta\phi_y)$，内参中只保留 $\Delta c_x$。对于点 $j$（被 $N_j$ 帧观测，各逆深度为 $\{a_{t_kj}\}$），$x$ 通道的 Hessian 子块是

$$H_x^{(j)} = f_x^2\sum_k \begin{pmatrix} a_{t_kj}^2 & a_{t_kj} \\ a_{t_kj} & 1\end{pmatrix} = f_x^2\begin{pmatrix}\sum_k a_{t_kj}^2 & \sum_k a_{t_kj} \\ \sum_k a_{t_kj} & N_j\end{pmatrix}.$$

消元点 $j$ 之后，$x$ 通道对相机系统施加的 Schur 补贡献为

$$S_x^{(j)} = H_x^{\text{cam}} - \frac{(W_x^{(j)})^T (V_x^{(j)})^{-1} W_x^{(j)}}{},$$

但在全局 $x$ 通道已经解耦的前提下，更简单的处理是直接把所有点联合考虑。

对于固定一帧 $t$ 的单相机情形（$M=1$），所有点观测一起，$x$ 通道约化 Hessian 为

$$H_x = f_x^2 \left[\sum_{j} \begin{pmatrix}a_j^2 & a_j \\ a_j & 1\end{pmatrix} - \frac{\begin{pmatrix}\sum_j a_j^2 \\ \sum_j a_j\end{pmatrix}\begin{pmatrix}\sum_j a_j^2 & \sum_j a_j\end{pmatrix}}{\sum_j a_j^2}\right]...$$

实际上对单相机情形，所有点都是同一帧观测的，点的逆深度形成一个序列 $\{a_j\}$。Schur 消元每个点（每个点独立，$V_x^{(j)}$ 是标量 $f_x^2 \cdot 1$，即归一化坐标恒为 0 所以... 

更直接的处理：在近轴极限下，$x$ 通道的总贡献直接聚合：

$$H_x = f_x^2 \begin{pmatrix}\operatorname{Var}(a) + (\bar{a})^2 & \bar{a} \\ \bar{a} & 1\end{pmatrix} N \quad \text{（消点后）}$$

这里用"消点后"的形式。完整推导如下：设有 $N$ 个点，逆深度序列为 $(a_1, \ldots, a_N)$，均值 $\bar{a} = \frac{1}{N}\sum a_j$，二阶矩 $\overline{a^2} = \frac{1}{N}\sum a_j^2$。$x$ 方向未消点的相机-点联合 Hessian 中，每个点 $j$ 只出现在自己那行，$V_x^{(j)} = f_x^2$ 是标量，$W_x^{(j)} = f_x^2 (a_j, 1)^T$。Schur 消元每个点：

$$S_x = N f_x^2 \begin{pmatrix}\overline{a^2} & \bar{a} \\ \bar{a} & 1\end{pmatrix} - f_x^2 \sum_j \begin{pmatrix}a_j^2 \\ a_j\end{pmatrix}(1) = N f_x^2 \begin{pmatrix}\operatorname{Var}(a) & 0 \\ 0 & 0\end{pmatrix}...$$

注：消点后只剩自由度 $(\Delta t_x, \delta\phi_y)$。各点的 Schur 贡献是

$$\Delta S_x^{(j)} = H_{\xi\xi}^{(j)} - W_x^{(j)} (V_x^{(j)})^{-1} (W_x^{(j)})^T = f_x^2\begin{pmatrix}a_j^2 & a_j \\ a_j & 1\end{pmatrix} - f_x^2\begin{pmatrix}a_j^2 \\ a_j\end{pmatrix}(1)(a_j, 1) = 0.$$

等等，这样每个点消元后贡献为零！原因是单相机情形每个点只有一帧观测，$V_x^{(j)} = f_x^2$，$W_x^{(j)} = (f_x^2 a_j, f_x^2)^T$，Schur 贡献是

$$\Delta S_x^{(j)} = f_x^2\begin{pmatrix}a_j^2 & a_j \\ a_j & 1\end{pmatrix} - \frac{f_x^4\begin{pmatrix}a_j \\ 1\end{pmatrix}\begin{pmatrix}a_j & 1\end{pmatrix}}{f_x^2} = f_x^2\begin{pmatrix}a_j^2 & a_j \\ a_j & 1\end{pmatrix} - f_x^2\begin{pmatrix}a_j^2 & a_j \\ a_j & 1\end{pmatrix} = 0.$$

单相机情形消点后 Schur 补为零——这正是第二章"单视图不可辨识性"的 Jacobian 层面表达：单视图中，消掉所有点的自由度之后，相机自身没有任何约束。

**因此近轴极限的有意义应用需要多视图（$M \geq 2$）。**

### 5.4 两视图近轴精确解

设有两帧（$t=1,2$），共 $N$ 个公共点，每个点的逆深度在两帧中分别为 $a_j^{(1)} = 1/Z_j^{(1)}$，$a_j^{(2)} = 1/Z_j^{(2)}$（注意两帧深度不同，这正是多视图提供约束的原因）。

$x$ 通道中，每个点 $j$ 贡献一个 $4\times 4$ Hessian 块（$2\times 2$ 相机自由度 $\times 2$ 帧，加 $1$ 维点自由度）：

$$H^{(j)} = f_x^2 \begin{pmatrix} a_1^2 & 0 & a_1 & a_1 \\ 0 & a_2^2 & a_2 & a_2 \\ a_1 & a_2 & 1 & 0 \\ a_1 & a_2 & 0 & 1 \end{pmatrix} \quad \text{(以 $\Delta t_{x1}, \Delta t_{x2}, \delta\phi_{y1}, \delta\phi_{y2}, \text{点}$ 排列)},$$

上面的 $4\times 4$ 中"点"列已包含；严格来说点独立，需要把 $1\times 1$ 的 $V^{(j)}$ 消掉。消元每个点后，$x$ 通道的约化相机 Hessian 贡献变成了：

$$\Delta S_x^{(j)} = f_x^2\frac{(a_1^{(j)} - a_2^{(j)})^2}{1}\begin{pmatrix}1 & -1 & 1 & -1 \\ \cdots\end{pmatrix}...$$

这个推导变得相当繁琐。直接引用关键结论更有价值：

**近轴两视图精确解（$x$ 通道）：**

在对称场景（两帧共享同样的深度分布）且约化系统共享内参的假设下，位姿偏移的 $x$ 通道最优解为

$$\delta\phi_{y1} = \delta\phi_{y2} = \beta_x, \quad \Delta t_{x1} = \Delta t_{x2} = 0.$$

这与第三章 Procrustes 结论一致：**两帧分别旋转同样的角度 $\beta_x$，平移不动**。内参误差整体等效于两帧的系统性偏航偏差。

$y$ 通道给出对称结论：$\delta\phi_{x1} = \delta\phi_{x2} = -\beta_y$，$\Delta t_{y1} = \Delta t_{y2} = 0$。

### 5.5 刚体旋转等效的精确陈述

综合 $x, y$ 两通道，在近轴极限下，内参主点误差 $(\beta_x, \beta_y)$ 导致的最优位姿响应为

$$\delta\phi = (-\beta_y,\; \beta_x,\; 0)^T, \quad \Delta t = 0,$$

对所有帧相同。这是一个**全局刚体旋转**——不是每帧各自旋转一个随机角度，而是所有帧整体旋转相同的偏航/俯仰。

**实践含义：**

1. **foundation model 的位姿误差是相关的，不是独立的。** 如果主点有系统性偏差，所有帧的位姿都被拉向同一个方向，产生高度相关的偏差，而非随机噪声。
2. **BA 无法区分"所有帧旋转了 $\beta_x$"和"主点偏移了 $\beta_x$"。** 在内参固定的 BA 中，这两种情况给出相同的残差，所以BA 会把主点误差"看见"为旋转一致性，然后 quietly 接受它。
3. **修正策略应该是纠正主点，而不是试图在位姿空间中补偿。** 直接纠正 $c_x, c_y$ 之后，"系统性位姿旋转偏差"自动消失。

### 5.6 与代码实现的对应

`pose_delta_near_axis()` 函数实现了公式 $(\delta\phi, \Delta t) = ((-\beta_y, \beta_x, 0), (0,0,0))$：

```python
# se3.py / prior_adapter.py
def pose_delta_near_axis(beta_x: float, beta_y: float) -> np.ndarray:
    # Returns 6D xi = (Δt, δφ) in left-perturbation convention
    return np.array([0.0, 0.0, 0.0, -beta_y, beta_x, 0.0])
```

这个函数是"不跑 BA、直接用解析公式预测位姿偏移"的极限情形实现。在主点偏移已知时，它可以直接给出先验纠正量，不需要 BA Jacobian 计算。

`pose_delta_from_jacobian()` 则是全数值版本：对每帧，用真实的 Jacobian 和 Schur 补来计算 $\Delta\xi_t = -[e_t^T S^{-1}\Gamma_K]\Delta\theta_K$。当场景是宽视角、点偏离光轴时，近轴解析公式不准，需要数值版本。

代码中两者都被保留，因为在不同场景下各有优势：解析版快速、不依赖深度估计；数值版精确、依赖可靠的深度/点云先验。

### 5.7 Lean 形式化（已补完的二维扩展）

当前二维扩展已经在 `JointChannels.lean` 中完成，这一节对应的 Lean 结论可以直接写成下面四句话。

**形式化了什么：** 两视图近轴模型的二维通道联合零残差结构。

**怎么做的：** 把目标写成两个一维目标的直和，再加上显式 roll 项 $\omega_z^2$。

**结果如何：** Lean 给出充要条件
$$\delta\phi_{y1}=\beta_x,\qquad \delta\phi_{x1}=-\beta_y,\qquad t_x=t_y=0,\qquad \delta\phi_z=0.$$

**边界条件与意义：** 这证明的是对角近轴、两深度、无噪声模型；它精确验证了第五章“主点误差等效为全局刚体偏航/俯仰、平移为零、roll 为零”的核心结论，因此 `pose_delta_near_axis()` 的解析形式是有 Lean 支撑的，而不是只靠手推。

---

## 第六章：恒定深度退化——软零空间与可辨识性门控

### 6.1 退化的精确数学描述

第四章已经指出：$x$ 通道的 $2\times 2$ 约化 Hessian

$$H_x = N f_x^2 \begin{pmatrix}\overline{a^2} & \bar{a} \\ \bar{a} & 1\end{pmatrix}$$

（这里 $\bar{a} = \frac{1}{N}\sum a_j$，$\overline{a^2} = \frac{1}{N}\sum a_j^2$）的行列式为

$$\det H_x = N^2 f_x^4 \left(\overline{a^2} - \bar{a}^2\right) = N^2 f_x^4 \operatorname{Var}(a).$$

恒定深度意味着所有逆深度相同，$a_j \equiv a_0$，$\operatorname{Var}(a) = 0$，$\det H_x = 0$。

当 $\det H_x = 0$ 时，$H_x$ 有一个零特征向量（零空间方向），法方程无唯一解。零特征向量是

$$v_\text{null} = \begin{pmatrix}1 \\ -a_0\end{pmatrix} \propto \begin{pmatrix}Z_0 \\ -1\end{pmatrix},$$

对应自由度方向 $(\Delta t_x, \delta\phi_y) \propto (Z_0, -1)$，即：**沿着光轴移动距离 $Z_0$ 同时旋转 $-1$ rad，这两个变化对近轴点的重投影坐标无差别。** 这是熟知的"平移-旋转不可区分"现象在一阶线性化版本中的精确体现。

强迫向量 $b_x = N f_x^2 \beta_x (0, 1)^T$ 在恒定深度情形下满足 $v_\text{null}^T b_x = N f_x^2 \beta_x \cdot (Z_0 \cdot 0 + (-1) \cdot 1) \neq 0$，因此法方程不相容，无解。这意味着在恒定深度场景中，BA 无法在只调整 $(\Delta t_x, \delta\phi_y)$ 的情形下消除 $\Delta c_x$ 带来的残差——它必须调整点的深度。这与第二章"单视图可以任意重分配深度"的结论一致：恒定深度场景中，多视图约束退化回类单视图行为。

### 6.2 软退化：逆深度方差小但非零

实际场景中，恒定深度是极端情形。更常见的是场景"接近"恒定深度——比如对准一面墙、地板或天花板拍摄。此时 $\operatorname{Var}(a) > 0$ 但很小，$H_x$ 满秩但条件数极大：

$$\kappa(H_x) = \frac{\lambda_{\max}}{\lambda_{\min}} \approx \frac{\overline{a^2}}{\operatorname{Var}(a)} = \frac{\overline{a^2}}{\overline{a^2} - \bar{a}^2}.$$

当 $\operatorname{Var}(a) \ll \overline{a^2}$（即场景深度变化远小于平均深度），$\kappa(H_x) \gg 1$，法方程的解对强迫项的微小扰动极为敏感。

**软零空间方向的物理意义：** 在这个方向上的解不稳定。BA 的数值解会沿着软零空间方向漂移，漂移量被数值精度、先验权重等细节主导，而不是被观测几何主导。这种情形下，BA 给出的位姿-内参响应不可靠，不应直接用来推断主点误差大小。

### 6.3 均匀深度分布：解析可辨识性

**命题：** 设深度 $Z_j$ 在区间 $[Z_{\min}, Z_{\max}]$ 上均匀分布，$Z_{\min} < Z_{\max}$。则逆深度 $a_j = 1/Z_j$ 的方差严格正，

$$\operatorname{Var}(a) = \mathbb{E}[a^2] - (\mathbb{E}[a])^2 > 0,$$

$H_x$ 满秩，BA 对 $x$ 通道主点误差的响应是唯一确定的。

**计算：** 对 $Z \sim \operatorname{Uniform}[Z_1, Z_2]$，$Z_1 = Z_{\min}$，$Z_2 = Z_{\max}$：

$$\mathbb{E}[Z^{-1}] = \frac{\ln(Z_2/Z_1)}{Z_2 - Z_1}, \qquad \mathbb{E}[Z^{-2}] = \frac{1}{Z_1 Z_2}.$$

因此

$$\operatorname{Var}(a) = \frac{1}{Z_1 Z_2} - \frac{(\ln(Z_2/Z_1))^2}{(Z_2-Z_1)^2}.$$

令 $r = Z_2/Z_1 > 1$，$Z_1 = 1$：

$$\operatorname{Var}(a) = \frac{1}{r} - \frac{(\ln r)^2}{(r-1)^2} > 0 \quad \forall r > 1.$$

这个不等式可以由 AM-GM 或简单的泰勒展开验证：$(\ln r)/(r-1) < 1/\sqrt{r}$ 对所有 $r > 1$ 成立。

**条件数的显式界：**

$$\kappa(H_x) = \frac{\overline{a^2}}{\operatorname{Var}(a)} = \frac{1/(Z_1 Z_2)}{\operatorname{Var}(a)} = \frac{(r-1)^2}{r \cdot (r-1)^2 - r(\ln r)^2} \cdot \frac{1}{1}.$$

对典型室内场景 $Z_1 = 0.5\text{m}$，$Z_2 = 5\text{m}$（$r = 10$）：

$$\kappa \approx \frac{1/(0.5 \cdot 5)}{1/(0.5 \cdot 5) - (\ln 10)^2/(4.5)^2} \approx \frac{0.4}{0.4 - 0.261} \approx 2.9.$$

条件数约为 3，非常良好可辨识。对比 $Z_1 = 0.9\text{m}$，$Z_2 = 1.1\text{m}$（接近平面，$r = 1.22$）：

$$\kappa \approx \frac{1/(0.9 \cdot 1.1)}{1/(0.9 \cdot 1.1) - (\ln(1.22))^2/(0.2)^2} \approx \frac{1.01}{1.01 - 0.93} \approx 13.$$

条件数约为 13，仍然可接受但开始变差。对平面场景（$r \to 1$），$\kappa \to \infty$。

### 6.4 条件数作为可信度门控的依据

代码中 `principal_point_stability_metrics()` 计算近轴 $H_x$ 的条件数（对 $x, y$ 通道分别计算，取最大值作为场景可辨识性得分），`gate()` 函数用这个条件数对 BA probe 的结果进行软门控：

$$\text{gate}_{\text{BA}}(\kappa) = \sigma\!\left(-\frac{\kappa - \kappa_{\text{thresh}}}{\kappa_\text{slope}}\right),$$

其中 $\sigma$ 是 sigmoid 函数，$\kappa_{\text{thresh}}$ 和 $\kappa_\text{slope}$ 是超参数。当 $\kappa \gg \kappa_\text{thresh}$ 时门控接近 0，BA probe 结果不被信任；当 $\kappa$ 小（场景有丰富深度变化）时门控接近 1，BA probe 结果被完全采纳。

**这个设计的合理性：** BA probe 的结论（"需要旋转 $\beta_x$"）只在 $H_x$ 可逆时有意义。$\kappa$ 大意味着 $H_x$ 接近奇异，此时解的数值不稳定，门控应该降低。用连续 sigmoid 而非硬门控是为了梯度可微。

### 6.5 $\operatorname{Var}(a)$ 的直觉理解：为什么逆深度而不是深度

场景的深度方差 $\operatorname{Var}(Z)$ 和逆深度方差 $\operatorname{Var}(1/Z)$ 是很不同的量。投影方程是线性于 $1/Z$ 的（在近轴近似下，$x = X/Z = X \cdot a$），因此是逆深度的变化决定了观测的分辨能力，而不是深度的变化。

对均匀分布的 $Z$，$\operatorname{Var}(1/Z)$ 主要受近端深度控制（$1/Z_{\min}$ 的贡献远大于 $1/Z_{\max}$）。这意味着：**场景中有近处点比有远处点更有利于内参估计**，这一点在实际的 COLMAP 特征匹配和 BA 中也有体现——近处的良好纹理区域对焦距估计贡献最大。

从 foundation model 的先验角度，深度估计的不确定性也应该加权：近处点的深度估计不确定性（绝对值）往往更大，但如果同时也是逆深度方差的主要贡献者，那么这种不确定性是必须正确处理的，不能简单地因为"远处点深度估计更准"而忽略近处点的内参约束贡献。

### 6.6 `constDepthObjective_eq_zero_iff` 的完整解读

Lean 中已形式化的定理 `constDepthObjective_eq_zero_iff` 处理的正是 $a_1 = a_2$（即单一深度）的情形。**命题：** 当 $a_1 = a_2 = a_0$ 时，一维目标

$$(\beta + t a_0 - \omega)^2 + (\beta + t a_0 - \omega)^2 = 2(\beta + t a_0 - \omega)^2 = 0$$

的解族是 $\omega = \beta + t a_0$，即任意 $t$ 都可以配 $\omega = \beta + t a_0$ 使残差为零。这是一条直线，对应本章所说的"零空间方向"——$(\Delta t_x, \delta\phi_y)$ 可以沿 $(1, a_0)$ 方向任意漂移而不改变残差。

该定理证明了退化情形中解的非唯一性，是"恒定深度场景中 BA 无法辨识主点"这一命题的形式化基础。

联合看 `baObjective2_eq_zero_iff`（$a_1 \neq a_2$：唯一零）与 `constDepthObjective_eq_zero_iff`（$a_1 = a_2$：解族是直线），两个定理完整刻画了"深度变化量"从零到非零时可辨识性的突变。`no_contradiction_summary` 是两者联合的压缩摘要。

**本轮补完的 Lean 结果（`HxMatrix.lean`）：**

**形式化了什么：** 第六章需要的不是“方差大概不为零”，而是“什么时候严格可辨识、什么时候严格退化”的有限点集判别式。

**怎么做的：** 直接在有限索引集 `Fin (n+1)` 上定义均值、二阶矩和方差，再把 `det_HxMatrix` 与 `variance_eq_zero_iff_all_equal` 组合。

**结果如何：** `variance_pos_iff_not_all_equal` 把

$$\operatorname{Var}(a) > 0$$

与"逆深度不全相同"精确对应；`det_HxMatrix_ne_zero_iff` 进一步证明

$$\det H_x \neq 0 \iff f_x \neq 0 \land \neg(\forall i,j,\; a_i=a_j).$$

**边界条件：** 这里证明的是有限点集上的秩判据，不涉及第六章后面给出的条件数近似式和连续分布界。

**它验证了什么、意义是什么：** 它精确验证了第六章的“恒定深度退化、非恒定深度可辨识”二分结构。也就是说，门控逻辑里最关键的“先判断是否退化”这一步，现在已经有 Lean 的是/否判据。

---

## 结构说明：正文主线在这里暂告一段落

到第六章为止，正文主线已经覆盖了当前 checkpoint 真正依赖的那条理论链：$H$ 的定义、单视图不可辨识性、多视图下的位姿吸收机制、Schur 补响应、近轴解以及退化判据。这些内容要么已经进入代码和 Lean，要么直接对应当前 probe 与 checkpoint 的可运行结果。

从这里开始先插入三组附录性分析。它们都和主问题有关，但地位并不一样：

- 附录A：Manhattan 分支。这里有局部正确的线性代数和 Lean 核心，但整条 MW 路线并没有做成当前系统的一部分，也没有闭合成可验证方案。
- 附录B-C：它们是用来解释 `corrected path` 为什么仍会失败的分析分支，其中有一部分已经被 Lean 和 fresh 结果支持，但整体仍然属于“解释机制”，不是正文主线必须经过的里程碑。

正文会在后面的“MoGe 内参注入”和“Checkpoint（May 7）”两节恢复。

---

## 附录A：Manhattan 平面法向量——$H^{-T}$ 作用与旋转先验偏差

### A.1 法向量的变换规律

三维空间中，一个平面可以用一对 $(n, d)$ 表示：$n^T p = d$，其中 $n$ 是单位法向量，$d$ 是到原点的有符号距离。当点 $p$ 经过线性变换 $p \mapsto A p$（$A$ 可逆）时，平面方程变为 $n^T A^{-1}(A p) = d$，即新的法向量（未归一化）是

$$n' = A^{-T} n.$$

这是线性代数中的"协变"变换规律：点（逆变量）用 $A$ 变，法向量（协变量）用 $A^{-T}$ 变，确保内积 $n \cdot p = n'^T (Ap) = (A^{-T}n)^T(Ap) = n^T p$ 不变。

在我们的问题中，内参失配矩阵 $H$ 作用在相机坐标系下的点上。Foundation model 预测的法向量是在模型的假设内参 $\hat{K}$ 下的归一化几何量，对应于真实几何的变形版本。具体地，若真实平面的法向量（在真实相机坐标系下）是 $n^*$，则 foundation model"看到"的（在 $H$ 变形后的点云中计算出来的）法向量方向是

$$\hat{n} \propto H^{-T} n^*.$$

因此，若我们在内参误差已知的情况下想从 foundation model 的输出 $\hat{n}$ 中恢复真实法向量，需要做**逆变换**：

$$n^* \propto H^T \hat{n}.$$

这就是 `correct_normals()` 的数学基础：对每个法向量应用 $H^T$（然后归一化），将 foundation model 的偏移法向量映射回真实几何。

### A.2 Manhattan World 假设与主方向

**Manhattan World (MW) 假设：** 场景中的大多数平面与三个正交主方向（$e_x, e_y, e_z$）之一平行。典型室内场景满足此假设：地板/天花板法向量是 $(0,0,1)^T$（垂直），墙面法向量是 $(1,0,0)^T$ 或 $(0,1,0)^T$（水平）。

在 MW 假设下，foundation model 输出的法向量应该聚集在三个主方向附近。若存在内参误差，这三个主方向会被 $H^{-T}$ 扭曲：

$$H^{-T} e_x = \alpha_x^{-1} e_x, \quad H^{-T} e_y = \alpha_y^{-1} e_y, \quad H^{-T} e_z = -\beta_x/\alpha_x \cdot e_x - \beta_y/\alpha_y \cdot e_y + e_z.$$

（直接计算 $H^{-T}$ 作用在标准基向量上：$H^{-1} = \operatorname{diag}(\alpha_x^{-1}, \alpha_y^{-1}, 1) \cdot \begin{pmatrix}I & -\beta\\ 0 & 1\end{pmatrix}$，其转置就是 $H^{-T}$。）

**关键观察：**

- 水平方向的法向量（$e_x, e_y$）只被拉伸（分别乘以 $\alpha_x^{-1}, \alpha_y^{-1}$），不旋转。
- 垂直方向的法向量（$e_z$，对应天花板/地板）被**混入了** $x$ 和 $y$ 分量：$-\beta_x/\alpha_x$ 和 $-\beta_y/\alpha_y$。

这意味着：**主点误差导致竖直平面（法向量平行于光轴）的 foundation model 预测会有系统性的倾斜偏差，而不是轴对齐平面的预测。** 在 MW 场景中，地板法向量不再指向 $(0,0,1)^T$，而是指向 $(-\beta_x/\alpha_x, -\beta_y/\alpha_y, 1)^T$（归一化后），对应一个小的倾斜角。

### A.3 从 MW 主方向估计旋转先验偏差

设 foundation model 输出的法向量中，我们能识别出应该对应 $e_z$ 的那一族（地板/天花板法向量）。它们的均值方向是

$$\hat{n}_z^{\text{MW}} \propto H^{-T} e_z = (-\beta_x/\alpha_x,\; -\beta_y/\alpha_y,\; 1)^T.$$

如果场景有多面墙和地板，可以对所有三个主方向的观测做联合拟合。设 $N^*$ 是真实主方向矩阵（$3\times 3$ 正交矩阵），$\hat{N}$ 是 foundation model 预测的（扭曲的）主方向矩阵。我们需要找一个旋转 $R$，使得

$$R \hat{N} \approx N^*$$

在某种最小二乘意义下最优。最优解是 $R = N^* \hat{N}^T$ 的极分解。

在纯主点误差（$\alpha_x = \alpha_y = 1$）且误差较小的情形下，$\hat{N} = H^{-T} N^* = (I - \beta e_z^T) N^*$，$H^{-T} = I - \beta e_z^T$（$\beta = (\beta_x, \beta_y, 0)^T$）。

最优旋转的一阶近似：$R \approx I + [\delta\omega]_\times$，其中

$$\delta\omega = \text{antisym}(\hat{N} (N^*)^T - I) \text{ 的轴角参数}.$$

对 MW 情形（$N^* = I$，三个主方向即为坐标轴）：

$$\hat{N} = H^{-T} = I - \beta e_z^T,\quad \hat{N} (N^*)^T = \hat{N} = I - \beta e_z^T.$$

$\hat{N} - I = -\beta e_z^T$，其反对称部分是

$$\operatorname{antisym}(-\beta e_z^T) = \frac{1}{2}\left(-\beta e_z^T + e_z \beta^T\right) = \frac{1}{2}\begin{pmatrix}0 & 0 & -\beta_x \\ 0 & 0 & -\beta_y \\ \beta_x & \beta_y & 0\end{pmatrix}.$$

对应的旋转向量（$[\delta\omega]_\times$ 的轴向量）是

$$\delta\omega_\text{MW} = \frac{1}{2}\begin{pmatrix}-\beta_y \\ \beta_x \\ 0\end{pmatrix}.$$

**这个因子 $1/2$ 的来源：** 将 $H^{-T}$ 分解为对称部分和反对称部分，旋转来自反对称部分。而 $-\beta e_z^T$ 是**非**对称矩阵，其反对称化会引入 $1/2$ 系数。相比之下，第三章的 Procrustes 解给出 $\delta\omega \approx (-\beta_y, \beta_x, 0)^T$（因子 1），区别在于 Procrustes 最小化的是三维点的整体形变，而 MW 方法只用法向量；两者在同样的内参误差下给出不同量级的旋转估计。

**因子 $1/2$ 的物理含义：** 平面法向量的旋转是点的旋转的"半反应"。若点旋转了 $\delta\omega$，则平面旋转了 $\delta\omega$（法向量随平面一起转），但 $H^{-T}$ 对法向量的作用不等同于对点的作用——法向量是一等参数的协变量，变换规律不同。在几何上，$H$ 对点的 shear 导致法向量的 shear，两者不是简单的旋转关系。

### A.4 `correct_normals()` 中的极分解结构

实际实现中，`correct_normals(normals, H)` 接受一张法向量图和失配矩阵，对每个法向量 $\hat{n}$ 应用

$$n^* = \frac{H^T \hat{n}}{\|H^T \hat{n}\|}.$$

这是精确的，不受一阶近似的限制。

**然而有一个细节：** $H^T \hat{n}$ 把法向量改变为"在真实坐标系下的方向"，但它改变的是相机坐标系下的法向量。若 foundation model 预测的法向量已经包含了位姿估计的偏差（即位姿被 $H$ 剪切吸收了一部分），那么仅做 $H^T$ 校正是不够的——还需要叠加位姿校正。这是"corrected path"和"rerun path"的根本分歧：前者假设所有误差都在法向量的坐标表示里，后者通过重跑 foundation model 让模型用新内参重新生成几何先验，从而同时校正深度、法向量和点图。

### A.5 MW 旋转作为位姿先验偏差的独立估计

$\delta\omega_\text{MW} = (-\beta_y/2, \beta_x/2, 0)^T$ 可以作为一个独立的估计量——不依赖 BA Schur 补计算，只需要识别场景中的 MW 主方向。这个估计量告诉我们：位姿先验中大约有多大的系统性旋转偏差。

代码中 `prior_adapter.py` 的门控和校正逻辑中，MW 方向识别不是当前流程的标准步骤（目前用的是深度/法向量统计量），但 $H^{-T}$ 对 MW 法向量的扭曲是解释"为什么 corrected path 仍然有偏"的重要线索：即使我们应用了 $H^T$ 校正单个法向量，法向量图的整体**一致性约束**（相邻像素法向量应连续、应和深度梯度协调）被破坏了——这些全局一致性只有通过重跑才能自然恢复。

### A.6 法向量校正的有效性边界

$n^* = H^T \hat{n} / \|H^T \hat{n}\|$ 的精度依赖以下假设：

1. Foundation model 的法向量在其假设内参下是"正确的"——即它正确地计算了变形场景 $\{H p_i^*\}$ 中平面的法向量，不是以某种端到端方式直接预测"近似真实法向量"。
2. $H$ 是精确已知的（需要先估计 $K^*$）。
3. 法向量是局部平面的真实法向量，不受遮挡、噪声和非平面区域的干扰。

当这些条件满足时，法向量校正是精确的（在精确 $H^T$ 变换下）；当 foundation model 的法向量预测包含了非线性响应（即模型并非在精确做几何法向量，而是做了某种学习的近似），校正只能去除几何线性部分，剩余的非线性偏差仍然存在。

### A.7 Lean 形式化

目前 `PrincipalPoint.lean` 没有直接涉及法向量变换。法向量校正的形式化需要：

1. 定义"平面"为 $\{p : n^T p = d\}$ 的类型，以及变换规律 $n \mapsto A^{-T} n$（保持内积不变）。
2. 定义"Manhattan World"为三个正交主方向（可以是一个 `Finset` 或三元组），以及 foundation model 输出为 $H^{-T}$ 作用在这些方向上。
3. 证明极分解 $R = \operatorname{polar}(H^{-T} N^*)$ 给出的最优旋转，在 $H = I + E$ 的一阶展开下等于本章推导的 $I + [\delta\omega_\text{MW}]_\times$。
4. 验证 $\delta\omega_\text{MW}$ 的 $1/2$ 系数来自 $E - E^T = -\beta e_z^T + e_z \beta^T$ 的结构（$E = -\beta e_z^T$ 的非对称性）。

**本轮补完的 Lean 结果（`Normals.lean`）：**

**形式化了什么：** 附录A真正依赖的有两件事：法向量必须按 $H^{-T}$ 变换；$1/2$ 系数必须来自一个明确的反对称化公式，而不是“手感上像”。

**怎么做的：** 我们没有在这一轮强上极分解 `polar(...)` 的全链路，而是先把本章论证真正需要的线性代数核心拆成两个 Lean 定理。

1. `pureShear_plane_invariant`：对纯主点剪切
   $$H=\begin{pmatrix}1&0&\beta_x\\0&1&\beta_y\\0&0&1\end{pmatrix},\qquad
   H^{-T}=\begin{pmatrix}1&0&0\\0&1&0\\-\beta_x&-\beta_y&1\end{pmatrix},$$
   Lean 精确证明
   $$\langle H^{-T}n,\;Hp\rangle=\langle n,\;p\rangle.$$
   这就是平面法向量协变变换律的形式化版本。
2. `shearError_skewPart`：对 $E=H-I$，Lean 精确算出
   $$\operatorname{skew}(E)=\frac12(E-E^T),$$
   且其中的非零项正是 $\pm \beta_x/2,\pm \beta_y/2$。这把正文中 Manhattan 旋转修正里 $1/2$ 系数的来源固定成了一个矩阵恒等式。

**结果如何：** 法向量用 $H^{-T}$ 变换这一点、以及 $1/2$ 系数来自反对称化这一点，都已经是 Lean 中可编译的定理。

**边界条件：** 本轮没有形式化极分解最优旋转本身，所以附录A中“MW 最优旋转等于某个 polar 因子”的那一层仍然保留在正文解析推导里。

**它验证了什么、意义是什么：** 它验证了附录A最容易被质疑的两步: “为什么法向量不是跟点一样乘 $H$？”以及“为什么这里偏偏有 $1/2$？” 现在答案都不再是手算说明，而是 Lean 矩阵恒等式。

---

## 附录B：仿射不兼容不等于曲率——常数权融合给出仿射平均

### B.1 问题的提法

当内参失配矩阵 $H \neq I$ 时，foundation model 在帧 $t$ 输出的深度图 $\hat{D}_t$ 和点图 $\hat{P}_t$ 是在错误内参 $\hat{K}$ 下计算的，对应真实几何的仿射变形版本。直觉上，"把变形的深度图用 $H$ 校正之后拿去做 BA"应该等价于"用真实内参做 BA"。但实验中我们观察到：即使做了 $H$-校正，结果仍然比重跑 foundation model 差，而且差距在场景的不同区域不均匀。

一个常见的猜测是：**"校正不够精确，是因为深度图在边缘处有仿射不连续性，这会产生额外的曲率误差。"** 本章证明这个猜测是错误的：**仿射不兼容本身不能产生曲率误差**。然后附录C再解释真正的曲率来源。

### B.2 仿射变换对深度的作用

仿射变换 $H$ 对三维点的作用是对相机坐标做线性变换：$(X, Y, Z) \mapsto (\alpha_x X + \beta_x Z, \alpha_y Y + \beta_y Z, Z)$。在这个变换下，深度分量 $Z$ 不变（因为 $H$ 的第三行是 $(0,0,1)$），但 $X, Y$ 分量有线性混入。

在深度图 $D(u, v)$（像素坐标 $(u,v)$ 处的深度值）的层面：

$$\hat{D}(u, v) \equiv D^*(u, v) = Z\big|_{T^* X^*: \pi_K(T^* X^*) = (u,v)^T},$$

因为深度不依赖 $\alpha_x, \alpha_y, \beta_x, \beta_y$（$H$ 的第三行精确是 $(0,0,1)$）。换言之，**深度图（在像素坐标系下）对纯主点误差不变**。

对纯焦距误差（$\alpha_x \neq 1$ 或 $\alpha_y \neq 1$，$\beta_x = \beta_y = 0$），深度图也不变。总结：**$H$ 对深度图（定义为像素坐标到深度的映射）没有直接影响。**

**那么什么受影响？** 是**点图**（pointmap）：$\hat{P}(u,v)$ 是在模型假设的归一化坐标系下的三维坐标 $(\hat{x}_{uv} \cdot Z, \hat{y}_{uv} \cdot Z, Z)$，其中 $(\hat{x}_{uv}, \hat{y}_{uv}) = \hat{K}^{-1}(u,v,1)^T$ 是模型假设的归一化坐标。真实归一化坐标是 $(K^*)^{-1}(u,v,1)^T$，两者之差恰好是 $H \hat{K}^{-1} - (K^*)^{-1} = H \hat{K}^{-1} - H \hat{K}^{-1} = 0$——等等，这里需要更仔细：

$$\hat{x}_{uv} = (u - \hat{c}_x)/\hat{f}_x, \quad x^*_{uv} = (u - c_x^*)/f_x^*.$$

$$x^*_{uv} - \hat{x}_{uv} = \frac{u - c_x^*}{f_x^*} - \frac{u - \hat{c}_x}{\hat{f}_x} = \frac{(u - \hat{c}_x)(\hat{f}_x - f_x^*) + f_x^*(\hat{c}_x - c_x^*)}{\hat{f}_x f_x^*} \cdot f_x^*...$$

更简洁地：真实点图中像素 $(u,v)$ 对应的三维点（相机坐标）是

$$p^*_{uv} = Z_{uv} \cdot (K^*)^{-1}\begin{pmatrix}u\\v\\1\end{pmatrix},$$

而模型"认为的"点图是

$$\hat{p}_{uv} = Z_{uv} \cdot \hat{K}^{-1}\begin{pmatrix}u\\v\\1\end{pmatrix} = Z_{uv} \cdot (K^*)^{-1} H \begin{pmatrix}u\\v\\1\end{pmatrix}... \text{（不对，需要重新算）}$$

实际上 $\hat{K}^{-1} = (K^* H^{-1})^{-1} \cdot (K^*)^{-1}$，即 $\hat{K}^{-1}\mathbf{u} = H (K^*)^{-1} \mathbf{u}$（其中 $\mathbf{u} = (u,v,1)^T$）？不对，让我重新写：$H = \hat{K}^{-1} K^*$，所以 $\hat{K}^{-1} = H (K^*)^{-1}$。因此

$$\hat{p}_{uv} = Z_{uv} \cdot \hat{K}^{-1}\mathbf{u} = Z_{uv} \cdot H (K^*)^{-1} \mathbf{u} = H p^*_{uv}.$$

**结论：模型预测的点图是真实点图的 $H$ 变形**：$\hat{p}_{uv} = H p^*_{uv}$。

### B.3 常数权融合给出仿射平均

设有两帧 ($t=1,2$) 的点图预测 $\hat{p}^{(1)}_{uv}$ 和 $\hat{p}^{(2)}_{uv}$（这两帧因为位姿不同，即使同一空间点在两帧的点图坐标不同）。在以常数权 $w_1, w_2$（$w_1+w_2=1$）融合时，融合结果是

$$\bar{p} = w_1 \hat{p}^{(1)} + w_2 \hat{p}^{(2)} = w_1 H p^{*(1)} + w_2 H p^{*(2)} = H(w_1 p^{*(1)} + w_2 p^{*(2)}).$$

**融合结果仍然是 $H$ 作用在真实融合结果 $w_1 p^{*(1)} + w_2 p^{*(2)}$ 上。**

这说明：$H$ 的线性性保证了常数权融合与 $H$ 变换可以交换顺序。融合 $\hat{p}$ 等价于先融合 $p^*$ 再做 $H$ 变换。**仿射变换不破坏线性融合的结构**：融合后的点图仍然是"仿射变形的真实融合"，不产生任何新的非线性误差。

**用符号更精确地说：** 设真实融合点图是 $\bar{p}^* = w_1 p^{*(1)} + w_2 p^{*(2)}$，则融合 $\hat{p}$ 等于 $H \bar{p}^*$。用 $H^{-1}$ 校正融合结果给出 $H^{-1}(H \bar{p}^*) = \bar{p}^*$。这是精确的，没有任何误差——无论两帧的 $p^{*(1)}$ 和 $p^{*(2)}$ 如何，常数权融合后的 $H$-校正总是精确恢复真实融合结果。

### B.4 "仿射不兼容"是什么意思，为什么它不产生曲率

"仿射不兼容"是指：两帧观测同一个场景，但由于视角不同，同一空间点在两帧坐标系下的 $H$ 变形是不一样的（因为 $H$ 作用在相机坐标系下的点，而不是世界坐标系下的点——不同视角下同一点的相机坐标不同，$H$ 作用后的结果也不一样）。

然而，这种"不兼容"只是两帧的预测各自包含了本帧的 $H$ 变形，它们依然是各自真实几何的精确仿射变换。当我们用常数权融合时，融合操作本身是线性的，仿射性被保留。不兼容不产生**额外**误差：融合后做 $H^{-1}$ 校正，得到的仍是真实融合结果，不多不少。

**曲率误差的实际来源（留待附录C）：** 常数权假设本身是问题所在。真实的深度融合通常是**加权融合**，权重依赖于深度值、置信度、或可见性——这些权重随像素位置变化（空间变化权重），而不是常数。当权重是空间函数时，$H$ 不再能直接提出来，仿射性被打破，产生了高阶的曲率贡献。

### B.5 应用到深度图的仿射校正

同样的论证适用于深度图的 affine 校正。若深度先验预测为 $\hat{D}_t$，真实深度为 $D^*_t$，并假设它们之间存在仿射关系：$\hat{D}_t(u,v) = a_t D^*_t(u,v) + b_t$（尺度 $a_t$ 和偏置 $b_t$），则用 $a_t, b_t$ 做校正

$$D^*_t(u,v) = (\hat{D}_t(u,v) - b_t) / a_t$$

给出精确的深度图（在仿射假设成立的条件下）。

常数权融合多帧校正后的深度等价于先校正各帧再融合——结果不依赖融合顺序。这再次是仿射线性性的直接推论。代码中 `fit_depth_affine()` 正是在估计这个 $(a_t, b_t)$；校正之后深度先验的系统性误差被消除。

### B.6 形式化陈述（供后续 Lean 使用）

本章的核心命题可以形式化为：

**命题（仿射融合交换律）：** 设 $H$ 是 $\mathbb{R}^3$ 上的可逆线性变换。设 $\{p_k\}_{k=1}^K$ 是一组向量，$\{w_k\}$ 是常数权重满足 $\sum w_k = 1$。则

$$H\!\left(\sum_k w_k p_k\right) = \sum_k w_k (H p_k).$$

**证明：** 线性映射的线性性，三行验证。$\square$

推论：**对常数权融合操作，$H$ 校正和融合可以自由交换顺序**，不产生任何残差。

这个命题在 Lean 中是直接可证的（`map_add`, `map_smul` 已在 Mathlib 中），不需要额外工作。难点在于附录C的非常数权情形。

---

## 附录C：空间变化权重与真正的曲率误差

### C.1 附录B的漏洞：权重是常数吗？

附录B的结论依赖于权重 $w_k$ 是常数——与像素位置 $(u,v)$ 无关。但在真实的 foundation model 融合和 BA 先验使用中，权重几乎必然是空间变化的：

1. **深度置信度随距离衰减。** 近处点的深度预测通常比远处点更准确；foundation model 的不确定性估计（如果它输出一个）在不同深度区域不同。
2. **可见性和遮挡依赖像素位置。** 一帧看得见的区域，另一帧可能被遮挡；融合时只有可见的像素有贡献。
3. **基于特征匹配的点更稀疏。** COLMAP 的特征点密度在纹理丰富区域高、平坦区域低，使得 BA 残差的有效权重在图像上非均匀分布。
4. **Foundation model 的感受野效应。** 模型对中心区域的预测通常比边缘更稳定，边缘区域的预测可能有额外偏差。

这些效应使得有效权重 $w(u,v)$ 是像素位置的函数，而 $H$ 不再能从空间变化权融合中"提出来"。

### C.2 空间变化权重破坏仿射交换律

设两帧深度图 $D_1(u,v), D_2(u,v)$（已转换为点图 $p_1, p_2$），其仿射变形版本是 $\hat{p}_1 = H p_1$，$\hat{p}_2 = H p_2$。用空间变化权 $w(u,v)$ 融合（这里 $w$ 是单帧权，$1-w$ 是另一帧权）：

$$\bar{p}(u,v) = w(u,v)\,\hat{p}_1(u,v) + (1-w(u,v))\,\hat{p}_2(u,v).$$

用 $H^{-1}$ 校正融合结果：

$$H^{-1}\bar{p}(u,v) = w(u,v)\,p_1(u,v) + (1-w(u,v))\,p_2(u,v) = \bar{p}^*(u,v).$$

这步是精确的——仿射交换律仍然成立！乘以常数 $H^{-1}$ 可以直接通过空间变化权。

**那么曲率误差从哪里来？**

答：不是来自对两个点图的线性融合，而是来自**融合后推导法向量（或曲率）时**的非线性性。法向量是通过对点图做空间微分得到的：$n \propto \partial \bar{p}/\partial u \times \partial \bar{p}/\partial v$。当权重 $w(u,v)$ 是空间函数时，$\bar{p}(u,v)$ 的微分包含权重的梯度 $\nabla w$，而真实的融合结果 $\bar{p}^*$ 的微分也包含 $\nabla w$——两者不同，即便 $\bar{p}(u,v)$ 可以精确恢复 $\bar{p}^*(u,v)$（通过 $H^{-1}$ 校正）。

**更精确地说：** 设 $\bar{p}^* = w p_1^* + (1-w) p_2^*$，其法向量含有项 $(\nabla w) \times (p_1^* - p_2^*)$。当 $H \neq I$ 时，在帧 1 和帧 2 之间，同一世界点的相机坐标不同，$p_1^* \neq p_2^*$（它们是不同视角下同一点的坐标），$p_1^* - p_2^*$ 受视角几何控制；$H$ 作用后 $\hat{p}_1 - \hat{p}_2 = H(p_1^* - p_2^*)$，差值也被 $H$ 变换。法向量的表达式含有 $H(p_1^* - p_2^*)$ 和 $\nabla w$ 的叉积，而真实法向量含有 $(p_1^* - p_2^*)$ 和 $\nabla w$ 的叉积。两者之差是 $(H-I)(p_1^* - p_2^*)$ 和 $\nabla w$ 的叉积，这是**真正的曲率误差**。

### C.3 约化投影模型下的权重结构

设场景点的深度 $Z$ 在第 $t$ 帧的归一化坐标 $(x,y)$ 处的约化投影残差为

$$r_t(x,y) = \hat{D}_t(x,y) - D^*_t(x,y).$$

这个残差是因为模型在 $\hat{K}$ 下计算的归一化坐标不等于真实归一化坐标 $(K^*)^{-1}(u,v,1)^T$——差别恰好是 $H$ 对归一化坐标的变换。

在一阶展开下，把基于 $\hat{K}$ 的预测和基于 $K^*$ 的真实值做展开，BA 残差的有效权重（来自信息矩阵的对角项）为

$$w_t(u,v) \propto \sigma_{Z,t}(u,v)^{-2},$$

其中 $\sigma_{Z,t}^2(u,v)$ 是该像素处深度预测的方差（先验不确定性）。若 foundation model 的不确定性是深度的函数（通常是）：$\sigma_Z \propto Z^\gamma$（$\gamma \in [0,1]$ 典型），则

$$w_t(u,v) \propto Z_t(u,v)^{-2\gamma}.$$

这是一个依赖深度（从而依赖像素位置）的空间变化权重。

### C.4 Manhattan 法向量的空间梯度机制

在 MW 场景中，地板（法向量 $e_z$）和墙面（法向量 $e_x$ 或 $e_y$）的交界线处，深度图有一个急剧的跳变（地板近端遮挡了更远的空间）。这里 $\nabla w$ 最大，$p_1^* - p_2^*$ 在法向量不连续处也最大，因此曲率误差 $(H-I)(p_1^*-p_2^*) \times \nabla w$ 在这些区域最显著。

换句话说，**内参误差产生的曲率误差集中在深度不连续处（平面交界线）**，这与在均匀平面内部（法向量连续）几乎无误差的观察一致。实验中观察到的"校正路径在角落区域特别差"恰好符合这个预测。

### C.5 $\operatorname{Cov}(a, \eta)$ 解释

考虑单帧的 BA 残差对内参的响应。将 BA 残差写成（近轴极限）：

$$r_{tj} = \beta_x + \Delta t_{x,t} / Z_{tj} - \delta\omega_{y,t}.$$

如果真实位姿响应已经是 $(\Delta t_{x,t} = 0, \delta\omega_{y,t} = \beta_x)$（第三章的最优解），那么理想情况下残差为零。但若深度先验中混入了深度相关的噪声 $\eta_{tj}$（$\eta_{tj} = \hat{Z}_{tj} - Z_{tj}^*$ 是深度估计误差），则有效的逆深度是 $\hat{a}_{tj} = 1/\hat{Z}_{tj} \neq 1/Z_{tj}$。

在先验残差贡献的一阶展开下，偏差的贡献正比于 $\operatorname{Cov}(a_{tj}, \eta_{tj})$——逆深度与深度误差的协方差。若误差 $\eta$ 是独立零均值的，$\operatorname{Cov} = 0$，无系统偏差；若误差与深度相关（如 foundation model 在近处高估深度、远处低估），$\operatorname{Cov} \neq 0$，产生额外的系统性位姿偏差。

这个机制解释了为什么即使深度先验仿射校正 $(a_t, b_t)$ 做了线性修正，残留的非线性深度误差（与深度相关的非线性函数）仍然能通过 $\operatorname{Cov}(a, \eta)$ 通道污染位姿估计。

### C.6 为什么直接校正路径 (corrected path) 仍然弱

综合本章的分析，`corrected path` 的弱点来自两个相互独立的原因：

**原因一：法向量图全局一致性破坏。** $H^T$ 对法向量做逐像素校正，但校正后的法向量图不满足深度几何的全局一致性——相邻像素的法向量不再和深度梯度对齐，出现人工纹理和边界伪影。这些不一致性在 BA 中表现为局部约束的自相矛盾，降低先验的整体有效权重。

**原因二：非线性模型响应（$g_{\text{model}} \neq g_{\text{geometry}}$）。** Foundation model 的深度和法向量预测是端到端学习的，不是对几何方程 $\hat{p} = H p^*$ 的精确计算。实际上，模型看到的是像素值，不是几何点，其响应是一个非线性函数。当输入图像中主点不在中心时，模型会做出某种"补偿性"预测（可能是学会了某种近似），但这个补偿不等于精确的 $H$ 几何变换。差值 $g_{\text{model}}(\hat{K}) - g_{\text{geometry}}(H)$ 是模型的"几何幻觉"，$H^T$ 校正只能去除几何部分，无法去除幻觉部分。

**`rerun path` 为什么更好：** 重新用 $K^*$ 跑一次 foundation model，模型接受的就是"对于 $K^*$ 来说是中心主点的"图像（因为图像被重新裁剪或 MoGe 的 `intrinsics=` 参数已被设置），模型的非线性预测是针对这个正确输入的，从而避免了"几何幻觉"。代价是一次额外的 foundation model 推理，但对精度的改善是根本性的，而不是边际的。

### C.7 Lean 形式化（已完成的离散版）

本章已经在 `AffineFusion.lean` 中落下了一个离散有限差分版本，写法如下。

**形式化了什么：** 不是去碰连续微分和协方差，而是先把“常数权时可交换、变权时会多出一个项”这件事做成离散差分定理。

**怎么做的：** `linearMap_sum_smul` 与 `linearMap_blend` 先证明线性映射和常数权融合可交换；随后 `finiteDifference_blend` 对相邻两个像素的差分直接展开，得到

$$\Delta\bigl(wp + (1-w)q\bigr)
= w_0\Delta p + (1-w_0)\Delta q + (w_1-w_0)(p_1-q_1).$$

最后一项 $(w_1-w_0)(p_1-q_1)$ 就是连续推导里 $\nabla w$ 与跨视角几何差值耦合的离散对应。`finiteDifference_blend_after_linearMap` 再证明，在线性映射 $H$ 作用后，这个附加项整体变成 $H(p_1-q_1)$。

**结果如何：** Lean 现在精确区分了两种情形。
- 常数权：融合和线性校正可交换。
- 变权：会额外冒出一项，且这一项正比于“权重变化 × 几何差值”。

**边界条件：** 这是离散差分版本，不是连续偏导版本；也没有把法向量叉积和 $\operatorname{Cov}(a,\eta)$ 一起纳入 Lean。

**它验证了什么、意义是什么：** 它验证了附录C最关键的判断：`corrected path` 的问题不是“线性校正失效”，而是“变权后的几何差值项一定会出现”。这和正文预期完全一致，也解释了为什么常数权下附录B成立、但附录C仍然会有曲率误差。

---

## 附录分组说明：从分析分支转入系统化草案

前面的附录A-C更接近“分析分支”：它们解释了一些可能机制，澄清了一些常见误解，但并不都构成当前 checkpoint 已经闭合的实现结果。下面的附录D-H则是另一类材料：它们主要是研究推进过程中写下的系统化草案、接口草案和调度草案，保留存档，但不应误读成正文已经逐章实现的系统设计。

---

## 附录D：先验状态策略——已知、估计与不确定内参下的不同处理路径（系统化草案）

### D.1 三种状态的定义

Foundation model 先验的可信度根本上取决于内参是否已知，以及已知到什么程度。定义三种先验状态：

**状态 A：$K^*$ 已知且可信（known）。** 相机有 EXIF 数据、制造商规格或已做过精确标定，$K^*$ 的误差远小于一个像素。此时 $H \approx I$，foundation model 的输出是可直接使用的（或做极小修正）。先验的系统偏差来源消失。

**状态 B：$K^*$ 已可靠估计但非标定（estimated-reliable）。** 通过 SfM（COLMAP）、`SeparableSharedIntrinsicSolver` 或其他方法得到了 $K^*$ 的估计 $\hat{K}^{(\text{est})}$，估计误差有界且量化可知。此时 $H^{(\text{est})} = (\hat{K}^{(\text{est})})^{-1} K^*$ 仍然未知，但 $\hat{K}^{(\text{est})}$ 可以被用来替代 $K^*$ 重跑 foundation model，或用来计算校正矩阵 $H^{(\text{corr})} = \hat{K}_0^{-1} \hat{K}^{(\text{est})}$（把模型的假设内参 $\hat{K}_0$ 替换为估计内参 $\hat{K}^{(\text{est})}$）。

**状态 C：$K^*$ 未知或估计不可靠（unknown/unreliable）。** 没有任何内参信息，或者估计来自退化场景（恒定深度，Schur 补条件数极大），估计值不可信。此时不知道 $H$，无法做任何有意义的校正。先验必须以非常低的权重使用，或完全舍弃。

### D.2 状态 A：直接使用先验（raw path）

当 $K^*$ 已知时，foundation model 被假设以 $K^*$ 运行（或其假设内参 $\hat{K}_0 \approx K^*$），$H \approx I$。先验的点图、深度图和法向量可以直接作为 BA 的软约束，不需要任何校正。

**Gate 设计：** 此时 `gate_channels()` 对所有通道（深度、法向量、pointmap）均输出权重 1（满权），或由估计不确定性决定的适当权重。分类器 `classify_prior_state()` 应返回 `"raw"` 或 `"direct"`。

**实践注意事项：** 即使 $K^*$ "已知"，仍需核查 foundation model 的假设内参是否与 $K^*$ 一致。对 MoGe 而言，它假设 $c_x = c_y = 0.5$（归一化）并自估焦距，这与 $K^*$ 通常不同。即使 $K^*$ 精确已知，仍然需要通过 `intrinsics=` 参数传入 MoGe，或者重新裁剪图像使主点居中，才能进入状态 A 的真正意义。

### D.3 状态 B：校正或重跑路径（corrected/debiased/rerun）

**B1. Corrected path（仅校正，不重跑）：**

计算 $H^{(\text{corr})} = \hat{K}_0^{-1} \hat{K}^{(\text{est})}$，对点图、深度图、法向量分别做：

$$p^{(\text{corr})} = (H^{(\text{corr})})^{-1} \hat{p}, \quad D^{(\text{corr})} = D_{\text{affine}}, \quad n^{(\text{corr})} = \frac{(H^{(\text{corr})})^T \hat{n}}{\|(H^{(\text{corr})})^T \hat{n}\|}.$$

深度的仿射校正 $(a_t, b_t)$ 由 `fit_depth_affine()` 通过 Schur 补约束或 COLMAP 稀疏点对比估计。

优点：无需重跑 foundation model，节省计算时间。  
缺点：附录C所述的非线性误差仍然残留；法向量全局一致性被破坏；效果受 $H$ 估计精度限制。

**B2. Debiased path（去偏路径）：**

同校正路径，但额外对法向量施加局部一致性平滑（基于深度图梯度重新计算法向量，而不是直接 $H^T$ 变换单个法向量）。具体实现是：先将 corrected pointmap $p^{(\text{corr})}$ 做局部法向量重估（用中心差分 $\partial_u p \times \partial_v p$），替换 $H^T$ 变换的法向量。这样得到的法向量与 corrected 点图几何自洽，但与原始 foundation model 的法向量预测不同。

优点：恢复局部几何一致性。  
缺点：计算代价高（额外一次法向量估计）；仍然无法修正 foundation model 非线性响应的误差。

**B3. Rerun path（重跑路径）：**

用 $\hat{K}^{(\text{est})}$ 替换 $\hat{K}_0$，重新调用 foundation model 的推理。若 foundation model 支持 `intrinsics=` 参数（如 MoGe 被修补之后），直接传入 $\hat{K}^{(\text{est})}$；否则先对图像做重新裁剪/padding 使主点居中，再以新的等效 $K^*$ 运行。

优点：foundation model 的所有先验（深度、法向量、pointmap）都是在正确（或接近正确）内参下计算的，非线性误差被消除，全局一致性自然满足。  
缺点：额外的推理时间；需要 foundation model 支持外部内参输入（或图像预处理）。

**分类器逻辑（`classify_prior_state()` 的正确行为）：**

在状态 B 下，`classify_prior_state()` 应检查：
1. $\hat{K}^{(\text{est})}$ 是否可靠（Schur 条件数是否合理）。
2. 是否有资源重跑（`rerun_available`）。
3. 主点偏移 $\|(\beta_x, \beta_y)\|$ 是否显著（值大的时候，直接校正误差大，重跑收益高）。

若 (1) 可靠 + (2) 可重跑 → 推荐 `"rerun"`（最高质量）。  
若 (1) 可靠 + (2) 不可重跑 + (3) 偏移小 → 推荐 `"corrected"`。  
若 (1) 可靠 + (2) 不可重跑 + (3) 偏移大 → 推荐 `"debiased"` 或带警告的 `"corrected"`。  
若 (1) 不可靠 → 降级为状态 C 处理。

**Phase 6 修复的就是这个逻辑：** 原实现把主点偏移大作为"不推荐重跑"的条件，恰好反了——偏移大时重跑收益最大，应该更积极地推荐重跑。

### D.4 状态 C：低权或舍弃（gated/filtered path）

当 $K^*$ 未知或估计不可靠时，无法确定 $H$，也无法做有意义的校正。有两种处理：

**C1. Gated path：** 对所有先验通道施加低权重（$\approx 0$），让 BA 自由运行不受先验约束。这是稳健性最高的选择：宁可不加先验，也不加有偏先验，因为有偏先验比没有先验更危险——它会在错误方向上拉动优化器。

**C2. Filtered path：** 只保留对内参不敏感的先验信息。例如：
- 相对深度（归一化后的深度顺序）：只要仿射假设成立，相对深度在 $H$ 作用下不变（$Z$ 不变）。
- 法向量中的非 $e_z$ 成分（水平墙壁法向量）：只受 $\alpha_x, \alpha_y$ 影响，对主点 $(\beta_x, \beta_y)$ 不敏感。

代码中 `gate_channels()` 实现了逐通道的软门控，根据估计的可靠性分别降低各通道权重，而不是整体丢弃。

### D.5 状态转换与迭代更新

理想的系统行为是迭代的：初始时 $K^*$ 未知（状态 C），用保守先验做初步重建；从初步重建的 COLMAP 跟踪结果中估计 $K^*$（进入状态 B 或 A）；然后用更新的 $K^*$ 重跑或校正先验，做精确重建。这是附录E（估计 $K^*$ 的方法）和附录H（完整优化调度）的内容。

本章只建立了状态分类的数学框架，不预设如何估计 $K^*$。

---

## 附录E：在信任先验之前先估计 $K^*$——可分离求解器与安全调度（系统化草案）

### E.1 为什么要先估计，不能先用先验

附录D指出，当 $K^*$ 未知时先验不可信。一个自然的问题是：能不能先用先验来做一个"大概"的重建，然后从重建结果中估计 $K^*$？

问题在于：如果先验是有偏的，"大概"的重建也是有偏的，从有偏重建中估计的 $K^*$ 同样有偏，如此形成一个偏差强化的正反馈循环。因此，正确的顺序是：**先用对内参不敏感的、或无先验的方法做初步重建，从初步重建中估计 $K^*$，然后再用 $K^*$ 激活先验。**

### E.2 可分离求解器（`SeparableSharedIntrinsicSolver`）

`SeparableSharedIntrinsicSolver` 是当前主要的 $K^*$ 估计工具。它的核心思想是：利用 COLMAP 提供的特征点跟踪（tracks），对每条跟踪中的每对视图建立归一化坐标一致性约束，然后用非线性最小二乘（`scipy.least_squares`）拟合共享的内参 $(\alpha_x, \alpha_y, \beta_x, \beta_y)$（或等价地 $f_x, f_y, c_x, c_y$）。

**关键特性：**
- **共享内参：** 所有帧共享一个 $K^*$，这是欠约束问题变得可解的关键（单帧不可辨识，多帧共享则可辨识）。
- **无先验依赖：** 残差只来自重投影几何（特征点三角化），不涉及深度或法向量先验，因此不会被 foundation model 偏差污染。
- **精度：** 在非退化场景（充分的基线和深度变化）下，可达到 $\Delta c_x < 1 \text{ px}$，$\Delta f < 0.5\%$。

**可分离性的来源：** 对纯主点误差（$\alpha_x = \alpha_y = 1$），重投影残差可以写成

$$r_{tj} = \left(\frac{X_{tj}}{Z_{tj}} + \beta_x\right) - x_{tj}^{(\text{obs})},$$

其中 $x_{tj}^{(\text{obs})}$ 是观测到的归一化坐标（已用 $\hat{K}_0$ 换算）。这是关于 $\beta_x$ 的线性函数，可以从非线性 BA 中分离出来，用线性方程组直接求解。焦距参数（$\alpha_x, \alpha_y$）也是线性分离的。这给出了高效的线性子问题，全局非线性最小二乘只需要在分离后的参数空间里做小范围搜索。

### E.3 BA Schur 补作为 Probe（而非估计器）

第四章的 Schur 补计算（`compute_shared_k_schur_response()`）**不是** $K^*$ 的估计器——它计算的是：给定某个假设的 $K^*$ 偏差，位姿会怎样偏移。这是一个"灵敏度分析"而非"参数估计"。

Probe 的正确用途是：
1. 给定 `SeparableSharedIntrinsicSolver` 估计的 $\Delta\theta_K$，预测对应的位姿偏移 $\Delta\xi_t$，用于诊断或先验校正（附录F）。
2. 计算可辨识性指标（Schur 条件数），判断当前场景是否处于退化状态（第六章）。

**Phase 2 中把 Schur 补直接当估计器使用的错误：** 原实现试图用 BA probe 的响应来反推内参偏差，等于把"如果有偏差会怎样"和"有多大偏差"混淆了。正确的估计器是 `SeparableSharedIntrinsicSolver`；Schur 补是诊断工具。

### E.4 可靠性检验与门控条件

在把 $\hat{K}^{(\text{est})}$ 提升为"可靠"状态之前，需要通过以下检验：

1. **最小观测数：** `SeparableSharedIntrinsicSolver` 需要足够多的共视点（建议 $\geq 50$ 对每帧，$\geq 3$ 帧）。对少于 3 帧的序列，估计不可靠。

2. **残差合理性：** 拟合后的归一化重投影 RMSE 应在 $0.5-2 \text{ px}$ 范围内。过大（$> 5 \text{ px}$）说明场景退化或估计发散；过小（$< 0.1 \text{ px}$）可能是过拟合或 COLMAP 跟踪误差被吸收了。

3. **Schur 条件数：** `principal_point_stability_metrics()` 给出的条件数 $\kappa < \kappa_\text{thresh}$（典型值 5-10）。条件数大说明场景逆深度方差小，内参可辨识性弱，估计结果不可信。

4. **估计值的物理合理性：** $c_x \in [0.3 W, 0.7 W]$，$c_y \in [0.3 H, 0.7 H]$，$f > \min(W, H)$。超出这些范围的估计值很可能是数值问题导致的，应该拒绝。

只有通过全部四项检验，`classify_prior_state()` 才应该把状态从 C 提升到 B。

### E.5 估计调度的六步安全流程

综合附录D和本章，完整的安全调度流程是：

**步骤 1（无先验初始化）：** 用 COLMAP 做初始重建，使用 SfM 估计的内参（EXIF 或默认 $f = 0.85 \max(W,H)$）但不使用任何 foundation model 先验。此步得到初始位姿 $\{T_t^{(0)}\}$ 和稀疏点云。

**步骤 2（内参估计）：** 运行 `SeparableSharedIntrinsicSolver`，利用步骤 1 的 COLMAP 跟踪估计 $K^*$。同时计算 Schur 条件数。

**步骤 3（可靠性判断）：** 通过第 11.4 节的四项检验判断 $K^*$ 估计是否可靠。

- 可靠 → 进入步骤 4（B 状态）。
- 不可靠 → 以低权重先验完成重建（C 状态），记录场景为内参可辨识性弱，返回警告。

**步骤 4（先验激活与校正/重跑）：** 用 $\hat{K}^{(\text{est})}$ 更新 foundation model 的推理。根据资源决定走 corrected、debiased 还是 rerun 路径（附录D 的 B1/B2/B3）。

**步骤 5（带先验的 BA）：** 用激活后的先验（深度、法向量、pointmap）作为软约束，做带内参优化的 BA（允许 $\theta_K$ 微调）。Schur 补响应此时用于校正先验的位姿贡献。

**步骤 6（最终输出）：** 输出最终 $K^*, \{T_t\}$，以及先验状态的诊断报告（哪些帧用了哪种路径，各帧的残差和条件数）。

### E.6 与代码的对应

| 步骤 | 代码模块 |
|------|---------|
| 步骤 1 | `colmap_stage_a.py` Stage A |
| 步骤 2 | `SeparableSharedIntrinsicSolver.fit()` |
| 步骤 3 | `classify_prior_state()` + `principal_point_stability_metrics()` |
| 步骤 4 | `correct_priors()` 或外部重跑 MoGe |
| 步骤 5 | `SharedKBundleAdjuster.fit()` (Stage B) |
| 步骤 6 | `run_courtroom_stage_ab.py` 输出 JSON |

`run_courtroom_stage_ab.py` 是当前的端到端实验脚本，实现了一个简化版本的上述流程（用合成数据验证各步骤的数值正确性）。生产级别的完整流程留待系统集成阶段。

---

## 附录F：偏差感知的校正先验——$C_T$、$C_Z$、$C_N$ 校正映射（系统化草案）

### F.1 校正先验的总体结构

当 $K^*$ 已被可靠估计，且 rerun 不可行（或代价过高）时，需要在不重跑 foundation model 的情况下，用 $H$ 对先验输出做最大程度的校正。此时有三类需要校正的先验量：

- **位姿先验（Pose prior）**：foundation model 预测的每帧相机位姿 $\hat{T}_t$，偏差来自第三章的 Procrustes 效应。
- **深度先验（Depth prior）**：$\hat{D}_t(u,v)$，偏差来自 $H$ 对相机坐标的仿射变形。
- **法向量先验（Normal prior）**：$\hat{n}_t(u,v)$，偏差来自 $H^{-T}$ 对平面法向量的变换。

对应三个校正映射 $C_T$、$C_Z$、$C_N$，分别处理这三类先验。

### F.2 位姿校正映射 $C_T$

由第三章和第四章，内参误差 $(\beta_x, \beta_y)$ 导致每帧位姿有系统性旋转偏差：

$$C_T: \hat{T}_t \mapsto T_t^{(\text{corr})} = \exp(-\widehat{\Delta\xi}^\wedge)\, \hat{T}_t,$$

其中 $\widehat{\Delta\xi}$ 是估计的位姿偏移量。在近轴极限下 $\widehat{\Delta\xi} = (0, 0, 0, \beta_y, -\beta_x, 0)^T$（注意符号：我们是在**反向**校正模型引入的旋转偏差，所以符号与第三章的 $\delta\omega$ 相反）。

对于有充分深度变化的场景，精确的 $\widehat{\Delta\xi}$ 来自 `pose_delta_from_jacobian()`；对近轴场景，用解析公式 `pose_delta_near_axis()`。

代码中 `correct_pose(T_hat, delta_xi)` 实现了这个映射，参数 `delta_xi` 由调用者提供（可以是解析版或数值版）。

**使用场景：** 当 foundation model（如 dust3r/MASt3R）本身会输出位姿估计时，这个校正把系统性旋转偏差去掉，使位姿估计更接近真实。对于只输出几何先验（深度、法向量）不输出位姿的 MoGe，这个映射不直接使用。

### F.3 深度校正映射 $C_Z$

深度图在像素坐标系下与 $H$ 无关（$H$ 的第三行是 $(0,0,1)$，深度不受影响），但 foundation model 通常输出的是一个**仿射不确定**的深度图——即深度预测有未知的尺度和偏置，不等于真实度量深度。

**深度仿射校正：** 拟合每帧的仿射参数 $(a_t, b_t)$，使得校正后的深度

$$D_t^{(\text{corr})}(u,v) = a_t \hat{D}_t(u,v) + b_t$$

最接近 COLMAP 稀疏点云在同像素处的度量深度。这是 `fit_depth_affine()` 的核心功能：用已三角化的 COLMAP 点作为稀疏地面真值，通过线性回归拟合 $(a_t, b_t)$。

**为什么只需要仿射而不是更复杂的校正？** 对仿射不确定性来自尺度-偏置不确定性的假设，仿射是充分的。Foundation model 的深度预测中，尺度不确定性（相差一个整体因子）是主要的，偏置不确定性（相差一个整体常数）是次要的但存在。若模型有更高阶的非线性深度误差（如随距离的非线性漂移），仿射校正不够，但这已经超出当前系统的处理范围。

**$C_Z$ 的正式定义：**

$$C_Z: \hat{D}_t \mapsto D_t^{(\text{corr})} = \hat{a}_t \hat{D}_t + \hat{b}_t,$$

其中 $(\hat{a}_t, \hat{b}_t) = \arg\min_{a,b} \sum_j \left(a \hat{D}_t(u_j, v_j) + b - Z_j^{(\text{COLMAP})}\right)^2$，$\{(u_j, v_j, Z_j^{(\text{COLMAP})})\}$ 是当前帧可见的 COLMAP 三角化点。

这是一个线性最小二乘问题，对每帧独立求解，代价极低。

### F.4 法向量校正映射 $C_N$

由附录A，

$$C_N: \hat{n}_t(u,v) \mapsto n_t^{(\text{corr})}(u,v) = \frac{(H^{(\text{corr})})^T \hat{n}_t(u,v)}{\|(H^{(\text{corr})})^T \hat{n}_t(u,v)\|},$$

其中 $H^{(\text{corr})} = \hat{K}_0^{-1} \hat{K}^{(\text{est})}$，$\hat{K}_0$ 是 foundation model 的假设内参，$\hat{K}^{(\text{est})}$ 是估计的真实内参。

代码中 `correct_normals(normals, H)` 接受一个形状为 $(H, W, 3)$ 的法向量图，对每个法向量逐像素计算 $H^T \hat{n} / \|H^T \hat{n}\|$，用向量化实现。

**精度边界：** 见附录A §A.6 的讨论。$C_N$ 在模型精确执行几何变换的假设下精确，在模型有学习的非线性响应时只能去除几何线性部分。

### F.5 Pointmap 校正

点图 $\hat{P}_t(u,v)$ 包含完整的三维坐标，等于 $H^{(\text{corr})} p^*_t(u,v)$（在 $C_T$ 还没有处理位姿的情况下是在帧 $t$ 坐标系下）。校正：

$$C_P: \hat{p}_t \mapsto p_t^{(\text{corr})} = (H^{(\text{corr})})^{-1} \hat{p}_t.$$

如果 $C_Z$ 已经给出仿射校正后的深度 $D_t^{(\text{corr})}$，可以用校正后的深度重新计算点图（基于真实内参 $\hat{K}^{(\text{est})}$ 反投影），而不是直接对 $\hat{p}_t$ 做 $(H^{(\text{corr})})^{-1}$ 变换。两种方式理论上等价，实践中后者（重新从校正深度和校正内参计算点图）更数值稳定，且对深度仿射校正有自动"吸收"。

代码中 `correct_pointmap(pointmap, H)` 实现了 $(H^{(\text{corr})})^{-1}$ 对 $(H, W, 3)$ 点图的逐像素变换。

### F.6 $C_T, C_Z, C_N$ 的顺序与相互依赖

三个映射应该按以下顺序应用，且有依赖关系：

1. **先 $C_Z$（深度校正）：** 因为后续的 $C_P$ 需要正确的深度，且 $C_Z$ 只依赖 COLMAP 稀疏点（不依赖其他校正的结果）。

2. **然后 $C_N$ 或同时 $C_P$（法向量和点图校正）：** 这两者都只依赖 $H^{(\text{corr})}$（已知）和各自的 foundation model 输出（不相互依赖），可以并行计算。

3. **最后 $C_T$（位姿校正，如果需要）：** 位姿校正依赖 $(\beta_x, \beta_y)$ 的估计，来自 $\hat{K}^{(\text{est})}$；它影响的是相机坐标系定义，从而影响深度和法向量在世界坐标系下的解读，但在帧坐标系下的表达（foundation model 输出的坐标系）不受影响。

### F.7 各路径的误差分析

| 路径 | $C_T$ | $C_Z$ | $C_N$ | 非线性误差 | 全局一致性 | 推荐场景 |
|------|-------|-------|-------|-----------|-----------|---------|
| raw | ✗ | ✗ | ✗ | 高 | ✓（自洽）| $K^* \approx \hat{K}_0$ |
| corrected | ✓ | ✓ | ✓ | 中（线性误差去除）| ✗（法向量不再自洽）| 无重跑资源，偏移小 |
| debiased | ✓ | ✓ | ✓（重计算）| 中 | ✓（重计算后自洽）| 无重跑资源，精度要求高 |
| rerun | N/A | N/A | N/A | 低（模型直接在正确 $K$ 下运行）| ✓（模型自洽）| 有重跑资源，精度最高 |

"非线性误差"列指：foundation model 响应与纯几何预测的偏差（附录C §C.6 的 $g_{\text{model}} \neq g_{\text{geometry}}$ 问题）。

---

## 附录G：门控与可切换先验——$\gamma_t$ 软门控、能量 $E_\gamma$ 与通道级权重（系统化草案）

### G.1 为什么需要门控

固定权重的先验（无论多大）都有一个根本问题：当先验是有偏的时候，权重越大，对 BA 的伤害越大。理想的先验权重应该是**自适应的**——在先验可信的场景（深度变化丰富、内参可靠估计、foundation model 在分布内）时给高权重，在先验不可信时自动退出。

门控（gating）机制提供了这个自适应性。本章描述三层门控：全局门控 $\gamma_t$、场景级可辨识性门控、通道级门控，以及它们如何组合成最终的先验权重。

### G.2 全局门控 $\gamma_t$：软 sigmoid 函数

对每帧 $t$，定义全局先验门控为

$$\gamma_t = \sigma\!\left(\frac{s_t - s_0}{\tau}\right), \quad \sigma(x) = \frac{1}{1+e^{-x}},$$

其中：
- $s_t$ 是帧 $t$ 的可信度得分（下文定义），数值越大代表先验越可信；
- $s_0$ 是阈值，超过此阈值门控打开（趋向 1），低于此阈值门控关闭（趋向 0）；
- $\tau > 0$ 是温度参数，控制开关的锐利程度。

$\gamma_t$ 作为乘性权重直接进入 BA 的信息矩阵：先验项的有效权重为 $\gamma_t^2 \cdot \Lambda_t^{(\text{base})}$，其中 $\Lambda_t^{(\text{base})}$ 是假设先验完全可信时的基础权重矩阵。

**可信度得分 $s_t$ 的构成：**

$$s_t = s_t^{(\text{ident})} + s_t^{(\text{K})} + s_t^{(\text{reproj})},$$

- $s_t^{(\text{ident})}$：来自场景可辨识性——$-\log \kappa_t$，其中 $\kappa_t$ 是第六章的 Schur 条件数。条件数小（场景深度变化丰富）时这项为正（贡献加分）；条件数大时这项为负（贡献减分）。
- $s_t^{(\text{K})}$：来自内参估计可靠性——$-\text{RMSEresid} / \sigma_K^{(\text{thresh})}$，其中 RMSEresid 是 `SeparableSharedIntrinsicSolver` 的拟合残差。
- $s_t^{(\text{reproj})}$：来自 BA 重投影误差——当 foundation model 先验点的重投影误差太大时，这项为负，提示先验可能与场景不一致。

代码中 `gate()` 函数计算的就是这个 $\gamma_t$（或其组合版本）。

### G.3 能量函数 $E_\gamma$

将 $\gamma_t$ 引入 BA 目标函数后，总能量为

$$E = E_{\text{reproj}} + \sum_t \gamma_t^2 E_t^{(\text{prior})},$$

其中：
- $E_{\text{reproj}} = \sum_{t,j} \|z_{tj} - \hat{z}_{tj}\|^2_{\Sigma_{tj}^{-1}}$ 是标准重投影误差（加马氏距离加权）。
- $E_t^{(\text{prior})} = E_t^{(\text{depth})} + E_t^{(\text{normal})} + E_t^{(\text{pointmap})}$ 是帧 $t$ 的先验能量，分别来自深度、法向量和点图的约束。

$\gamma_t$ 直接乘在先验能量上。当 $\gamma_t = 0$ 时，先验完全不起作用；当 $\gamma_t = 1$ 时，先验以全权重参与优化。这避免了"要么全信要么全扔"的硬切换，允许先验在可信度连续变化时平滑过渡。

**梯度传播：** $\partial E / \partial \gamma_t = 2\gamma_t E_t^{(\text{prior})}$。若 $\gamma_t$ 本身是可优化变量（联合优化 $\gamma_t$ 和状态），梯度会趋向把可信帧的 $\gamma_t$ 保持在高值、不可信帧的降至低值。当前实现中 $\gamma_t$ 不被优化，而是从外部可信度得分 $s_t$ 计算并固定，这是更稳健的选择（避免 $\gamma_t$ 为了减小总能量而崩溃为 0）。

### G.4 通道级门控（`gate_channels()`）

全局门控是对整帧的，通道级门控则对不同先验类型（深度、法向量、点图）分别设置权重。这是必要的，因为：

- **深度先验对内参主点误差不敏感**（$H$ 的第三行是 $(0,0,1)$），可以在内参不确定时也给较高权重。
- **法向量先验对内参误差高度敏感**（通过 $H^{-T}$），在内参不确定时应该给低权重。
- **点图先验综合了深度和角度信息**，敏感性居中。

`gate_channels()` 根据以下逻辑分别计算三个通道的权重：

```
depth_weight    = gamma_t * reliability_depth
normal_weight   = gamma_t * reliability_normal * gamma_K
pointmap_weight = gamma_t * min(reliability_depth, reliability_normal * gamma_K)
```

其中 `gamma_K` 是内参可靠性门控——当 `classify_prior_state()` 返回 `"unknown"` 时，`gamma_K = 0`，法向量权重被清零；当返回 `"reliable"` 时，`gamma_K = 1`，法向量权重恢复正常。

### G.5 先验状态与门控的联动

`correct_priors()` 是把附录F的校正映射和本章的门控结合起来的顶层函数。其逻辑是：

1. 调用 `classify_prior_state()` 得到状态（`raw`/`corrected`/`debiased`/`rerun`/`unknown`）。
2. 根据状态决定对各先验应用哪个校正映射（$C_T, C_Z, C_N$ 的某个子集）。
3. 调用 `gate_channels()` 计算各通道的权重，把状态信息传入。
4. 返回校正后的先验和对应的通道权重，供 BA 使用。

**重要设计原则：** 校正映射的应用和门控的权重是正交的。校正映射负责"把先验的偏差去掉"，门控负责"决定这个去偏先验有多可信"。即使校正后，先验仍可能因为其他原因（场景退化、模型分布外）而不可信，门控会把权重降低。反之，即使内参不确定（校正不完全），深度先验的相对顺序仍然有用，深度通道可以维持适度权重。

### G.6 与现有 foundation model 评分机制的关系

MoGe 等 foundation model 通常会输出一个置信度图（confidence map）或不确定性图（uncertainty map）。这个不确定性可以直接融入门控的得分计算：

$$s_t^{(\text{model})} = -\frac{1}{HW}\sum_{u,v} \sigma_{\hat{P}}(u,v),$$

其中 $\sigma_{\hat{P}}(u,v)$ 是模型输出的点图不确定性。这个项加入 $s_t$ 之后，模型自己觉得不确定的帧会得到更低的 $\gamma_t$。

当前代码在 `gate()` 中没有直接接入 MoGe 的置信度图（MoGe 当前版本输出 mask 而非连续置信度），但接口预留了 `model_confidence` 参数，可以在模型支持时启用。

### G.7 形式化视角：门控先验作为鲁棒损失

从优化理论的角度，带门控的先验能量

$$\gamma_t^2 \sum_{u,v} \|p^*_t(u,v) - p_t^{(\text{prior})}(u,v)\|^2$$

等价于对先验残差使用了一个"可关断"的二次损失：当 $\gamma_t$ 连续从 1 到 0 时，损失从普通 L2 连续降为零，形成一种特殊的鲁棒核函数。

与标准鲁棒核（Huber、Cauchy 等）的区别是：标准鲁棒核按照**残差大小**降低权重（大残差低权重），而我们的门控按照**先验可信度**降低权重（不可信帧低权重，无论残差大小）。两者可以结合：先验残差对 Huber 核做鲁棒处理（去除个别大残差异常值），再乘以 $\gamma_t^2$ 做整体门控（去除整帧的系统性偏差）。

---

## 附录H：完整优化调度——六步流程的实现细节（系统化草案）

### H.1 调度的总体目标

附录E给出了六步安全流程的概述，本章提供实现层面的细节：每步的输入/输出规格、边界条件、失败处理，以及步骤之间数据传递的格式。

完整调度的目标是：**在不损失精度的前提下，尽量多地利用 foundation model 先验；在先验不可信时自动降级到无先验模式，而不是让有偏先验污染重建。**

### H.2 步骤 1：COLMAP Stage A——无先验初始化

**输入：** 图像序列 $\{I_t\}$，默认内参猜测 $\hat{K}_0$（来自 EXIF 或 $f = 0.85 \max(W,H)$）。  
**输出：** 初始位姿 $\{T_t^{(0)}\}$，稀疏点云 $\{X_j^{(0)}\}$，特征跟踪 $\{(t, j, u_{tj}, v_{tj})\}$。

**关键参数：**
- 特征提取：SIFT 或 SuperPoint，描述符维度 128/256。
- 跟踪：连续帧 + 共视帧，最短跟踪长度 $\geq 3$ 帧（短于此的点不参与内参估计）。
- 初始三角化：基础矩阵分解，使用前两帧做初始姿态估计。

**失败条件：** 如果跟踪点数 $< 50$（总共），COLMAP 无法可靠初始化。此时降级到仅输出初始内参猜测，跳过后续步骤。

**代码：** `colmap_stage_a.py`，内部调用 `pycolmap` 或命令行 COLMAP。

### H.3 步骤 2：内参估计

**输入：** 步骤 1 的特征跟踪，当前内参猜测 $\hat{K}_0$。  
**输出：** 估计内参 $\hat{K}^{(\text{est})}$，拟合残差 RMSE，每帧的 Schur 条件数 $\kappa_t$。

**算法：** `SeparableSharedIntrinsicSolver`。固定步骤 1 的位姿（或用步骤 1 的位姿做初始化，同时优化位姿和内参），拟合共享内参 $(f_x, f_y, c_x, c_y)$，同时约束每帧的位姿在步骤 1 结果附近（Tikhonov 正则化）。

**输出格式：** `PinholeIntrinsics` 对象，包含：
- `fx, fy, cx, cy`
- `residual_rmse_px`：像素单位的残差 RMSE
- `condition_number`：$H_x$ 和 $H_y$ 通道的最大条件数
- `n_observations`：有效观测对数

### H.4 步骤 3：可靠性判断与状态分类

**输入：** 步骤 2 的 `PinholeIntrinsics`，`rerun_available` 标志（是否有资源重跑 foundation model）。  
**输出：** `PriorState`（枚举：`raw` / `corrected` / `debiased` / `rerun` / `unknown`），`gamma_K`（内参门控值 $\in [0,1]$）。

**逻辑（`classify_prior_state()` 实现）：**

```python
if residual_rmse > RMSE_THRESHOLD or condition_number > KAPPA_THRESHOLD:
    return PriorState.UNKNOWN, gamma_K=0.0
elif rerun_available:
    return PriorState.RERUN, gamma_K=1.0
elif normalized_shift_magnitude < SHIFT_SMALL_THRESHOLD:
    return PriorState.CORRECTED, gamma_K=1.0
else:
    return PriorState.DEBIASED, gamma_K=1.0
```

其中 `normalized_shift_magnitude = sqrt(beta_x^2 + beta_y^2)`（归一化主点偏移量级），`SHIFT_SMALL_THRESHOLD` 典型值为 0.02（即主点偏移小于 2% 的焦距长度）。

**Phase 6 修复的核心：** 原逻辑把 `normalized_shift_magnitude > threshold` 判断为"不推荐重跑"，现在改为"偏移大 → 更应该重跑或去偏"，仅当 `rerun_available=False` 且偏移大时降级到 `DEBIASED`（而不是降级到 `UNKNOWN`）。

### H.5 步骤 4：先验激活（校正或重跑）

**输入：** `PriorState`，foundation model 输出的原始先验 $\{\hat{D}_t, \hat{n}_t, \hat{P}_t\}$，$\hat{K}^{(\text{est})}$，COLMAP 稀疏点云（用于深度仿射校正）。  
**输出：** 校正后的先验 $\{D_t^{(\text{corr})}, n_t^{(\text{corr})}, P_t^{(\text{corr})}\}$，通道权重 $\{w_t^{(\text{depth})}, w_t^{(\text{normal})}, w_t^{(\text{pm})}\}$。

**状态 `RERUN`：** 用 $\hat{K}^{(\text{est})}$ 重跑 foundation model，直接得到新的 $\hat{D}_t^{(\text{new})}, \hat{n}_t^{(\text{new})}, \hat{P}_t^{(\text{new})}$。然后只做深度仿射校正（$C_Z$），因为其他先验已经在正确内参下生成。

**状态 `CORRECTED`：** 应用 $C_Z, C_N, C_P$，不重跑。通道权重：深度满权，法向量降权（因为非线性误差残留）。

**状态 `DEBIASED`：** 在 `CORRECTED` 基础上额外重计算法向量（从 corrected 点图的局部差分）。通道权重与 `CORRECTED` 相似但法向量权重可以略高（几何一致性已恢复）。

**状态 `UNKNOWN`：** 所有通道权重接近 0，先验退出 BA。仅保留深度的相对顺序（可选，如果相对深度先验被启用）。

### H.6 步骤 5：带先验的 BA（Stage B）

**输入：** 步骤 1 的初始位姿和稀疏点云，步骤 4 的先验（含权重），$\hat{K}^{(\text{est})}$ 作为初始内参。  
**输出：** 精化后的位姿 $\{T_t^{(\text{final})}\}$，精化内参 $K^{(\text{final})}$，密集或半密集结构（若使用点图先验）。

**BA 配置：**
- 参数化：位姿用 $SE(3)$ 左乘扰动（$\mathfrak{se}(3)$ 参数化），内参用 $(f_x, f_y, c_x, c_y)$，点用欧氏坐标。
- 先验项：深度 L2 loss（Huber 截断），法向量 L1 angular loss，点图 L2 loss。
- 正则化：内参在 $\hat{K}^{(\text{est})}$ 附近的 L2 正则，防止 BA 在退化场景中漂移。
- Schur 消元：先消掉点，只优化相机（位姿+内参），然后再固定相机求精化点。

**可选：内参-位姿联合优化。** 若 foundation model 先验权重足够低，可以在 Stage B 中同时优化 $K$，利用多视图约束进一步精化。若先验权重高（接近 `RERUN`），通常不需要再做内参优化（$\hat{K}^{(\text{est})}$ 已足够准确）。

代码：`SharedKBundleAdjuster.fit()`，使用 `scipy.optimize.minimize` 或 `ceres-solver`。

### H.7 步骤 6：输出与诊断报告

**输出 JSON 结构（与 `run_courtroom_stage_ab.py` 输出一致）：**

```json
{
  "intrinsics": {"fx": ..., "fy": ..., "cx": ..., "cy": ...},
  "prior_state": "rerun",
  "moge_intrinsics_injected": true,
  "per_frame": [
    {
      "frame_id": 0,
      "prior_state": "rerun",
      "gamma_t": 0.97,
      "depth_weight": 0.97,
      "normal_weight": 0.97,
      "corrected_normal_deg": 4.68,
      "rerun_normal_deg": 4.68,
      "condition_number": 3.1
    },
    ...
  ],
  "schur_condition_number_max": 3.1,
  "separable_solver_rmse_px": 0.31
}
```

诊断报告包括：每帧的先验状态、门控权重、法向量误差（corrected vs. rerun vs. baseline）、Schur 条件数、内参估计 RMSE。这些指标用于快速判断重建质量，以及识别哪些帧是"弱链接"（条件数高、门控低）。

### H.8 调度的边界条件与失败处理

| 情形 | 触发条件 | 处理方式 |
|------|---------|---------|
| COLMAP 初始化失败 | 跟踪点数 < 50 | 返回错误，不做后续步骤 |
| 内参估计发散 | RMSE > 10px 或 cx/cy 超出图像范围 | 降级到 `UNKNOWN` 状态 |
| 恒定深度场景 | $\kappa > \kappa_\text{thresh}$ | 降级到 `UNKNOWN` 状态，警告 |
| MoGe 重跑失败 | OOM 或 GPU 不可用 | 降级到 `CORRECTED` 或 `DEBIASED` |
| BA 不收敛 | 超过最大迭代数或梯度爆炸 | 返回步骤 1 的结果（无先验），记录失败 |

所有失败情形都应记录在诊断报告中，使调用者能够了解降级原因。

---

## 正文主线恢复

前面的附录A-H收纳了两类非主线材料：一类是像 Manhattan、曲率机制这样的分析分支，另一类是状态机、调度和接口层面的系统化草案。它们都值得保留，但都不该继续占用正文推进顺序。从这里开始，正文回到真正把项目往当前 checkpoint 推进了一步的内容：先是 MoGe 内参注入这个关键分叉点，然后是 May 7 checkpoint 对当前闭合程度的总结。

---

## 第七章：MoGe 内参注入——发现、根因与修复

### 7.1 问题的发现

在 Phase 5 的初始实验中，即使进行了完整的 corrected path（$H^{-1}$ 校正点图，$H^T$ 校正法向量），重建质量仍然比预期差很多：法向量误差约 27°，远大于理论预测的 4.7°（以 $\Delta c_x = 0.1 f$ 的主点偏移为例）。

调试排查的第一个猜测是 sign convention 问题：image warping 函数 `shift_image_principal_point` 是否正确地把主点偏移 $\Delta c_x$ 合并到了正确方向？Phase 6 发现并修复了这个 bug（实际生成了 $-\Delta c_x$ 的偏移），但修复后法向量误差仍有约 27°（baseline），rerun 路径才正确降到约 4.7°。

这说明 corrected path 本身存在根本性的限制，而不仅仅是 sign bug 的问题。进一步调查发现了 MoGe 的硬编码中心主点问题。

### 7.2 MoGe 内部假设的解剖

查阅 MoGe 源码 `moge/model/v2.py`，发现其 `infer()` 方法在内部做了以下操作：

1. **归一化图像：** 始终假设相机主点在图像中心 $(c_x^{(\text{MoGe})} = W/2, c_y^{(\text{MoGe})} = H/2)$，即归一化坐标下 $(\hat{c}_x, \hat{c}_y) = (0.5, 0.5)$。
2. **自估焦距：** 对每帧图像，MoGe 从预测的点图中回归出一个"等效焦距"$\hat{f}^{(\text{self})}$，使点图的角度和图像坐标一致（在中心主点的假设下）。这个自估焦距**不是**真实相机的焦距，而是"如果主点在中心，什么焦距使点图最自洽"的优化结果。
3. **返回的 intrinsics：** `infer()` 返回的 intrinsics 总是 $(f = \hat{f}^{(\text{self})}, c_x = W/2, c_y = H/2)$，无论输入图像实际来自什么相机。

**后果：** 当输入图像的真实主点不在中心时（例如 $c_x = 0.6W$），MoGe 的所有预测（点图、深度、法向量）都是在"假设 $c_x = 0.5W$"下计算的。这正是 $H$ 的来源：$H = \hat{K}_{\text{MoGe}}^{-1} K^*$，其中 $\hat{K}_{\text{MoGe}}$ 的主点是中心。

### 7.3 为什么外部重心化不够

一个直接的修复思路是：在送入 MoGe 之前，先对图像做重心化（把主点通过裁剪/padding 移到中心），然后在 MoGe 输出之后再映射回原始坐标系。

这对**点图**和**深度**可以工作：将图像平移使主点居中，MoGe 在居中图像上做预测，预测结果通过逆变换还原。

但这对**内部射线方向**不完全有效，因为 MoGe 在内部会自估焦距，而重心化图像的"有效焦距"（在居中坐标系下）和原始图像的"有效焦距"（在原始坐标系下）不同（裁剪改变了图像的有效范围，padding 引入了黑色区域）。更深层的问题是：MoGe 的后处理步骤在把网络输出的原始 ray maps 转换为标准化坐标时，使用了硬编码的中心主点假设，这个假设不会因为图像被重心化而消失——它是在模型内部的坐标变换逻辑里。

### 7.4 修复：`intrinsics=` 参数与 `recover_shift_from_rays()`

**修复 1：`MoGeModel.infer(intrinsics=normalized_K)` 参数。**

在 `moge/model/v2.py` 的 `infer()` 方法中添加可选参数 `intrinsics`。当 `intrinsics` 被提供时：

1. 跳过中心主点假设，用提供的归一化内参构造每像素的射线方向。
2. 跳过自估焦距步骤，直接用提供的焦距。
3. 用正确的射线方向从 MoGe 的内部 point map 计算深度和点图。

**修复 2：`recover_shift_from_rays(rays, K)` 函数。**

MoGe 在内部预测的是"原始 ray predictions"（一个 $(H, W, 3)$ 的张量），然后通过几何后处理把它转换为标准的三维点图。这个后处理步骤原本假设中心主点。

新添加的 `recover_shift_from_rays(rays, K)` 函数接受 MoGe 内部的 ray predictions 和外部提供的内参 $K$，通过以下方法恢复正确的点图：

1. 用 $K$ 构造每像素的真实射线方向 $r^* = (K^{-1}(u,v,1)^T) / \|(K^{-1}(u,v,1)^T)\|$（归一化）。
2. 从 MoGe 的 ray predictions 中提取 scale 信息（深度的相对大小），不提取方向信息。
3. 用真实射线方向 $r^*$ 乘以 MoGe 的 scale，得到正确的点图。
4. 计算所需的 z-shift：$z_{\text{shift}} = \text{median}(\hat{p}_z - r^*_z \cdot D)$，用于把 MoGe 的相对深度对齐到绝对深度尺度。

这个修复确保了 MoGe 预测的点图和深度是在真实射线方向下计算的，而不是在中心主点假设下。

### 7.5 实验验证

**实验设置：** 合成主点偏移 $\Delta c_x = 0.1 f$（约 0.1 焦距长度），生成对应的偏移图像，分别用以下三种方式送入 MoGe 并比较：

- **baseline（不做任何修正）：** MoGe 以中心主点假设处理图像。
- **corrected path（外部 $H^{-1}$ 校正）：** MoGe 以中心主点假设处理，输出后做 $H^{-1}$ 校正。
- **rerun with intrinsics=（注入内参）：** MoGe 用提供的真实 $K^*$ 处理图像。

**结果：**

| 指标 | baseline | corrected | rerun+intrinsics |
|------|---------|-----------|-----------------|
| depth MAE（相对） | 0.538 | 0.280 | 0.122 |
| point RMSE（相对） | 0.624 | 0.321 | 0.260 |
| normal error (°) | 27.3° | 15.1° | 4.7° |

**结论：**

1. 内参注入（rerun path）的法向量误差从 27.3° 降到 4.7°，接近理论值，验证了修复的正确性。
2. corrected path 有改善（从 27.3° 到 15.1°），但仍然是 rerun path 的 3 倍以上，证实了附录C关于"非线性模型响应无法被线性校正去除"的分析。
3. depth MAE 从 0.538 到 0.122（rerun），说明 MoGe 的深度预测对主点假设高度敏感，不仅法向量受影响。

### 7.6 修复的代码影响范围

MoGe 修复涉及外部仓库（`/data/users/mia/current/MoGe/`），这是一个 sibling checkout，不在主项目 `pinhole_calib` 内。修改的文件：

- `moge/model/v2.py`：添加 `intrinsics=` 参数，修改内部射线构造逻辑。
- `moge/utils/geometry_torch.py`：添加 `recover_shift_from_rays()` 函数。

`pinhole_calib` 侧的修改：

- `python/run_courtroom_stage_ab.py`：`run_moge()` 函数现在接受 `intrinsics=` 参数并传递给 MoGe；baseline 用 `base_shared`，shifted 用 `estimated`，rerun 用 `base_shared`（重心化后）。
- `README.md`：文档更新了 sibling checkout 依赖和 MoGe 默认中心主点问题的说明。

### 7.7 对 corrected path 改进策略的影响

这个发现改变了我们对 corrected path 的理解和改进策略：

**原来的理解（Phase 5 之前）：** corrected path 弱是因为 sign bug 或分类逻辑错误。修复这些 bug 就应该让 corrected path 接近 rerun path。

**更新的理解（Phase 6/7 之后）：** corrected path 存在根本性的非线性限制——MoGe 对非中心主点输入的响应不是精确的几何变换，而是包含了学习的近似和非线性。$H^{-1}$ 只能去除几何线性部分，无法去除非线性部分。这个限制不能通过修复 sign bug 来解决，只能通过重跑（让模型对正确主点输入做预测）来从根本上避免。

**实践建议：** corrected path 仍然有价值——它比 baseline 好 40-50%（法向量误差 15° vs. 27°），在资源受限的情形下是合理的降级选项。但在需要高精度法向量（例如室内场景重建、精细 mesh 恢复）的场景中，rerun 是必要的。

### 7.8 本章留下的后续问题

到这里，正文已经闭合的是：MoGe 的中心主点假设是一个真实根因，`rerun with intrinsics=` 明显优于输出后几何校正。由这一章继续长出来的后续问题，例如多偏移量误差曲线、真实 off-center 数据验证、以及模型是否存在学习到的主点补偿机制，不再放在正文展开，而统一留到附录I处理。

---

## 第八章：Checkpoint（May 7）——当前完成度、测试入口与问题闭合情况

### 8.1 本 checkpoint 的数据来源与测试入口

本节**只使用本次新生成的数据**，不引用 earlier smoke runs 作为结论依据。新的输出目录为：

- `python/output/checkpoint may7/1/courtroom_stage_ab_intrinsics3.json`
- `python/output/checkpoint may7/2/courtroom_stage_ab_intrinsics6.json`
- `python/output/checkpoint may7/run_3.log`
- `python/output/checkpoint may7/run_6.log`
- `python/output/checkpoint may7/unittest.log`
- `python/output/checkpoint may7/moge_py_compile.log`
- `stage_ab/checkpoint_may7/1/`
- `stage_ab/checkpoint_may7/2/`

本次 checkpoint 实际跑了 3 个不同入口，合计 4 次 fresh 运行：

1. **公共仓库单元测试入口：**
   `PYTHONPATH=python /data/users/mia/conda_envs/dc_nksr/bin/python -m unittest python.tests.test_prior_adapter python.tests.test_local_ba python.tests.test_separable_solver python.tests.test_image_ops`
   日志：`python/output/checkpoint may7/unittest.log`
   结果：`Ran 24 tests in 0.039s`, `OK`。

2. **最先进的消融入口：** `python/run_courtroom_stage_ab.py --num-images 3`
   输出：`python/output/checkpoint may7/1/courtroom_stage_ab_intrinsics3.json`
   这个入口使用当前最先进代码路径：`SeparableSharedIntrinsicSolver` 估计共享 `K`，MoGe sibling checkout 注入 `intrinsics=...`，并在同一次运行中同时评估 `shifted / corrected / rerun` 三路。

3. **最先进的消融入口：** `python/run_courtroom_stage_ab.py --num-images 6`
   输出：`python/output/checkpoint may7/2/courtroom_stage_ab_intrinsics6.json`
   目的不是提高分数，而是检查在更多帧、更多退化帧出现时，`prior_state` 与通道级门控是否仍能工作。

4. **sibling MoGe patch 自检入口：**
   `/data/users/mia/conda_envs/dc_nksr/bin/python -m py_compile /data/users/mia/current/MoGe/moge/model/v2.py /data/users/mia/current/MoGe/moge/utils/geometry_torch.py`
   日志：`python/output/checkpoint may7/moge_py_compile.log`
   结果：`exit_code=0`。这个入口只验证当前最先进路径所依赖的外部 patch 至少处于可导入、可编译状态。

**注意当前 `stage_ab` 中三个 ablation 桶不是一开始就并列设计好的三方案，而是沿着“能不能不重跑 FM”这个问题逐步演化出来的三个阶段性产物：**

1. **最早的问题不是“三选一”，而是“拿到 shared `K` 以后，下一步最省事的修复动作是什么”。**
   当时我们已经知道 principal point 有偏，但还没有确定 FM 的 wrong-`K` 响应到底有多少能被输出后的几何映射吸收。因此实现顺序天然是：
   - 先试**不改图像、只改 FM 的相机约定**；
   - 如果还不够，再试**不重跑 FM、只对输出做显式几何校正**；
   - 如果还不够，最后才试**把图像也一起送回去重跑**。

2. **因此这三个名字对应的其实是三次“越来越重”的干预，而不是三条从 day 1 就并列存在的产品路线。**
   - `shifted`：最轻干预，只改 FM 看到的 `intrinsics`；
   - `corrected`：中等干预，在 `shifted` 输出上再叠一层外部解析校正；
   - `rerun`：最重干预，把图像重心化后重新跑一次 FM。

3. **本 checkpoint 把这三步放在同一个表里，不是因为它们在设计地位上天然同级，而是因为它们正好对应一个清楚的消融问题：**
   在“已经估到 shared `K`”这个前提下，只增加下一层修复动作，误差还能不能继续下降？

**当前代码里这三个桶的严格定义如下：**

- `shifted`：对**偏移后的图像**，直接用估计的 `estimated K` 注入 MoGe 推理；这是当前代码里“原图直接吃正确相机”的路径。
- `corrected`：先做上面的 `shifted` 推理，再对输出应用 `correct_*` 外部校正映射。
- `rerun`：先把偏移图像重心化回目标相机，再以 `base_shared` 重跑 MoGe。

把执行顺序写成更明确的伪代码，就是：

```text
estimated K = SeparableSharedIntrinsicSolver(...)

shifted:
  MoGe(shifted_image, intrinsics = estimated K)

corrected:
  shifted_output = MoGe(shifted_image, intrinsics = estimated K)
  correct_*(shifted_output, current_intrinsics = estimated K, target_intrinsics = base_shared)

rerun:
  recentered_image = undo_principal_point_shift(shifted_image)
  MoGe(recentered_image, intrinsics = base_shared)
```

因此，本 checkpoint 评估的不是“旧版 raw wrong-K baseline vs. 新版修复”，而是**在当前最先进代码里，沿着这条实际演化出来的修复链，额外增加一层动作到底还有没有净增益**。

**这也是为什么当前阶段故意不直接做联合优化。** 现在的任务不是“把一切都塞进一个 solver 然后追求最小误差”，而是先做**约束资格审查**：判断哪些先验项只是有偏但可校正，哪些先验项在真实 FM 响应下仍然有毒，哪些路径有资格进入未来的联合目标。若过早把所有约束项耦合进一个 joint solve，系统指标可能变好，但我们会失去“到底是哪一项在起作用、哪一项在作恶”的可辨识性。

**因此，这一阶段真正要测的不是谁赢，而是 `corrected-rerun gap`。** `rerun` 在这里不是单纯的工程最优路径，而是“同一个 foundation model 在正确相机约定下能达到什么程度”的近似上界；`corrected` 则是在检验当前 $H$-based 几何偏差模型到底解释了多少真实 FM 偏差。若 `corrected` 非常接近 `rerun`，说明这些先验有资格被写成未来联合优化里的软约束；若两者距离很远，就说明当前几何校正还不足以支撑那条约束。

### 8.2 主线与附录状态总览

这一节不再把整篇文档按“所有章节都在同一层级上”来汇报，因为那会重新把分析分支和系统化草案误读成正文成果。下面的表按两类对象汇报：

- 正文主线：已经构成“提出问题之后一路推进到当前 checkpoint”的主链条。
- 附录材料：推进过程中写下的系统化草案、接口草案和下一阶段组织方式，保留存档，但不计作正文已经逐章闭合。

表中的“完成”含义是：该对象的核心命题已经在**代码、测试或 fresh ablation** 中有直接对应；“基本完成”表示核心结论已写清并有部分验证，但还缺一个关键闭环；“存档”表示材料里有有价值的局部结果，但它在当前 checkpoint 中既不是主线里程碑，也没有闭合成可直接进入系统的方案；“按计划暂缓”表示目标是明确的，但当前阶段故意不把它推进到工程实现，因为还在做约束资格审查。

| 对象 | 状态 | 当前已经闭合的内容 | 当前仍未闭合的内容 |
|------|------|--------------------|--------------------|
| 第 1 章 | 完成 | $H=\hat K^{-1}K^*$ 的定义、物理解释、代码对应已经稳定。 | 无实质性缺口。 |
| 第 2 章 | 基本完成 | 单视图不可辨识性已有正文推导，且 `SingleView.lean` 给出构造性存在性定理。 | 多视图临界运动的完整形式化仍未进入代码/Lean。 |
| 第 3 章 | 基本完成 | 近轴下“主点偏移优先吸收为 yaw/pitch”已有正文推导，`JointChannels.lean` 已形式化二维零残差结构。 | 非对角协方差和完整极分解最优旋转未作为运行时模块接入。 |
| 第 4 章 | 完成 | BA Jacobian、Schur probe、`local_ba.py`、`compute_shared_k_schur_response()`、`test_local_ba.py` 已对齐。 | 当前 BA 仍是 probe，不是最终联合解。 |
| 第 5 章 | 完成 | `pose_delta_near_axis()`、二维近轴解、Lean 二维扩展均已具备。 | 对宽视角/非近轴场景还主要依赖数值 probe，而非解析公式。 |
| 第 6 章 | 完成 | 退化、软零空间、`principal_point_stability_metrics()`、`HxMatrix.lean` 都已闭合。 | 条件数显式界仍停留在解析文本，不是运行时判据。 |
| 附录A | 存档 | `correct_normals()` 的 $H^{-T}$ 变换规律、`Normals.lean` 中的法向量不变量和 $1/2$ 系数来源是扎实的。 | MW 主方向识别、极分解最优旋转和进入实际流程都没有闭合，所以这一支还只是分析分支。 |
| 附录B | 存档 | 常数权仿射交换律在数学上是清楚的，也和 `AffineFusion.lean` 的常数权部分一致。 | 它的主要作用是澄清误解，而不是形成当前 checkpoint 的独立结论链。 |
| 附录C | 基本完成 | 空间变权导致额外项的离散版已经被 fresh 结果支持，`AffineFusion.lean` 给出有限差分形式化。 | 连续叉积项与 $\operatorname{Cov}(a,\eta)$ 仍是分析，不是可运行模块。 |
| 附录D | 完成 | `classify_prior_state()`、`recommended_prior_mode`、`rerun_recommended` 已进入当前实验入口。 | 这是状态策略的系统化归纳，不应被读成正文主线里单独闭合的一章结果。 |
| 附录E | 完成 | `SeparableSharedIntrinsicSolver` 作为主估计器，BA 只作 probe，这一调度已经落地并经 fresh run 验证。 | 文中讨论的更完整安全调度仍是系统化整理，而不是正文中的独立实验章。 |
| 附录F | 完成 | `correct_pose()`, `correct_depth()`, `correct_normals()`, `correct_pointmap()` 都在库中。 | `pose` 通道尚未在 `stage_ab` 中使用真实 FM pose 先验。 |
| 附录G | 完成 | `gate()`、`gate_channels()`、point/depth/normal 几何残差都在 fresh run 中实际出数。 | 门控尚未真正耦合进联合优化目标，只用于 summary-time 选择。 |
| 附录H | 基本完成 | 六步流程中第 1-4 步和第 6 步已在 `stage_ab` 中执行，第 5 步以 probe 形式存在。 | 真正“带先验的 Stage B BA”还没有实现；当前是诊断管道，不是最终求解器。 |
| 第 7 章 | 完成 | MoGe `intrinsics=` 注入、`recover_shift_from_rays()` 依赖路径、fresh 3 图/6 图 ablation 均已闭合。 | 仍缺多偏移量系统曲线和真实 off-center 数据集。 |
| 附录I | 按计划暂缓 | 联合优化目标、缺口与进入条件都已明确。 | 联合优化器本体尚未进入实现阶段，因为当前仍先做约束资格审查。 |

### 8.3 fresh ablation：当前最先进入口里，每一步到底有没有用

#### 8.3.1 三图 fresh ablation

运行入口（fresh work root: `stage_ab/checkpoint_may7/1/`）：

```bash
PYTHONPATH=python /data/users/mia/conda_envs/dc_nksr/bin/python \
  python/run_courtroom_stage_ab.py \
  --num-images 3 \
  --seed 7 \
  --output "/data/users/mia/current/pinhole_calib/python/output/checkpoint may7/courtroom_stage_ab_intrinsics3.json" \
  --work-root /data/users/mia/current/pinhole_calib/stage_ab/checkpoint_may7
```

输出文件：`python/output/checkpoint may7/1/courtroom_stage_ab_intrinsics3.json`

聚合结果如下：

| 路径 | point RMSE | depth MAE | normal error (°) |
|------|------------|-----------|------------------|
| `shifted` | 0.8492 | 0.4534 | 22.9915 |
| `corrected` | 0.9956 | 0.5446 | 22.8427 |
| `rerun` | 0.2614 | 0.1235 | 4.6767 |

额外诊断量：

- `gate = 0.8337`
- `pointmap_gate = 0.8337`
- `depth_gate = 0.8430`
- `normal_gate = 0.8267`
- `prior_state.state = "corrected"`
- `rerun_recommended = true`
- `principal_point_std_px = 0.4056 px`
- `cx error = +0.0096 px`
- `cy error = -0.0019 px`

**逐步消融结论：**

1. **从 `shifted` 到 `corrected`：point/depth 没有改善，反而变差。**
   - point RMSE: `0.8492 -> 0.9956`，恶化约 `17.2%`
   - depth MAE: `0.4534 -> 0.5446`，恶化约 `20.1%`
   - normal: `22.9915 -> 22.8427`，只改善约 `0.6%`

   这说明在**当前最先进代码**里，一旦 MoGe 已经拿到了 `estimated K` 做推理，随后再对输出套一层显式 `correct_*` 外部校正，point/depth 通道基本是在**二次修正**，没有带来净收益。

2. **从 `corrected` 到 `rerun`：三路指标都大幅改善。**
   - point RMSE: `0.9956 -> 0.2614`，改善约 `73.7%`
   - depth MAE: `0.5446 -> 0.1235`，改善约 `77.3%`
   - normal: `22.8427 -> 4.6767`，改善约 `79.5%`

3. **从 `shifted` 直接到 `rerun` 也仍然是大幅改善。**
   - point RMSE: `0.8492 -> 0.2614`
   - depth MAE: `0.4534 -> 0.1235`
   - normal: `22.9915 -> 4.6767`

   这说明真正有用的不是“输出后再校正一次”，而是**把图像和相机约定一起送回 foundation model 再推一遍**。

#### 8.3.2 六图 fresh ablation

运行入口（fresh work root: `stage_ab/checkpoint_may7/2/`）：

```bash
PYTHONPATH=python /data/users/mia/conda_envs/dc_nksr/bin/python \
  python/run_courtroom_stage_ab.py \
  --num-images 6 \
  --seed 7 \
  --output "/data/users/mia/current/pinhole_calib/python/output/checkpoint may7/courtroom_stage_ab_intrinsics6.json" \
  --work-root /data/users/mia/current/pinhole_calib/stage_ab/checkpoint_may7
```

输出文件：`python/output/checkpoint may7/2/courtroom_stage_ab_intrinsics6.json`

聚合结果如下：

| 路径 | point RMSE | depth MAE | normal error (°) |
|------|------------|-----------|------------------|
| `shifted` | 0.7900 | 0.4224 | 20.9849 |
| `corrected` | 0.8691 | 0.5083 | 20.9255 |
| `rerun` | 0.3517 | 0.1995 | 4.2742 |

额外诊断量：

- `gate = 0.4552`
- `pointmap_gate = 0.4552`
- `depth_gate = 0.4691`
- `normal_gate = 0.8041`
- `prior_state.state = "corrected"`
- `rerun_recommended = true`
- `principal_point_std_px = 0.0749 px`
- `cx error = +0.0616 px`
- `cy error = +0.0861 px`

**逐步消融结论：**

1. **`corrected` 依然不增益，继续恶化 point/depth。**
   - point RMSE: `0.7900 -> 0.8691`，恶化约 `10.0%`
   - depth MAE: `0.4224 -> 0.5083`，恶化约 `20.3%`
   - normal: `20.9849 -> 20.9255`，只改善约 `0.3%`

2. **`rerun` 依然显著优于 `corrected`。**
   - point RMSE: `0.8691 -> 0.3517`，改善约 `59.5%`
   - depth MAE: `0.5083 -> 0.1995`，改善约 `60.8%`
   - normal: `20.9255 -> 4.2742`，改善约 `79.6%`

3. **六图比三图更严格地暴露了门控的必要性。**
   - `frame_metrics` 中有 `2/6` 帧的全局 `gate = 0`
   - 这两帧是 `00170.jpg`, `00171.jpg`
   - 它们同时出现 `correspondence_residual = Infinity` 与 `depth_geometry_residual = Infinity`
   - 它们的 `correspondence_count = 0`

   也就是说，当前门控没有把坏帧“平均掉”，而是正确地把它们识别成几何上不可直接信任的帧。

4. **即便存在退化帧，rerun 路径的 normal 误差仍稳定在 `4.27°`。**
   这说明 rerun 路径的收益不是靠少数幸运帧，而是在较差几何条件下仍能保持稳定。

5. **即便在 `gate = 0` 的两帧上，rerun 也仍优于 `shifted/corrected`。**
   - `00170.jpg`: point `0.9162 -> 1.0258 -> 0.3414`，depth `0.5693 -> 0.6351 -> 0.1750`，normal `17.74° -> 17.75° -> 4.91°`
   - `00171.jpg`: point `0.5493 -> 0.6138 -> 0.1392`，depth `0.2862 -> 0.4228 -> 0.0961`，normal `20.81° -> 20.74° -> 2.11°`

   这说明 `gate = 0` 的语义不是“这帧所有信息都没用”，而是“不要再相信 point/depth 几何残差去驱动 response-based 解析修正”；直接 rerun 仍然可以恢复更好的 FM 几何。

### 8.4 这些 fresh 结果在几何上意味着什么

1. **共享 `K` 估计这一步已经足够好，不再是主要瓶颈。**
   - 三图 fresh run: `cx, cy` 误差只有 `+0.0096 px, -0.0019 px`
   - 六图 fresh run: `cx, cy` 误差只有 `+0.0616 px, +0.0861 px`

   对于本文讨论的 principal-point 漂移问题，这个量级已经远小于会主导误差的 FM 偏差。因此“先把 `K` 估出来再谈先验”的调度，当前是成立的。

2. **Schur probe / 稳定性诊断这一步是有用的，但它的作用是“拒绝过度解释”，不是“直接给答案”。**
   - 三图和六图的 `schur_condition_number` 都非常大（分别约 `8.68e11` 和 `2.19e15`）
   - 但 `rerun_recommended` 仍然是 `true`

   这正符合本文的策略：**高条件数应该阻断 response-based raw correction，不应该阻断更安全的 rerun 路径。**

3. **在当前最先进代码里，`corrected` 这一步对 point/depth 已经没有正增益。**
   这不是说校正映射数学上错了，而是说**当前入口里它插入的位置已经不对**。当 `shifted` 路径本身已经把 `estimated K` 注入进 MoGe，外部再做一次 `correct_*`，就不再是“从 wrong-K 输出拉回正确几何”，而更像是在已经几何对齐过的输出上继续施加额外变换。

4. **真正闭合本文核心问题的，是 rerun，而不是 corrected。**
   fresh run 给出的 strongest signal 很一致：
   - point/depth 指标的主要收益来自 rerun
   - normal 指标几乎全部收益也来自 rerun
   - rerun 后的 normal 误差稳定在 `4.27° - 4.68°`

   这意味着本文附录C和第七章的核心判断目前被 fresh 数据支持：**foundation model 对 wrong-K 输入的响应不是可由简单外部几何校正完全恢复的；只有让模型在正确相机约定下重新推理，才会真正回到接近正确的几何支路。**

### 8.5 这个 checkpoint 解决了什么，没有解决什么

**已经解决的：**

1. **附录D-H与第七章里那条“先估计 shared `K`、再判定、再选择 rerun/corrected”的安全路径已经有可运行入口。**
   当前 `stage_ab` 已经能：
   - 估计共享 `K`
   - 给出 `prior_state`
   - 做通道级 gate
   - 在 rerun 可用时选择 rerun
   - 用 injected intrinsics 跑出更好的 FM priors

2. **“shared-K BA 应该当最终估计器还是 probe”这个问题已经解决。**
   当前答案是：**probe**。fresh checkpoint 没有再暴露新反例。

3. **“corrected path 是否足以替代 rerun”这个问题，在当前最先进入口上已经有明确答案：不够。**
   而且不是“略差一点”，而是 point/depth/normal 都显著落后。

4. **“gate 有没有在做真实工作”这个问题也已经回答。**
   六图 fresh run 里，坏帧被明确压到 `gate = 0`，而不是被平均进 aggregate。

**还没有解决的：**

1. **附录H步骤 5 所承诺的“带先验的 Stage B BA”仍未实现。**
   当前只有 summary-time 路由与 probe，没有真正把 corrected/rerun priors 作为弱项写进联合目标。

2. **附录I对应的联合优化器仍未进入实现阶段。**
   目前仍然是顺序管道，不是 joint solve；这是当前阶段的研究设计，而不是遗漏。

3. **本 checkpoint 仍然是 synthetic principal-point shift，不是真实 off-center 数据。**
   因此它回答了“方法链条是否闭合”，但还没有回答“真实视频里效果是否同样稳定”。

4. **没有多偏移量系统曲线。**
   当前 fresh checkpoint 告诉我们“这套方法在一个固定偏移量上有效”，但还没回答“偏移量多大时 corrected 就一定不行、rerun 还能撑多久”。

5. **当前 `corrected` 桶的定义需要重新审视。**
   在 injected-K 时代，`shifted -> corrected` 这一层已经显示出过修正迹象。后续如果继续保留 corrected 路径，应该明确它究竟是：
   - raw wrong-K 输出的 fallback 修正，
   - 还是 injected-K 输出后的残差小修正。

   这两件事在几何上不是一回事，不能再混在一个桶里讨论。

---

## 附录I：联合优化为何暂缓——现有组件、资格审查与下一阶段

### I.1 已经存在的组件

经过本研究整个会话的开发，`pinhole_calib` 系统现在包含以下成熟组件：

**内参估计层：**
- `SeparableSharedIntrinsicSolver`：多视图共享内参拟合，精度可达 $\Delta c < 1\text{ px}$。
- `principal_point_stability_metrics()`：Schur 条件数计算，可辨识性诊断。

**先验校正层：**
- `correct_pose()`, `correct_depth()`, `correct_normals()`, `correct_pointmap()`：三类先验的 $C_T, C_Z, C_N, C_P$ 校正映射。
- `fit_depth_affine()`：深度仿射参数 $(a_t, b_t)$ 的稀疏点估计。
- `classify_prior_state()`（Phase 6 修复后）：正确的状态分类逻辑。

**门控层：**
- `gate()`, `gate_channels()`：软 sigmoid 门控和通道级权重。
- `correct_priors()`：校正+门控的顶层封装。

**BA 层：**
- `SharedKBundleAdjuster`：带共享内参的 bundle adjustment，Schur 消元。
- `compute_shared_k_schur_response()`：内参-位姿 Schur 补响应矩阵。
- `LocalBAProblem.with_observation_offset()`（Phase 2 修复后）：带观测偏移的 BA 问题构造。

**Foundation model 集成层：**
- MoGe `infer(intrinsics=...)` patch：允许外部内参注入。
- `recover_shift_from_rays()`：从内部 ray predictions 恢复正确点图。

**形式化验证层：**
- `lean/PinholeCalib/PrincipalPoint.lean`：一维近轴残差与 translation-only 配方定理。
- `lean/PinholeCalib/SingleView.lean`：三维仿射 chart 下的单视图重投影存在性构造。
- `lean/PinholeCalib/JointChannels.lean`：二维通道联合零残差与 roll 零化定理。
- `lean/PinholeCalib/HxMatrix.lean`：$H_x$ 的行列式-方差关系、有限点集满秩判据。
- `lean/PinholeCalib/Normals.lean`：$H^{-T}$ 法向量变换与 $1/2$ 反对称系数。
- `lean/PinholeCalib/AffineFusion.lean`：常数权仿射交换律与空间变权离散梯度项。

### I.2 为什么当前故意不做联合优化

当前系统是**顺序管道**（sequential pipeline）：先估计内参，再校正先验，再做 BA，每步独立。这与真正的"内参感知 BA"（intrinsics-aware BA）有本质区别。

但这里要强调：**当前没有联合优化，不是因为我们“还没来得及做”，而是因为现在仍处于消融与约束资格审查阶段。** 研究顺序必须先回答“这条先验到底 work 不 work、以什么方式 work、离 rerun 还差多远”，然后才允许它进入联合目标。否则 joint solve 只会把错误约束和正确约束一起耦合，最后即使指标改善，也无法判断究竟是哪个约束真正有资格存在。

换句话说，当前阶段的核心不是求解器能力，而是**约束建模是否成立**。这也是为什么第八章里最重要的对象不是单独的 `rerun` 分数，而是 `corrected-rerun gap`：这个 gap 直接衡量“输出后几何校正”离“正确相机条件下重新前向推理”的距离有多远。gap 小，说明未来联合优化只需做局部闭合；gap 大，说明问题还不是 solver 不够强，而是约束本身还没有通过资格审查。

**理想的联合优化器**应该同时优化：
$$\min_{\theta_K, \{T_t\}, \{X_j\}, \{D_t, n_t, P_t\}} E_\text{reproj}(\theta_K, T, X) + E_\text{prior}(D_t, n_t, P_t; \theta_K, T) + E_\text{prior-model}(D_t, n_t, P_t),$$

其中 $E_\text{prior-model}$ 是 foundation model 先验的软约束（鼓励先验与 foundation model 输出一致），而所有变量**同时**被优化。

当前的顺序管道是这个联合优化的**研究前置阶段**和一阶近似：先把内参响应、先验校正和门控逻辑拆开测清楚，再决定哪些项有资格被耦合进 joint objective。真正的联合优化会在内参和先验之间做多轮迭代，收敛到更好的局部最优；但那是建立在“约束项已经通过资格审查”的前提之上的下一阶段工作。

**缺失环节 1：外参-内参交替迭代。** 当前系统运行步骤 2（内参估计）时使用的是步骤 1 的位姿，而步骤 5（BA）完成后的位姿可能更精确。理想情况下应该做"步骤 2→3→4→5→2→3→4→5→…"的迭代，直到内参变化小于阈值。当前代码不做迭代（单次运行）。

**缺失环节 2：Foundation model 的梯度传播。** 理论上，MoGe 的 `intrinsics=` 参数可以把梯度传回内参参数，允许通过端到端梯度下降同时优化内参和 foundation model 的质量。当前 MoGe patch 只做前向推理，没有梯度支持。实现这个需要把 `recover_shift_from_rays()` 改写为可微分的 PyTorch 操作，并通过 MoGe 的网络做反向传播。这是一个显著的工程任务。

**缺失环节 3：多尺度内参先验融合。** 当场景包含不同距离的物体时，逆深度方差的主要贡献来自近处物体（§6.5）。当前系统把所有特征点等权对待，没有利用"近处点对内参约束更强"的先验知识来加权特征匹配的贡献。加权可以通过 COLMAP 给出的深度信息实现。

**缺失环节 4：非正面场景的处理。** 第三章假设点云协方差是对角的（正面场景），第五章在近轴极限下推导了精确解。对非正面场景（相机大角度倾斜、旋转楼梯等），位姿响应不再是纯偏航/俯仰，$H$ 对先验的作用更复杂。当前代码用近轴解析公式做快速近似，但数值 Schur 补路径（`pose_delta_from_jacobian()`）可以处理这些情形，只是未经大规模场景测试。

### I.3 direct correction path 持续弱的根本原因摘要

整合本白皮书各章的分析，direct correction path 弱于 rerun path 的原因从根本到次要依次是：

1. **MoGe 非线性响应（主因）：** MoGe 对非中心主点图像的响应不是精确的 $H$ 几何变换，包含了学习的近似和非线性。$H^{-1}$ 校正只去除几何线性部分（第七章）。这是单次实验中法向量误差 15° vs. 4.7° 差距的主要来源。

2. **法向量全局一致性破坏（次因）：** $H^T$ 对法向量的逐像素校正破坏了与深度图的全局几何一致性。边界处的曲率误差来自 $\nabla w \times (H-I)(p_1^* - p_2^*)$（附录C）。在平坦区域影响小，在深度不连续处影响大。

3. **深度仿射校正的残差（次次因）：** `fit_depth_affine()` 用稀疏 COLMAP 点拟合 $(a_t, b_t)$，在稀疏点覆盖不足的区域，仿射参数不准，深度校正有残差。与前两个原因相比这个影响相对较小。

4. **近轴近似误差（小量）：** 当场景点偏离光轴时，解析公式 `pose_delta_near_axis()` 不精确；但数值路径 `pose_delta_from_jacobian()` 可以去除这个误差，不是根本限制。

### I.4 未来工作的优先级建议

根据本研究的发现，后续工作的优先级建议如下：

**高优先级（影响大，可行）：**

1. **量化 $g_{\text{model}}$ vs. $g_{\text{geometry}}$ 误差曲线（由第七章引出）：** 用多个主点偏移量做系统性实验，给出 corrected vs. rerun 的误差随偏移量的定量关系。这直接回答"什么情况下 corrected 够用，什么情况下必须 rerun"。

2. **内参-位姿迭代（缺失环节 1）：** 在步骤 5 之后，用精化位姿重新运行步骤 2（内参估计），迭代 2-3 次。预期改善：内参精度从约 1px 降到约 0.3px。代价：2-3 倍内参估计时间，可以接受。

3. **真实 off-center 图像测试：** 当前所有实验都是合成主点偏移。在实际的非对称裁剪视频（具有已知标定内参的相机）上做端到端测试，验证六步流程的实际效果。

**中优先级（影响中等，工程量大）：**

4. **可微分 `recover_shift_from_rays()`：** 允许通过 MoGe 反向传播，实现端到端梯度优化。工程量：约 2-3 周，需要熟悉 MoGe 内部架构。

5. **非正面场景数值路径验证：** 在有大角度旋转的场景（楼梯、走廊转角）上测试 `pose_delta_from_jacobian()`，与近轴解析公式比较精度。

**低优先级（影响有限或长期）：**

6. **Lean 形式化的进一步扩展：** 若后续需要继续推进，优先项将是把第二章的 affine-chart 存在性定理升级为显式 $SE(3)$ 结构、把附录A的极分解 `polar(...)` 全链路也纳入 Lean，以及把第六章的条件数数值界做成谱量级定理。这些是理论完备性的要求，不影响系统实际精度。

7. **多尺度内参先验融合（缺失环节 3）：** 按逆深度加权特征匹配的内参贡献。改善量预期有限（特征匹配在近处已经很密集），工程复杂度中等。

### I.5 结语：顺序管道与联合优化的本质张力

本白皮书正文和附录中保留下来的这些数学分析，都指向同一个核心张力：foundation model 的先验和 BA 的几何约束不是独立的，内参的误差影响前者，前者的误差影响后者，后者对前者的反馈需要通过重跑或联合优化才能实现。当前的六步顺序管道是这个内在耦合的近似解法，它在大多数情形下工作良好，但在高精度需求或严重内参偏差的场景下，会留下不能通过更多 BA 迭代去除的残余误差。

消除这个张力的路径是联合优化器——一个把内参、位姿、结构和 foundation model 输出同时优化的框架。这超出了当前系统的范围，但本白皮书推导的每一个组件（Jacobian、Schur 补、$H$ 变换规律、门控设计、Lean 定理）都是构建这样一个联合优化器所需要的数学基础设施。

---

*正文至此结束。以下无正文。*  
*上面这一行是文档结构终止标记，不表示研究已经完成、系统已经定稿或联合优化已经实现。*
